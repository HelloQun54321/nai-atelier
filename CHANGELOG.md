# 更新日志

这里记录 NAI Atelier 独立维护版本的重要功能、修复和维护变更。同一天的记录按时间倒序排列，最新修改在最上方。

## 2026-09-04
### 优化:元信息字号 token 化，VIP 动效纳入动效偏好
- 新增 `--text-tiny/mini/micro/meta` 字阶（rem 基准），全站 308 处 `text-[8/9/10/11px]` 绝对像素任意值完成迁移：「字体大小」外观偏好现在对标签、计数等元信息同样生效。
- VIP 用户名彩虹字与徽章辉光的无限循环动画只在动效偏好「完整」下启用，系统减弱动效与「减少/关闭」偏好下静止。

### 修复:服务层竞态与资源治理
- 流式生成的真实费用（estimatedSpent）从 SSE 事件透传进历史记录，流式路径成本不再缺失。
- 滚动恢复追赶改为「成功写回且到位」才停止（上限延长到 3s），慢图/详情返回后位置恢复更可靠。
- 缩略图缓存 release 不再误杀已完成待写缓存的请求；局域网 401 改为单次全局广播而非逐图报错。
- 标签词典更新瞬间的在途旧分片请求按代际失效，不再短暂报错；Opus 额度与运行时配置轮询改为模块级共享订阅并在页面隐藏时暂停。
- 批量导入进度改为单调计数（不再回跳/越界）；画廊浏览记录写盘失败时内存态回滚保持一致。

### 优化:实验室交互健壮性
- 生成进行中禁止切换四模式（按钮禁用 + 逻辑层双保险），杜绝结果落入新模式上下文。
- 大图灯箱支持 Esc 关闭与焦点管理（role=dialog）；预览卡下载失败有明确提示。
- 移动端软键盘弹出时生成按钮跟随键盘上沿保持可见，不再被迫先收键盘。
- 画板空格拦截避开按钮等可激活控件与隐藏状态；蒙版撤销栈改用 ImageBitmap（不可用时回退）并补解码失败回滚与栈顶去重。

### 功能:NovelAI 密钥保管箱迁入 local-data 由本地服务保管
- 密钥清单与命名备注从浏览器 localStorage 迁入本地 D1（Worker `/api/nai-key-vault`），首次打开设置自动迁移并清除浏览器副本；浏览器不再全量持有所有密钥，仅「当前使用的密钥」按「记住」开关保留在原槽位（所有生图消费方不受影响）。
- Worker 不可达时回退浏览器本地存储并在恢复后自动同步，单机使用不丢数据。
- 全局设置同时补齐模态可访问性（role/aria/焦点管理）与 Esc 双发关闭守卫。

### 优化:管理器与模态层一致性与可访问性
- 新增 useModalA11y：模态/覆盖层统一 role=dialog、aria-modal、打开焦点移入、Tab 圈禁、关闭归还焦点；覆盖确认框、Vibe/角色参考/历史选图/反推面板等。
- 角色参考管理器选中态从固定青色改为跟随主题强调色；Vibe/角色参考/标签更新等页面手写图标与加载态收敛到共享组件。
- 移动端详情层支持安全模式标题遮挡；角色库触底加载失败提示并防抖；全局设置 Esc 与手势返回双发关闭加守卫。

### 优化:画廊与设计系统一致性收敛
- DesignSystem 新增共享 CloseButton/BackButton/PageSpinner/EmptyState/FavoriteButton；各画廊与列表的手写 SVG 图标、自绘收藏心形、页面级加载/空态全部收敛到共享组件。
- 图库详情侧栏断点从 xl 收敛到 lg：768–1279px 平板宽度改为桌面网格+侧栏详情，不再整屏覆盖跳层。
- AITag 预览图候选源修复：候选列表 memo 化（无关渲染不再重置重试进度），全部失败时显示带重试与原页链接的占位；详情轮询连续 3 轮无推进自动停止。
- 生成历史卡片抽为 memo 组件，单图加载/收藏只重渲染相关卡片。

### 修复:应用级事件监听每次渲染反复重挂
- `nai-agent-ui-preferences`/`nai-agent-navigate` 监听 effect 补依赖数组并经 ref 转发导航；全局 toast 的 `notify` 改为稳定引用，连带消除全局设置等页面的监听重复重挂。

### 修复:编辑器「放弃并离开」与生成中断处理的真实语义
- 放弃未保存更改现在会真正清除该串的工作区草稿：重进不再复活已放弃的修改，也不会再把当前图静默设为封面；从中断会话恢复草稿时如实标记「未保存」并提示，杜绝界面谎报已保存。
- 流式生成连接中断时不再自动重发：4xx 明确拒绝直接报错；网络中断等「结果未知」场景必须经用户确认后才重新生成，避免重复扣费。
- 生成途中离开编辑页：结果仍完整写入本地历史（产物不丢），不再对已卸载界面执行无效更新；离开自动补封面改用内存中的原图，修复偶发失败。

### 修复:角色预览与画师实装测试生成接入费用预检
- 两处此前绕过费用确认直发请求，且可使用 steps>28 / 付费模型参数静默扣费；现与实验室同一估算口径（受限模型先刷新真实 Opus 额度），扣费前必须确认，预算用尽或常量同步异常时红色警示确认；画师批量任务在入队时按「任务数 × 单耗」一次性确认，队列内不再逐张打断。

### 修复:取消排队等网关错误改为结构化识别，Agent 会话不再被单行坏数据中断
- API 层统一解析网关错误为带 `code`/`status` 的结构化错误（JSON 优先、长文截断），取消排队按 `QUEUE_CANCELLED`/499 判定、不再依赖中文文案匹配（旧文案仍兼容兜底）。
- Prompt Agent 流式事件逐行容错，坏 JSON 行跳过而非中断整个会话。

### 修复:实验室编辑资产可能被并发删除误清
- 蒙版自动保存与「换底图/重置模式」的删除使用同一确定性资产键，异步并发下删除可能落在后写入之后误删新蒙版；现同一资产键的写入/删除按发起顺序串行执行，从结构上消除竞态。
- 修复资产清理漏收「编辑结果」引用，避免仍在会话中的编辑结果被当作孤儿资产删除。

### 修复:历史迁移可能误删迁移窗口内新生成记录
- 浏览器历史迁移到本地服务时，先向其他标签页广播「远端接管」使新记录直接写远端；迁移改为多轮补迁增量、确认浏览器库无残留后才清空，清空前仍有新增则放弃清空并提示重试，杜绝迁移窗口内新生成被连带清除。
- 删除/清空历史时对远端与浏览器两侧副本做幂等清理，修复存储模式翻转后旧记录「复活」的问题。

### 修复:角色库「生成预览」封面重启后永久失效
- 生成结果此前直接以会话级 `blob:` URL 写入数据库封面，刷新或重启后封面空白；现先上传为持久资产再落库，并在数据读写边界统一拦截 `blob:` 封面（读取视为无封面、写入直接丢弃），防止同类回归。

## 2026-09-02
### 测试:清理已不再适用的画板右键拖动交互测试
- 移除与当前画板事件实现不再匹配的冗余测试用例，保留其余画板行为测试。

### 维护:执行全仓过度工程审计并移除个人模式死代码
- 删除个人模式下永远为空的客户端日志链路、未使用状态与导出、空 Worker Pixiv 路由及无效元数据构造，保留本地数据、存储键和旧客户端禁用兼容行为。

## 2026-09-01
### 修复:修复全屏精修大画板切换时位图丢失变全白的缺陷，并新增鼠标右键拖动画板
- **就地全屏容器切换**：全屏大画板改为在原 DOM 节点上无缝切换定位类，不再跨树卸载/重建 Canvas 节点，彻底杜绝浏览器清空底层 Canvas 位图缓冲区的问题；
- **鼠标右键拖拽平移**：编辑画板新增鼠标右键按住拖动漫游，并自动拦截默认右键菜单，提供丝滑的 CAD/图形软件级视口平移手感。

### 优化:将智能扩图画幅模拟台直接整合进底图面板
- **底图与扩图画板合一**：扩图模式下，「底图与导入」模块直接呈现目标画幅比例选择、可视化画幅模拟台（布）、可拖拽原图摆放与「应用画布扩展」操作，告别在底图区与参数区之间上下翻滚的割裂体验；
- **编辑参数区聚焦**：精简下方「编辑参数」模块，使 Strength 与 Noise 参数设置更紧凑清爽。

### 优化:切换目标比例或拖动原图时自动双向填充各方向扩图像素
- **动态填充白边**：切换比例或载入底图时，系统自动根据目标画幅差值与摆放方位（居中/靠边等）即时计算并填充上、下、左、右各方向所需的扩展像素数值（均为 64 倍数）；
- **拖拽实时同步数值**：在画幅模拟台拖动原图或点选快速对齐时，各方向输入框与四周标签即刻动态联动刷新。

### 修复:修复画幅模拟台因绝对定位导致舞台折叠为 0 且无法拖动的缺陷
- **动态容器几何适配**：采用 `ResizeObserver` 动态计算画幅模拟台边界，确保 9:16、16:9、2:3 等任意画幅外框均能完整按真实长宽比撑开居中渲染；
- **拖拽与事件捕获修复**：启用 `touch-none`、默认底图尺寸保护与高敏指针捕获，彻底解决原图缩略图过小及鼠标拖动不跟手的问题。

### 功能:扩图新增真实比例画幅模拟台（布）与鼠标直接拖拽摆放原图
- **真实比例画幅外框（布）**：根据选择的目标画幅（如 16:9、2:3、9:16、21:9 等）以真实长宽比渲染模拟画板，并在内部等比呈现原图缩略；
- **鼠标直接拖拽摆放**：用户可在模拟台直接按住原图拖拽调整摆放位置，系统以 64 像素为步长动态吸附计算四周白边与扩展尺寸；
- **对齐辅助与数值微调**：保留水平/垂直快捷对齐按钮、四周扩展像素实时徽章与精确数值调节。

### 功能:扩图支持按目标比例（16:9/9:16/1:1等）一键扩展与 3x3 九向方位对齐摆放
- **目标画幅比例智能换算**：支持一键将任意尺寸底图扩充至 `16:9 电脑横屏壁纸`、`9:16 手机全屏壁纸`、`1:1 方形头像`、`21:9 超宽带鱼屏` 等比例，并自动保证输出画布为合法的 64 整数倍；
- **3x3 九向方位锚点摆放**：提供可视化的九向对齐选择器（靠左/居中/靠右/靠顶/靠底/四隅），一键决定原图在扩展新画幅中的具体位置（如人物靠左、向右侧大面积延伸画背景），并保留像素级手动微调。

### 修复:修复 ImageEditCanvas 画布节点模板语法异常导致的本地打包失败
- **补齐缺失的 Canvas 标签**：纠正蒙版画布与 `canvasBoxStyle` 的结构闭合，恢复 `npm run build:local` 与生产打包编译成功。

### 修复:修复画板放大超过 150% 时因 max-w-full 约束导致图片被纵向拉长变形的缺陷
- **解除缩放宽度封顶**：在放大模式下自动解除 `max-w-full` 限制，使宽高始终按真实比例等比缩放；
- **优化双向漫游**：引入 `m-auto` 智能居中与边界对齐，确保大比例放大下水平与垂直全范围均可平滑滚动漫游。

### 功能:底图编辑画板支持自由缩放漫游与一键全屏大画板精修
- **视口自由缩放（Zoom）**：画板右上角增加快捷缩放浮条（`➕` / `➖` / `适应`），支持 `1.0x ~ 4.0x` 多档缩放与 `Ctrl + 滚轮` 平滑缩放；
- **平移漫游（Pan）**：放大后支持按住 `空格` 或 `鼠标中键` 拖拽漫游视口，快速聚焦五官、手部等局部细节；
- **全屏沉浸大画板（Fullscreen Modal）**：提供 `⛶` 全屏展开按钮，可在超大视野中从容框选与绘制蒙版，按 `Esc` 退出并实时无缝同步状态。

### 修复:修复 Opus 会员在 Focused Inpainting 模式下费用误标为扣点的缺陷
- **对齐官方订阅等级枚举**：兼容 NovelAI 官方服务端以 0 为基准的订阅枚举（`tier: 3` 为 Opus），不再因严格对比 `tier === 4` 导致真实 Opus 账号被误判为非 Opus；
- **恢复免费档正确展示与结算**：Opus 账号在局部重绘开启 Focused Inpainting 且未透支时，生成按钮与网关结算准确显示「Opus 免费」并不扣除 Anlas 点数。

### 修复:修复图生图模式下点击底图尺寸规范化按钮无响应的缺陷
- **适配无蒙版画布场景**：`applyNormalization` 兼容图生图模式无 `maskCanvas` 的状态，不再因蒙版画布为 `null` 提前中断执行；
- **全模式支持一键规范化**：点击「居中裁剪」「完整保留并填充」「直接缩放」后立即将底图与工作区画布规范化为合法尺寸，并正确同步更新左侧底图展示。

### 体验:移除图生图冗余的「点击看底图」与「以此为底图」悬浮按钮
- **精简右侧预览层**：彻底移除悬浮在工作区上方产生视觉遮挡与定位穿模的「结果 · 点击看底图」与「以此为底图」按钮；
- **左右对照更纯粹**：鉴于左侧底图区已常驻展示原图缩略图，右侧预览直接回归纯粹的结果展示、全屏查看与下载交互，消除画面冗余。

### 修复:修复图生图等编辑模式导入底图后生成按钮被误禁用的缺陷
- **纠正生成按钮禁用判定**：`ImageEditPreview` 不再以「右侧是否有结果展示（`displayedImage`）」来判定是否禁用生成，改为接收真实底图载入与尺寸校验状态 `canGenerate`；
- **解决首次生成死锁**：在导入底图但右侧尚无生成结果（`image` 为 `null`）时，桌面端「生成图生图结果」按钮正确保持可用并可直接点击发起生成；
- **同类项覆盖**：局部重绘与扩图模式同步享受一致的底图就绪即点亮判定，无底图或加载中时依然可靠禁用。

### 体验:图生图底图移入左侧「底图与导入」区展示，右侧预览只显示生成结果
- **底图位置与重绘/扩图一致**：图生图底图缩略图（含真实宽高）显示在左侧底图来源区，与局部重绘、扩图的画板同属「底图与导入」模块，遵循统一工作区语言；
- **右侧回归纯结果预览**：生成区只展示最近一次生成结果，未生成时显示引导文案；底图/结果 A/B 对比按钮保留（仅在结果存在时出现）；
- **移动端一致**：悬浮预览只跟随编辑结果，不再回退显示底图。

### 修复:图生图按整张底图全尺寸计费并透明展示，杜绝显示图与提交底图错位
- **修正图生图免费预期**：明确图生图按底图全尺寸计费（面积 × 步数 × Strength）、不享受免费档额度，并在编辑参数区前置计费说明，避免生成前账单意外；
- **根治「显示图 ≠ 提交底图」**：历史翻页、删除、清空不再覆盖编辑工作区结果预览；结果持久化为模式草稿，切走切回仍保留，可独立观察与 A/B 对比；
- **图生图迭代闭环**：新增「结果 · 点击看底图」A/B 切换与「以此为底图」一键把结果作为新一轮底图继续演化；历史详情标注父图，形成 A → B → C 演化链；
- **剔除图生图蒙版体系**：图生图不再渲染蒙版画布与蒙版状态机，仅保留底图画布，消除隐形 DOM 与跨模式泄漏；
- **换底图语义一致**：历史选择器默认只替换底图、保留当前提示词与参数（与文案一致），可勾选「同时导入该图参数」显式载入；
- **固定 seed 可复现**：编辑模式 seed=-1 由服务端随机并省略派生噪声种子，与文生图一致；
- **正确性修复**：Strength=0 不再回退默认 0.7 计费；无底图时生成按钮禁用并给出明确提示；「底图原 Prompt」绑定实际底图而非当前浏览的历史项。

### 修复:将 Infill 蒙版按官方发送链还原为全尺寸不透明黑白 PNG
- **补齐请求前最终蒙版转换**：`1/8` 透明/白二值蒙版只用于官方式膨胀与羽化计算；发送前以最近邻放大回请求画布尺寸，并将保留区编码为不透明黑色、重绘区编码为不透明白色；
- **修复重复斜纹扩展死区**：避免把低分辨率透明蒙版直接交给 NovelAI Infill 接口，导致后端错误解释蒙版尺寸与通道、在上下或左右扩展区生成重复灰色斜纹；
- **覆盖全部编辑入口**：普通局部重绘、扩图、Focused Inpainting 以及流式生成继续复用同一预处理结果，并增加全尺寸 RGBA 蒙版编码回归测试。

### 修复:对齐 NovelAI 官方 Infill/Outpaint 协议与回贴算法，彻底根治扩图区域条纹、噪点及死区生成异常
- **修正 `add_original_image: false`（对齐官方 Infill 规范）**：严格对齐 NovelAI 官方前端 Web 端 `generateInfill` 协议，在局部重绘与扩图模式下关闭服务端原图回贴，避免返回图与客户端羽化回贴发生双重合成；
- **扩图底图使用官方白色补底**：新增画布与透明像素统一用纯白填充后再叠放原图，避免把单行边缘像素拉伸成肉眼可见的横向或纵向条带；
- **蒙版严格按 Alpha 处理**：编辑器蒙版先以关闭平滑的方式缩至 `1/8` 并按 Alpha `> 155` 二值化；该低分辨率蒙版用于客户端羽化，发送前再恢复为全尺寸不透明黑白 PNG；
- **精准复刻官方客户端合成**：从内部 `1/8` mask 构建 4px 膨胀、8x 最近邻放大、20px 两轮定点模糊的羽化层，再以 `destination-in`、`destination-out`、`lighter` 完成单次回贴，避免接缝权重被重复衰减；
- **补齐编辑请求参数**：图生图发送 `color_correct: false`，Infill Strength 非 1 时发送 `{ strength, color_correct: true }`，随机编辑请求生成明确 Seed，并按官方规则设置 `extra_noise_seed = seed - 1`；
- **同类链路回归**：局部重绘、扩图、Focused Inpainting 与流式最终帧共用同一预处理和回贴逻辑，并新增低分辨率蒙版膨胀、缩放、羽化及 Payload 定向测试。

### 重构:全面优化图生图、局部重绘与扩图工作流，实现底图提示词与元数据自动继承及扩图交互升级
- **底图提示词与参数自动继承（根治扩图死白与空词问题）**：解决进入编辑模式提示词被静默置空的缺陷；无论是载入「文生图最新」、从历史库挑选底图，还是上传携带 NovelAI 信息的 PNG/WebP 本地图片，系统自动完整继承原始 Prompt、负面词、模型与生成参数，彻底消除扩图与重绘时因空提示词导致的纯白/噪点输出；
- **自上而下顺畅动线（板块层级重组）**：将左侧默认面板顺序重构为 **【底图与画布】 $\to$ 【提示词输入】 $\to$ 【编辑参数】 $\to$ 【生成参数】**，解决因果倒置与上下翻找痛点；
- **一键快捷扩图预设与尺寸预览**：扩图面板新增 `四周 +128px`、`四周 +64px`、`左右 +128px`、`上下 +128px` 一键快捷预设，并实时显示扩展前后的目标分辨率与扩图生成指导提示。
### 体验:全站滑块升级 0.01 高细腻度无级微调与直接数字点击输入
- **尺寸清晰度滑块无级调节与直接输入**：将尺寸放大滑块精度细化至 `0.01`，并将倍率数值升级为可直接点击修改的数字输入框，兼具顺滑拖拽与精准键入；
- **全站参数滑块体验统一（同类项补齐）**：同步为图像编辑模式（`Strength`、`Noise`、`笔刷大小`、`最小上下文`）、`Vibe Transfer` 强度及 WD Tagger 识别阈值滑块增加同款数字直接输入框，实现全站所有滑条控件交互规范彻底统一。

### 重构:彻底精简移除冗余的自定义像素输入，统一为纯粹的画幅比例与尺寸清晰度滑块
- **心智模型高度统一**：彻底移除多余的自定义像素输入框、免费联动开关与预设管理，生图参数统一为清晰的两步操作：**【选择画幅比例】（11 种常见预设） $\to$ 【调节清晰度放大】（1.0x 免费基准 $\sim$ 画幅极限）**；
- **消除用户心算与踩坑成本**：底层全程自动完成 64 像素整除对齐、长宽比保持与官方 2048px/3.14MP 物理边界钳制，彻底避免手动输入导致的比例失调、超限报错与五官崩坏；
- **外部历史自适应识别**：导入外部非标图片或历史参数时，由 `detectClosestAspectRatio` 自动吸附至最近的画幅比例与清晰度倍率，无缝兼容所有历史会话。

### 修复:尺寸滑块上限根据画幅比例自适应封顶，彻底消除滑块空转区
- **画幅真实上限动态自适应**：新增 `getMaxDimensionsForRatio`，根据各画幅比例精确推算触及单边 2048 像素或总面积 3,145,728 像素的真实几何极限（如 `9:21` 封顶 1.33x、`1:2` 封顶 1.45x、`9:16` 封顶 1.52x、`2:3` 封顶 1.69x、`3:4` 封顶 1.72x、`1:1` 封顶 1.69x）；
- **消除滑块空转区**：滑块最大行程动态匹配当前比例的真实上限，拉到最右端刚好是该比例下的最大合法分辨率，滑块每一步均有真实的尺寸与点数响应；
- **状态徽章与清晰标示**：达到画幅极限时显示「已达画幅极限」标签与官方封顶尺寸提示。

### 修复:尺寸滑块以 1.0x 原生免费为起步基准，严格封顶在 NovelAI 官方上限（2048px / 3.14MP）
- **清除低画质劣质区间**：将尺寸缩放滑块起点锁定在 **1.0x（Opus 免费最佳画质基准点）**，移除无画质与成本收益的 `<1.0x` 劣质区间，避免用户误触生成崩坏小图；
- **官方物理上限安全钳制**：在计算任意画幅缩放尺寸时，严格将单边尺寸封顶在 **`2048` 像素**、总面积封顶在 **`3,145,728` 像素**，彻底消除超出官方服务上限导致的 HTTP 400 校验报错；
- **自定义超限保护**：自定义尺寸输入限制最大 2048 像素，超限时展示红色警告状态。

### 功能:画幅比例与尺寸滑块解耦、常见画幅比例库、预设管理及 CFG 直接输入
- **画幅比例与尺寸滑块解耦**：预置 11 种常见画幅比例（1:1 方形、2:3/3:2 写真、3:4/4:3 竖横插画、9:16/16:9 壁纸、1:2/2:1 长幅、9:21/21:9 宽银幕），新增以 **1.0x（Opus 免费约 100 万像素）** 为基准点的尺寸清晰度（Scale）滑块，所有档位等比缩放且全程自动 64 像素对齐；
- **自定义尺寸预设管理**：支持一键将当前宽高保存为本地自定义预设（LocalStorage），支持快速切换与删除已存预设；
- **修复自由精确像素输入**：重构自定义像素编辑态，消除输入过程中击键即时量化锁死的缺陷，失焦/回车时规范对齐，持久化 Opus 免费像素联动开关；
- **CFG 精度提升与直接数字输入**：`CFG Rescale` 调节步进细化至 `0.01`，`CFG Scale` 与 `CFG Rescale` 顶部数值改造为可直接点击输入的数字输入框，兼具滑块拖拽与精准数字键入。

### 功能:图生图、局部重绘与扩图接入 NovelAI 流式生成与过程图即时预览
- **流式生图能力对齐**：新增 `generateImageEditStream` 服务，支持图生图、局部重绘与扩图模式通过 SSE（`/api/generate-stream`）接收 NovelAI 过程图（`intermediate`）与成图（`final`），与文生图保持完全一致的过程图流式反馈与进度展示；
- **优雅降级与异常回退**：当流式过程发生中断或当前模式/参考图不支持过程预览时，自动平滑回退至普通生成接口（ZIP），不影响成图结果；
- **全流程无缝合成**：成图返回后由 `composeImageEditResult` 自动完成选区裁剪还原与原图蒙版合成，保留完整的 `ImageEditMetadata` 并异步保存本地历史。

## 2026-08-31
### 修复:统一 ws 8.21.0，清除运行时 WebSocket 安全告警
- **传递依赖安全覆盖**：在 npm overrides 中将 AI SDK、jsdom 与 Miniflare 共用的 `ws` 从 `8.18.0` 统一提升到 `8.21.0`，修复碎片数据内存耗尽与未初始化内存披露告警；不新增直接依赖，也不升级 Wrangler/Miniflare。
- **审计收敛**：本地 `npm audit` 从 6 个受影响包降至 5 个，剩余项全部位于 Wrangler/Miniflare 工具链。

### 修复:升级 Vite 6.4.3 与构建链安全补丁
- **Vite 补丁升级**：Vite `6.4.1 → 6.4.3`，修复开发服务器 WebSocket 任意文件读取、Windows 路径绕过与 NTLMv2 哈希泄露等 4 条直接依赖告警，继续保持开发服务仅绑定 `127.0.0.1`。
- **构建链同步修复**：刷新 Rollup、PostCSS、Picomatch、Nanoid 与 Babel Core 的兼容安全补丁；本地 `npm audit` 从 12 个受影响包降至 6 个，剩余项均属于未在本次范围内处理的 Wrangler/Miniflare 与共享 ws 依赖链。

### 工程化:精简提交前质量门禁，避免重复运行无关测试
- **基础门禁保留**：pre-commit 仅保留暂存区密钥扫描、ESLint 错误检查与 TypeScript 类型检查；Gateway 与 Vitest 改为按改动范围定向执行，不再阻塞每次提交。
- **消除重复扫描**：`test:gateway` 不再隐式执行全仓库密钥扫描，密钥保护统一由独立命令与提交门禁负责；所有原有测试继续保留。

### 修复:本地服务 Wrangler 调试日志重定向到项目内，避免写满系统盘触发 ENOSPC
- **日志位置重定向**：`local-server.mjs` 将 wrangler 运行调试日志从系统盘全局目录（`AppData\Roaming\xdg.config\.wrangler\logs`，从不清零，实测累积 7GB 触发 ENOSPC）默认重定向到项目内 `.wrangler-logs/`（可整体删除），可用环境变量 `WRANGLER_LOG_PATH` 覆盖，启动信息中会显示实际日志位置。

### 修复:Keep-Alive 滚动恢复在容器隐藏期间保持追赶，杜绝窄屏详情关闭后位置丢失
- **隐藏期恢复锁与追赶保持**：`useKeepAliveScrollRestore` 的恢复锁与追赶定时器不再因 `clientHeight === 0`（如 `< xl` 窄屏打开详情时主容器 `hidden`）提前释放，容器恢复可见后由定时器自动完成恢复，不依赖 `trigger`/`active` 再次变化；
- **隐藏期 1200ms 超时自动续期**：容器保持隐藏时超时定时器自动顺延，恢复可见后按正常终止条件（到位或达最大可滚上限）结束追赶；
- **修复触发时机关联回归**：`useLayoutEffect` 在容器隐藏时仍更新激活时间戳，保证后续 `hidden → visible` 过渡瞬间的虚假 `scrollTop = 0` 事件落入 250ms 保护窗口；
- **补充追赶保持测试**：新增“容器隐藏期间追赶定时器保持存活、trigger 不变恢复可见后自动完成恢复”的确定性单元测试。

### 修复:Keep-Alive 滚动保持中追赶提前退出、恢复期虚假 0 事件冲刷及详情开关重跑缺失
- **校准追赶终止条件**：在 `useKeepAliveScrollRestore` 中修正 `tryRestore` 的终止逻辑，只有当实际 `scrollTop` 到位或达到容器最大物理可滚上限时才停止追赶，彻底解决“高度足够即误判为已恢复”导致首帧清除定时器、异步组件无法追赶的问题；
- **恢复期写入锁与时间窗防护**：在视图重新激活（`active` 变 true）与恢复追赶期间，屏蔽浏览器从 `display: none` 恢复可见时派发的初始 `scrollTop = 0` 虚假事件，防止冲刷有效历史缓存；
- **支持详情开关与重新可见触发**：引入 `trigger` 依赖，当用户在 AITag、Danbooru、Pixiv 等画廊中关闭详情侧边栏（主容器从隐藏恢复可见）时，自动重新触发滚动恢复；
- **useLayoutEffect 绘制前同步首帧**：在 DOM 布局完成后、浏览器绘制前同步执行一次恢复，消除切页时先回顶后跳回的视觉闪动。

### 修复:AITag 画廊跨页面往返切换后无法保持滚动位置而回顶的问题
- **移除冲突的旧版缓存覆盖**：清理 `AitagGallery` 内部遗留的手工 `cacheScrollPositions` 与一次性 `didRestoreScrollRef` 逻辑，消除页面切出隐藏时（`display: none`）由容器塌陷触发的无保护 `scrollTop = 0` 覆盖问题；
- **统一切页滚动保持**：主滚动容器纯粹由工程统一的 `useKeepAliveScrollRestore` hook 管理，并在主动搜索、切换排序或切换筛选时按需回顶；
- **持久化瀑布流卡片宽高比**：将 `aitagRatios` 纳入模块级生命周期缓存，避免切回视图时因卡片高度重算塌陷导致恢复位置被强制截断。

### 修复:媒体网关支持 AITag 图床防盗链请求头与原图直接代理，解决列表条带灰块与详情多图空白
- **网关远程抓取 Referer**：在 `requestRemoteBuffer` 中为 `ai-img.10118899.xyz` 目标附加 `Referer: https://aitag.win/` 与浏览器 User-Agent，防止上游图床 403 拦截，彻底解决瀑布流列表未缓存批次大面积灰块断续问题；
- **网关原图直出代理**：在 `/api/media?variant=original` 路由中将 AITag 图床纳入网关代理（不走 302 浏览器重定向，由网关带 Referer 抓取字节流返回），彻底解决作品详情中 P2/P3 多图区域空白无法展示的问题。

### 修复:空文件夹批量导入死循环、AITag 多图组远程加载及浏览器密码管理器误填搜索框
- **空文件夹扫描**：修复选择空目录时因未重置扫描状态导致的无止境转圈，及时阻断并友好提示；
- **AITag 详情多图**：统一放开外链图片的媒体网关代理（`/api/media?source=...&variant=original`），桌面端原图自动携带合法 Referer 并通过本地服务中转，彻底解决 P2/P3 远程图床直连防盗链拦截（403）导致图片空白的问题；
- **密码自动填充防御**：在局域网密码框添加 `autoComplete="new-password"`，并为 `ToolbarSearch` 全局搜索框添加 `type="search"`、`autoComplete="off"` 与密码管理器忽略标记，阻断浏览器自动回填用户 Key 别名到搜索框。

### 修复:历史页面、AITag 画廊与 Danbooru 画廊刷新按钮误将列表跳至更早历史
- **根因**：无限滚动向下加载时累加的当前页码变量（`page` / `currentPageRef.current`）被直接传给了刷新事件。刷新执行单页替换模式时，向后端请求了滚到底部的第 N 页（更早以前的数据），丢弃了第 1~N-1 页的最新图片，导致点击刷新直接跳向更早历史。
- **修复**：
  1. `GenHistory.tsx`：`handleRefresh` 重置 `currentPageRef.current = 1` 并请求第 1 页，同时滚动回顶；
  2. `AitagGallery.tsx`：移动端与桌面端刷新按钮统一固定加载第 1 页并重置滚动位置；
  3. `DanbooruGallery.tsx`：同步修复同类项刷新按钮，固定加载第 1 页。

### 功能:风格串批量导入支持「自动清理无用素材」与「导入后删除本地源文件（收件箱模式）」
- **背景**：批量导入文件夹图片时，原需进入二级弹窗手动确认删除无意义图片；且成功导入的图片若留在源文件夹，下次扫描会变成「已在库中」的重复垃圾素材，干扰后续批次。
- **改动**：
  1. 在批量导入选项面板新增「自动清理无用素材」（自动剔除无元数据、非 NAI、以及未导入的重复图片）与「导入后删除本地源文件」（收件箱模式，将已转存至工坊的本地原图移出文件夹）两个独立开关；
  2. 开关状态与「标记为待实测」均接入 `localStorage` 自动持久化记忆用户偏好；
  3. 执行导入时联动完成物理删除并合并反馈 Toast 提示，消除多步弹窗确认；
  4. 补充了自动清理候选集计算与边界保护的单元测试。

## 2026-08-29

### 安全:vite dev server 仅绑定本机回环，消除局域网暴露面
- **背景**：Dependabot 报告的 4 条 vite 漏洞（Windows 文件读取绕过、WebSocket 任意文件读等）全部只在 `npm run dev` 开发服务器运行时暴露，且此前配置绑定 `0.0.0.0`——开发期间局域网设备可达。
- **修复**：dev server 改绑 `127.0.0.1`，局域网设备彻底不可达，4 条漏洞的实际暴露面归零（大版本升级到修复版留待例行维护）。日常使用的 `dev:local` 走构建产物，不经过 vite，本就不受影响。

### 安全:overrides 强制升级 shell-quote 至 1.10.0，消除 critical 级告警
- **背景**：`shell-quote` 是开发工具 concurrently 的传递依赖，Dependabot 给 ≤1.8.4 标了 critical（quote() 不转义换行）。被利用的前提是"攻击者可控输入进入 shell 解析"，本项目该工具只处理 package.json 里自己写的命令——实际不可利用，但标签扎眼。
- **修复**：通过 npm `overrides` 强制钉到 1.10.0（补丁级覆盖）；concurrently 冒烟验证正常（依赖树中 rxjs 物理文件缺失的安装残留已顺手重建）。

### 安全:升级 sharp 至 0.35.4（libvips 8.18.6），消除不可信图片处理的内存安全漏洞
- **背景**：sharp 是网关给远端不可信图片（AITag/Danbooru/Pixiv 封面、历史原图）生成缩略图的组件，Dependabot 报告 0.35.0 以下版本继承了 libvips 的 4 个内存安全类 CVE（CVE-2026-33327 等）——恶意构造的图片可在缩略图处理时触发，是该链路上唯一的真实攻击面。
- **验证**：升级后以网关同款操作（resize + webp 输出）做功能冒烟通过，libvips 8.18.6；网关测试 140/140 通过。WD Tagger 的图像预处理同样基于 sharp，首图打标时留意一次即可。

### 安全:升级 undici 至 7.29.0，清空代理请求链路的 11 条依赖告警
- **背景**：GitHub Dependabot 报告的 32 条依赖告警中，`undici` 独占 11 条（含 4 条 high）——它是网关所有对外请求（AITag/Danbooru/Pixiv/NovelAI 经出站代理）的 HTTP 客户端，处理的全是远端不可信内容，其中 CRLF 注入、cookie 注入、缓存串读等条目在"代理不可信响应"场景下理论可达。
- **评估**：11 条中 4 条不受影响（SOCKS5 相关 3 条——项目使用 HTTP 代理；WebSocket DoS 1 条——项目未使用其 WebSocket 客户端），但升级一并清除、无需取舍。
- **验证**：`ProxyAgent`/`fetch`/`setGlobalDispatcher` API 签名不变（7.x 小版本升级），网关测试 140/140 通过。

### 性能:桌面端页面全部常驻（待机上限最终 9），彻底消除切页重建
- **背景**：此前上限先后为 4、8，但可保留页面有 9 个——只要轮换覆盖全部页面，就永远有一个被挤出卸载，切回时整页重建、全部封面重新挂载（浏览器实测确认：上限 8 时全轮换后返回页面 DOM 确实被重建）。
- **修复**：桌面端待机上限设为 9（全部可保留页面常驻、永不淘汰），配合图片组件"隐藏时保留"，浏览器实测全 9 页轮换后返回 AITag，页面 DOM 节点原样存活、封面零重新请求。手机端维持 4 不变。

### 性能:页面待机上限桌面端 4 → 8，多页来回切换不再整页重建
- **根因**：keep-alive 待机上限为 4，而可保留页面有 9 个——轮换的页面一旦超过 4 个，最久未看的页面就被整个卸载，切回来时整页重建、全部封面重新挂载（字节虽在浏览器永久缓存里，但"加载中"占位逐个浮现，观感如同重新加载）。
- **修复**：桌面端待机上限提高到 8（可保留页面几乎全部常驻），配合图片组件"隐藏时保留"的修复，在 8 个页面内来回切换均瞬时完整呈现；手机端维持 4（手机浏览器内存管理激进，避免整页被系统刷新），且手机端图片字节本就有 Cache API 持久缓存兜底。

### 修复:画师库"只看收藏"与角色库同源的问题——筛选改为按收藏清单渲染
- **根因**：与角色库同款——词库画师是无限滚动分页加载的，"只看收藏"只在"当前已加载的几十条"里过滤，散布在整个词库中的收藏（尤其未加载页的）永远显示不出来。
- **修复**：收藏画师时落一份展示快照（中文名/作品数，存于 `nai_artist_favorite_details`）；"只看收藏"改为**直接按收藏清单渲染**——优先取本地已保存画师，其次取已加载词库条目，最后取快照。旧收藏没有快照的，进入筛选时按名字向本地词库串行补齐（每条间隔 120ms；查不到的记名字占位避免重复检索）。`nai_fav_artists` 收藏清单格式不变，Agent 面板的读写不受影响。

### 修复:角色库"只看收藏"看不到词库角色——筛选改为按收藏清单渲染
- **根因**：词库角色是无限滚动分页加载的（每次进页面从第 1 页重来、不持久），而"只看收藏"只在"当前已加载的那几十条"里做过滤——收藏散布在整个词库里，绝大多数不在已加载分页中，永远显示不出来；自定义角色是全量加载的，所以看起来"只剩自定义角色"。收藏本身没有丢，仍完整保存在本地。
- **修复**：收藏的瞬间把条目展示信息（英文名/中文名/作品数）落一份本地快照；"只看收藏"改为**直接按收藏清单渲染**——自定义角色取本地链、词库角色取快照（或已加载条目），与标签页（词库/自定义/全部）的组合语义保持一致。旧收藏没有快照的，进入筛选时按名字向本地词库串行补齐（每条间隔 120ms；词库中已查不到的记名字占位，避免每次进入重复检索）。

### 修复:"启动时自动开启安全模式"开关恢复生效，启动状态由它唯一决定
- **根因**：该开关一直存在于安全模式开关下方，但上一轮修复（恢复"上次手动开关状态"）把 `nai_safe_mode` 记录的优先级压在了它之上——而这个记录几乎总有值，导致启动偏好形同虚设：上次手动关闭过安全模式的话，无论开关怎么设，重启后都是关闭。
- **修复**：按用户指定的语义反转优先级——开关**开启**则每次启动必定进入安全模式，开关**关闭**则每次启动必定关闭；会话中的手动开关只影响当前会话（`nai_safe_mode` 键继续作为当前状态镜像供 Agent 读取，不再参与启动判定）。

### 体验:切页往返不再闪现"加载中"——图库图片在页面隐藏期间保留不再拆图
- **根因**：图片组件（SmartImage，全站图库共用）此前在页面被切走（隐藏）的瞬间把该页所有图片元素整个卸载，切回来再逐个重建并显示"加载中…"占位——即使图片字节都在永久缓存里，每次往返也闪现一次整列表重载。AITag、历史、灵感、画师、角色、Danbooru、Pixiv 全部受影响。
- **修复**：页面隐藏时保留已显示的图片不再卸载（display:none 不产生任何流量），懒加载观察器仅在页面可见时激活新图；换图与手动重试仍正常重置。代价是隐藏页面在内存中保留已显示的图片。切回任意图库现在瞬时完整呈现。

### 修复:实验室实例被后台淘汰时误触发"自动设置封面"并跨页弹提示
- **根因**："离开编辑页自动补封面"挂在组件卸载时机上，而生图实验室（playground）恰好满足全部触发条件（按风格串处理、永久无封面、有生成图），却缺少"实验室豁免"——实验室是 keep-alive 常驻视图，当用户在其它页面间浏览、实验室被最近使用上限（4 个）挤出卸载的瞬间，就会把生成的图偷偷"设为封面"并弹提示，而用户此刻早已在其他页面。`handleSavePreview` 有实验室豁免，这里漏了。
- **修复**：`autoSaveCoverOnExit` 增加 `chain.id === 'playground'` 豁免，实验室不再参与自动补封面；真实风格串的自动补封面功能保持不变。

### 体验:去掉 AITag 画廊"无 prompt 作品不显示"的过滤规则，首次打开立即显示完整列表
- **移除的规则**：此前画廊会把"首图没有 prompt 元数据"的作品整条过滤（列表过滤、详情预览壳、详情图片列表三处），本意是挡住无法提取提示词的作品。
- **问题**：作品的 prompt 元数据由后台任务在搜索返回之后才逐条补齐（每条 0.5 秒起、整页要几十秒到几分钟），而"最新"首页的内容更新很快——第一次打开页面时返回的条目几乎都还没补完，于是被整页滤空，显示误导性的"没有匹配结果"，必须手动刷新才有内容。
- **现状**：按用户决定彻底去掉这条过滤，画廊与 aitag.win 所见即所得——第一次打开立即显示完整列表，后台补信息照常进行（封面缓存、详情缓存不受影响）；点开未带参数的作品，详情会显示"无 prompt_text"占位，模型版本筛选也不会归类它们。
- **附带说明**：追加翻页里"整页被滤空时续拉"的分支随之自然不再触发，逻辑保留用于应对上游偶发空页。

### 修复:备份完成提示每次打开设置页都误报；secret-scan 扩展漏网密钥模式
- **备份误报**：备份完成侦测不区分"本次会话刚完成"与"上次的历史完成"——只要上次备份是 completed 状态，每次打开设置页都弹一次成功提示，掩盖真正需要的新备份。首次观测现只记录基线。
- **密钥扫描**：扩展 secret-scan 漏网模式——sk 家族变体（Stripe `sk_live_`/`sk_test_`、OpenRouter `sk-or-v1-`、Anthropic 全系）、HuggingFace `hf_`、GitLab `glpat-`、Telegram bot token、无引号的 `API_KEY=字面量` 赋值；同时排除 `process.env.*` 等读取引用避免误报，仓库全量扫描通过。

### 修复:脚本层小修批次（流式参数守卫、桥接判空、版本脚本原子性、并发槽位、Tag 服务请求体）
- **流式参数守卫**：流式生图端点对缺 `parameters` 的请求直接 `delete` 抛 TypeError 返回 502，与非流式路径不一致——现返回 400。
- **桥接判空**：st-chatu8 Vibe 同步在 import 返回 200 但缺 item 时对 `result.item.id` 取值抛 TypeError，中断整个同步——现统一判空。
- **版本脚本**：`bump-version.mjs` 先校验 README 徽章再落盘，失败不再留下"包已升、徽章未升"的中间状态。
- **并发槽位**：媒体缩略图并发的 `withJobSlot` 单次判断存在唤醒竞态，短暂突破并发上限造成内存尖峰——改为循环等待。
- **Tag 服务**：POST 触发更新时消费并丢弃请求体，避免 keep-alive 连接被未读 body 污染。

### 修复:两处响应式样式缺陷（移动端圆形按钮被拉成蛋形、桌面端方形卡片偏好失效）
- **蛋形按钮**：移动端全局 `min-height: 44px` 触控规则会把卡片角上显式 28/32px 的圆形收藏/选择按钮拉成竖椭圆。新增 `.mobile-size-locked` 选择器供小圆钮保持原尺寸（历史收藏钮与灵感选择钮已应用）。
- **失效的桌面比例**：历史/灵感卡片比例规则（未分层作者样式）优先级高于 Tailwind utilities 层的 `md:aspect-square`，桌面端"方形"偏好实际从不生效——比例规则移入 `@layer components`，断点覆盖恢复正常。

### 修复:安全模式开关重启后总是回到启动偏好，手动状态丢失
- **根因**：`safeMode` 状态从 `nai_safe_mode_startup`（启动偏好）初始化，但手动开关持久化写入的是 `nai_safe_mode`——App 自己从不读取后者，重启后上次的手动开关状态丢失（该键此前只被 Agent 侧读写）。
- **修复**：启动时优先恢复 `nai_safe_mode` 记录的上次开关状态，无记录时回退到启动偏好。

### 修复:移动端画布绘制无多点触控防护，双指误触导致笔迹跳变
- **根因**：蒙版绘制的 pointer 事件不区分主指针——第二根手指/手掌误触会再次进入绘制分支并重置起点，笔迹在两指间跳变，还会把误触笔迹写入撤销栈；捏合手势也被当作绘画（画布 `touch-none` 阻止了滚动）。
- **修复**：绘制/框选的按下、移动、抬起三个处理函数均只响应主指针（`isPrimary`），多点触控不再干扰单指绘画。

### 修复:worker 后端请求处理加固（缺字段 500、NaN 绑定、缓存协商、统计虚高、Agent 概览）
- **缺字段 500**：创建风格串未传 name/description、基准图配置缺 config、灵感缺 updatedAt 等场景，undefined 直接进入 D1 bind 抛错变 500——现统一补默认值；坏 JSON 请求体也不再炸端点。
- **NaN 穿透**：Vibe 默认强度、灵感 useCount/updatedAt、历史 createdAt、st-chatu8 导入 createdAt 等数值绑定加 isFinite 归一。
- **缓存协商**：`/api/assets` If-None-Match 只做全等比较，浏览器带弱校验器（W/）或多值列表时缓存整包 200——现按规范解析。
- **统计虚高**：st-chatu8 历史导入 `INSERT OR IGNORE` 不看实际写入行数、一律 `imported++`——主键冲突静默忽略后返回值虚高，现按 meta.changes 计数。
- **Agent 概览**：`/api/agent/project-overview` 直接 COUNT vibe/角色参考表但从不 ensure schema——恢复自旧备份的库上稳定 500，现先补齐表结构。
- **外链卫生**：灵感 `sourceUrl` 入库只接受 https，防止 `javascript:` 等 scheme 渲染为链接（纵深防御）。

### 修复:风格串列表并发刷新无守卫，loading 状态互相踩踏
- **根因**：`App.refreshData` 无并发保护——创建/删除风格串与 Agent 数据变更事件可同时触发多次刷新，先完成者的 finally 提前清掉后者的 loading，响应乱序时旧数据覆盖新数据。
- **修复**：刷新加请求代际守卫，过期响应的状态写入与 loading 复位一律丢弃。

### 性能:Pixiv"加入灵感库"改为转存 R2 资产，不再把原图 base64 常驻内存
- **根因**：收藏 Pixiv 图片时以多 MB 的 base64 dataURL 直接入库，而 App 层会全量缓存灵感列表（keep-alive 下常驻）——连续收藏几张原图后内存占用显著抬升，移动端更敏感。
- **修复**：与手工上传灵感走同一条 `/api/upload` 转存链路，库里只保存 R2 资产 URL；原图不再以 dataURL 形式进入数据库与内存缓存。

### 修复:历史批量删除无异常保护，失败后选中集与列表不一致
- **根因**：批量删除循环裸 `await` 无 try/catch——中途一条删除失败即产生未捕获异常，选中集不清理、列表不刷新、无错误提示；清理预览数量请求也无兜底。
- **修复**：批量删除加统一异常处理（失败时保留选中集便于重试并提示）；清理预览计数失败时回退为 0。

### 修复:Agent 服务不可用时面板卡在"正在加载对话"；生图确认异常导致服务端任务永久等待
- **初始化吞错**：打开面板时会话/模型初始化失败被静默吞掉——面板永远停在"正在加载对话…"、输入框禁用，无任何提示。现显示错误信息并提供「重试」按钮。
- **finalize 缺口**：Agent 触发生图后，若前端生图流程本身抛异常，确认结果不会回传服务端——任务永久等待 finalize。现补 catch，异常时回传失败终态。

### 修复:四个图库/弹窗的竞态守卫缺口（快速操作时数据互相覆盖）
- **AITag**：后台自动追加分页的每一页都会抢占全局加载序号，把在途的用户搜索结果静默作废——搜索"看起来没反应"。现改为追加循环只取一次序号、每页让位给用户加载；同时加载完成的 finally 不再被过期旧请求提前触发（ spinner 提前消失、追加闸门提前打开）。
- **Danbooru**：同款 finally 缺口修复——过期旧加载不再提前关掉新加载的 spinner。
- **Pixiv**：快速连点两幅作品时，先点作品的"看了又看"慢响应覆盖新作品推荐；登录等待轮询遇一次网络抖动即永久停止、1 秒间隔无在途去重。分别加代际守卫、连续失败容忍（15 次）与在途守卫。
- **Vibe 库**：搜索防抖窗口内快速输入时旧响应覆盖新结果，加载态互相踩踏——加请求代际守卫。

### 修复:Tag 词库更新先删后写，中途失败留下残缺词库
- **根因**：更新脚本先递归删除 `public/tag-data` 再开始写几百个分片文件，manifest 最后才写——生成阶段任何失败（磁盘满、进程被杀）都会让词库变成空目录或半成品且无 manifest，前端自动补全与画师/角色目录全挂，必须重新联网下载。
- **修复**：改为先写入 `public/tag-data.staging` 暂存目录，manifest 就绪后原子替换正式目录；失败路径清理暂存残留，现有词库始终完好。

### 修复:Linux/macOS 启动脚本功能残缺，与主编排器严重不一致
- **根因**：`local-server.sh` 自行直启 wrangler，缺少 media-gateway、局域网 PIN 绑定、Agent、Pixiv、st-chatu8 桥与 Tag 更新服务，且端口布局（单进程 3000）与 `local-server.mjs`（网关 3000 + worker 3001）冲突——Termux/macOS/Linux 用户按文档跑 `.sh` 得到的是功能残缺的服务。
- **修复**：`.sh` 改为薄启动器，保留 Termux 环境检测与 Node 检查后直接 `exec` 委托给 `local-server.mjs`，各平台行为完全一致。

### 修复:开启 NAI_WRANGLER_LOG=all 调试日志会导致服务启动失败
- **根因**：该模式直接跳过 stdout 消费——wrangler 写满 64KB 管道缓冲后阻塞假死；同时看门狗因始终收不到"有输出"标记，在 25 秒后把健康进程误判为代理黑洞杀掉重启一次，随后 180 秒超时报「核心页面服务启动超过 3 分钟」退出。
- **修复**：新增 fullWranglerLog 透传函数——完整输出仍然持续消费管道并推进看门狗标记，调试模式恢复正常可用。

### 修复:局域网 PIN 锁定到期后失败计数归零，4 位密码可被按天爆破
- **根因**：worker 与网关两处 PIN 限流都在触发 60 秒锁定时把失败计数清零——每轮锁定到期后攻击者又有完整的 5 次尝试，4 位 PIN（1 万空间）可被约 7200 次/天的持续尝试逐日击穿，而成功后获得的是 30 天有效会话。
- **修复**：失败计数跨锁定周期累计、仅在成功解锁时清零（worker 与网关同步修改）；正常用户锁满一分钟再输对即可通过，体验不变。

### 修复:空蒙版提交局部重绘/扩图会白耗 Anlas
- **根因**：提交前只校验底图尺寸与 Focused 选区，不检查蒙版是否有内容——在局部重绘模式未画任何笔迹、或扩图模式改了数字却没点「应用画布扩展」时，全透明蒙版照常发出。NAI 的 infill 语义"白=重绘"下等于不重绘，但仍按编辑请求计费。
- **修复**：提交前逐像素检查蒙版 alpha 通道，为空时拦截并在画布上方显示对应提示（重绘请先涂画区域 / 扩图请先应用画布扩展）。

### 修复:Agent 快速切换会话可能显示错误对话；中文输入法回车误发消息
- **会话竞态**：会话历史加载没有请求守卫，快速点击会话 A → B 时，A 的慢响应会覆盖 B 的消息列表——此后发送的消息以 B 的界面呈现 A 的上下文。现加加载代际计数，晚到的旧响应直接丢弃。
- **输入法 Enter**：Agent 输入框与 AITag 搜索框的回车触发均未排除中文输入法组字状态，选词回车会把未上屏的拼音串当消息发出/当关键词检索。现统一检查 `isComposing`（与 TagAutocompleteTextarea 的既有正确做法一致）。

### 修复:移动端两处悬浮层互相遮挡（画师历史抽屉被导航条压住、Agent 悬浮球挡生成按钮）
- **画师库「复制历史」抽屉**（z-40）低于移动端底部导航（z-50），导航条直接盖住抽屉底部的「清空历史」按钮且遮罩盖不住导航——可带着抽屉开着的状态切页。抽屉/遮罩提升到 z-[60]/z-[55]，打开期间正常覆盖导航。
- **Agent 悬浮球**（z-950）在实验室页（隐藏底部导航）可拖到与生成 FAB（z-900）同高的右下角，把生成按钮挡住。悬浮球垂直位置上限在实验室页收紧到视口 78%，保证永不与 FAB 重叠。

### 性能:历史与 Vibe 表结构自愈逻辑加进程级缓存，不再每个请求重跑整套 DDL
- **根因**：灵感表此前已加进程内标记避免重复 DDL，但 `ensureLocalHistorySchema`（1 CREATE TABLE + 8 ALTER + 3 INDEX + 一次全表 UPDATE）与 `ensureVibeSchema`/`ensureCharacterReferenceSchema` 漏掉了——每个历史翻页/加图请求都要重付约 12 条 DDL 往返与一次无索引全表扫描，ALTER 靠抛异常兜底在 workerd 中代价不低。
- **修复**：与灵感表同型——三个 ensure 函数各加进程级幂等标记，同一实例只在首个请求执行一次。

### 修复:隐私模式/存储配额满时修改设置导致整页崩溃
- **根因**：外观偏好与图片显示偏好的 `localStorage.setItem` 无异常保护，而外观偏好保存在 `useLayoutEffect`（渲染阶段）——Safari 隐私模式、锁定模式或配额满时 `setItem` 抛异常会击穿 React 树白屏，且每次改任意设置都会复现。同项目的其他偏好写入均有防护，唯独这两处遗漏。
- **修复**：两处写入加 try/catch 兜底——持久化失败只影响跨会话记忆，当前会话内的设置状态照常生效。

### 修复:.naiv4vibe 导入失败路径留下零编码脏数据
- **根因**：导入接口先写入 Vibe 资产行与原图/缩略图到 R2，再扫描编码；全部编码无效时对客户端返回 400，但库里已留下一个零编码的 Vibe 条目与孤儿文件，重试同一文件反而"成功"，掩盖首次失败。
- **修复**：改为先完整校验编码（参数范围、Base64 格式、字节长度）再落库——全新 Vibe 没有任何可用编码时立即 400，不产生任何写入。

### 修复:画师基准图数据损坏后该画师永久无法编辑（两处裸 JSON.parse）
- **根因**：画师保存路径直接 `JSON.parse` 数据库 `benchmarks` 列（同列的删除路径却有容错解析，防御策略不一致），列一旦损坏（旧版本写入/备份恢复）该画师每次保存都 500；基准图配置读取端点同样裸解析，settings 表值损坏后稳定 500。基准图数组内的非字符串元素也会让 `startsWith` 抛 TypeError。
- **修复**：保存路径改用与删除路径一致的 `parseStoredJson` 容错解析；基准图数组元素先做字符串归一再判断前缀；`/api/config/benchmarks` 读取加容错，损坏值按空配置返回。

### 修复:重存本地历史会把 st-chatu8 外链标记抹掉
- **根因**：`POST /api/local-history` 使用 `INSERT OR REPLACE`（整行删除重插），但列清单缺少 `external_source`/`external_id`——同 id 重传外部来源行时这两个字段被重置为 NULL，`localHistoryImageUrl` 随之指向不存在的 R2 对象。收藏字段有读回保留逻辑而外链字段没有。
- **修复**：INSERT 列清单补上两个外链字段，重存时优先取请求值、否则保留数据库已有值（与收藏保留逻辑同型）。

### 修复:流式生成中用户取消被前端识别为错误而非取消
- **根因**：网关流式端点把取消消息写为「已取消生成」，而前端排队状态机（与非流式端点）匹配的是「已取消排队」——用户在流式生成中主动取消后，队列状态以红色错误样式显示并停留 8 秒，而非安静的取消态。
- **修复**：统一网关流式与非流式路径的取消消息为「已取消排队」，与前端终态判定对齐。

### 修复:任一视图渲染崩溃导致整站白屏，新增顶层错误边界
- **根因**：全项目没有 ErrorBoundary，而 App 以 keep-alive 方式同时挂载最多 4 个重型图库视图与 2 个实验室实例——任何一个视图渲染抛错（如后端返回异常数据形状）都会卸载整棵组件树，只能手动刷新。
- **修复**：入口处新增顶层 ErrorBoundary，崩溃时显示带错误信息与「刷新页面」按钮的可恢复页面，本地数据不受影响。

### 修复:角色库/画师库清空搜索词后加载状态永久卡死
- **根因**：搜索防抖 effect 在搜索词为空时直接返回、不复位 loading；同时在途请求的完成回调会被先行递增的请求代际守卫拦下——输入字符后在防抖窗口内退格清空（中文输入法快速删词极易触发），加载横幅/搜索 spinner 便永久停留。
- **修复**：清空搜索词分支同步复位加载状态（`CharacterLibrary` 与 `ArtistLibrary` 同源修复）。

### 修复:手机端生成结果"下载/设为封面"零入口，平板生成后无可见反馈
- **根因**：实验室预览卡在 <lg 整体 `display:none`，下载与设为封面只存在于该卡片——手机与平板没有任何入口；且"生成完成后自动弹大图"用 767px 判断，与预览卡消失的 1024px 断点错位，平板（768–1023）生成完成后既无预览卡也不弹图，唯一反馈是右下角悬浮缩略图悄悄换图。
- **修复**：生成完成后的自动弹灯箱判定改为与布局断点一致的 <lg；大图灯箱左上角新增操作条——「下载」始终可用，「设为封面」在查看的正是当前生成图且为串所有者时显示（语义与桌面预览卡一致），手动上传封面仍保留在桌面预览卡。

### 修复:编辑模式切换的蒙版状态机三处竞态
- **蒙版跨模式残留**：切换实验室编辑模式（如局部重绘 → 扩图）时，Panel 会以"新模式 + 上一模式的蒙版数据"渲染一帧，旧蒙版被画进新模式画布并可能随生成请求发出。现于切模式前先清空蒙版态，再由各模式草稿异步恢复自己的蒙版。
- **扩图后撤销栈过期**：应用画布扩展后撤销/重做栈未清空，Ctrl+Z 会把旧尺寸快照按原坐标贴回扩大后的画布，把扩图白边错误恢复成"保留"语义。现在扩图应用时清空两栈。
- **Ctrl+Z 过期闭包**：键盘撤销/重做 effect 为空依赖，捕获的是挂载首帧的回调闭包（其中的编辑模式是挂载时的模式），撤销可能写进另一个模式的草稿。现改为经由 ref 始终调用最新一轮渲染的处理函数。

### 修复:局部重绘/扩图画布三套尺寸路径不同步，蒙版坐标与可见底图错位
- **根因**：编辑画布的底图 canvas 由 `max-h-[62vh]` 自行约束、蒙版/叠加 canvas 填满 aspect-ratio wrapper、wrapper 又被容器高度钳制——三者尺寸路径互不相同。视口偏矮（如竖图叠加参数面板把画布区压矮）时，蒙版坐标系与可见底图分离，笔刷落点系统性偏移且 Focused 选区框失准。
- **修复**：`ImageEditCanvas` 改为按容器实际可用空间（含 62vh 上限）用 ResizeObserver 精确计算唯一显示尺寸，wrapper 显式定宽高，底图/蒙版/叠加三张画布统一绝对定位填满同一盒子——坐标映射自洽、所见即所得；jsdom 等无 ResizeObserver 环境自动退化为窗口 resize 监听。

### 修复:封堵上传通道的存储型 XSS 与配额绕过
- **根因**：`/api/upload` 不校验文件大小，且 Content-Type 直接采信客户端声明（可存任意 `text/html`）；`/api/assets` 响应又缺 `X-Content-Type-Options: nosniff`——持有局域网会话的设备可上传 HTML/脚本内容并在应用同源执行，调用全部 `/api/*`（清历史、删库、改预算）。
- **修复**：上传仅接受 PNG/JPEG/WebP（按扩展名白名单推导 Content-Type，不再采信客户端声明），并加上与 base64 路径一致的 12MB 上限；`/api/assets` 响应补 `nosniff`，禁止浏览器嗅探 MIME。

### 修复:移除全局限 CORS 通配头，封堵恶意网页无认证读写本机 API 的通道
- **根因**：worker 所有响应（含回环直连）都携带 `Access-Control-Allow-Origin: *`，而回环 Host 本身免认证——本机浏览器访问任意恶意网页时，该网页的 JS 可直接 fetch `localhost:3000/api/*` 读取风格串/历史/灵感并增删改数据（drive-by CSRF）。同源单机应用本不需要任何 CORS 头。
- **修复**：删除 worker 全部 `Access-Control-Allow-*` 响应头；同源 SPA 不会发起预检，跨源预检因无 CORS 头必然失败。OPTIONS 分支保留并改为空 204。

### 修复:局域网设备发送非法 JSON 可令本地服务进程崩溃（未认证远程 DoS）
- **根因**：网关 `handleLanUnlock`、`handleLanPinUpdate` 与 `/api/generation-queue/cancel` 三处直接 `JSON.parse` 请求体且无异常捕获，调用点也无 try/catch 包裹——局域网内任意设备无需密码即可发送畸形 JSON 触发未捕获异常，Node 默认行为是整个进程退出，本地服务随之全站不可用。
- **修复**：新增 `readJsonBody` 安全解析助手（空 body 与非法 JSON 一律按空对象处理），替换三处裸解析；畸形 PIN 会正常走"密码不正确"路径并计入锁定计数。

### 修复:生成排队/上游响应超过 30 秒时连接被网关误杀，可能"已扣费却拿不到图"
- **根因**：网关读取请求体时设置的 30 秒超时实际作用于底层 socket 的空闲超时，body 读取完毕后并未解除——生成排队等待、NovelAI 上游生成、流式首帧到达前的任何超过 30 秒的静默期都会把连接销毁；非流式路径下上游已完成、Anlas 结算已执行，但图片无法送达客户端。
- **修复**：请求体读取完成后立即解除 socket 空闲超时（`setTimeout(0)`），慢速客户端发送 body 的防护保留不变。

### 功能:生成步数上限改为可关闭的设置开关
- **新增设置**：全局设置新增「生成步数锁定在免费额度内」开关，默认开启——步数输入框锁定在官方免费门槛内（不再写死 28，而是跟随官方运行时同步的 `freeMaxSteps`，官方调整门槛后自动更新）。
- **付费探索**：关闭开关后允许手动输入更高步数（NovelAI 硬上限 50 步），输入框旁显示「已解除上限」提示；超出免费门槛的步数仍由费用估算按实际步数计费并在生成前确认。
- **同步范围**：开关覆盖工坊编辑器与实验室全部四种模式（文生图/图生图/局部重绘/扩图）共用的参数面板，并补偏好持久化回归测试。

### 文档:README 增加适用范围声明：仅面向 NovelAI Opus 档位会员
- **范围澄清**：在 README「项目定位」处增加醒目说明——本项目的费用估算、免费额度判定、Opus 限额与免费门槛逻辑均按 Opus 档位设计，其他订阅档位（Paper / Tablet / Scroll）与第三方中转服务未做适配，非 Opus 档位下"免费"标识可能与实际扣费不符。
- 同时补上缺失的 2026-08-28 日期节标题，恢复按日期分节的格式。

## 2026-08-28

### 修复:手机端实验室导航死角：顶栏新增返回箭头
- **退出实验室有路了**：手机端进入生图实验室后，底部导航栏按工作台设计隐藏，而实验室顶栏此前只有四个模式切换、没有任何出口，用户会被困在实验室页面。现于模式导航左侧新增返回箭头（仅无侧边栏的窄屏显示，桌面/平板由侧边栏承担退出、不造重复入口），返回进入实验室之前的内容页（从历史进则回历史、从图库进则回图库），兜底回风格串列表；keep-alive 机制保证往返不丢任何状态。

### 修复:启动本地服务时快时慢/卡住数十秒到数分钟：短路 wrangler 启动期 npm 更新检查
- **根因**：wrangler 在打印启动横幅前会做一次 npm 更新检查，该检查（update-check 库）走原生 https、无视全部代理环境变量；registry 直连被网络黑洞时实测阻塞 36 秒起步，且失败不写缓存、下次启动继续阻塞。表现为启动窗口长时间停在「核心页面服务正在启动」，时快时慢全看更新检查缓存是否新鲜——缓存新鲜时秒开，过期后每次启动都被黑洞。
- **修复**：启动 wrangler 子进程时经 `npm_config_registry` 环境变量把 registry 一并短路到本地快速失败端口，更新检查毫秒级失败被 wrangler 静默跳过；原快速失败出站代理继续保留作为 TUN 网关黑洞的兜底。对照实验：修复前 Proxy 提示到横幅静默 36 秒，修复后横幅与 Ready 均在 1 秒内。

### 体验:手机端编辑器低频操作收进更多菜单，顶栏回归单行
- **工具行收进 ⋯ 底部动作面板**：手机端不再平铺一排无文字的图标按钮，模式导航行右侧新增 ⋯ 更多按钮，点开底部动作面板（复用全站 mobile-sheet，支持下滑或系统返回键关闭）——导入图片或 JSON、引用预设、图片反推 Tag、Tag 辅助（实时显示开/关状态）、重置实验室（红色危险项）、保存到库/保存修改（绿色主操作、不可保存时置灰）全部以带文字的大触控行呈现，可发现性与触控手感同步提升，并省出一整行垂直空间。
- **平板与桌面布局不变**：768px 以上仍为原有右对齐工具行，1024px 以上双栏顶栏完全不变；新增 ChainEditorHeader 断点语义与面板交互回归测试。

### 修复:手机端编辑器工具栏真正居中
- **auto 边距破坏居中修复**：上一版仅把工具栏改为 `justify-center`，但容器遗留的 `ml-auto` 在 grid 布局中会把按钮行收缩至内容宽度并钉在右侧，居中实际不生效；现将 auto 边距限定为桌面端专属（`lg:ml-auto`），手机端按钮行以整行宽度真正居中，桌面端保持右对齐不变。新增 ChainEditorHeader 回归测试锁死该断点语义，防止无前缀 `ml-auto` 再次混入移动端。

### 修复:手机端实验室编辑模式隐藏顶部大图预览并居中编辑器工具栏
- **编辑三模式移动端隐藏顶部预览**：图生图 / 局部重绘 / 扩图在手机端不再于顶部直接平铺大图预览区（含删除/清除/下载/历史切换浮层），改为与文生图页面完全一致的形态——仅保留底部浮动圆圈，点击即可查看当前预览图；圆圈在编辑模式下实时显示当前底图或最新生成结果，点开灯箱仍可左右切换历史。桌面端左右分栏布局完全不变。
- **编辑器工具栏移动端居中**：实验室与风格串/角色串编辑器顶部的导入、引用预设、图片反推、Tag 辅助、重置等按钮行在手机端由右对齐改为居中显示，消除窄屏下靠右堆挤的失调感；桌面端保持右对齐不变。

### 修复:修复手机端多处前端显示问题（10 轮全站检查）
- **移动端实验室生成错误可见**：文生图模式下移动端预览容器隐藏导致生成失败的错误提示不可见，现于移动端底部浮动生成按钮上方常驻错误提示条，生成失败原因一目了然。
- **Pixiv 移动端顶栏独立导航行**：窄屏下搜索框、五个导航按钮与操作按钮挤在一行互相挤压；现移动端将推荐/关注动态/我的收藏/排行榜/足迹独立为第二行横向滚动导航，搜索与操作按钮保持首行不换行。
- **画师配置弹窗参数窄屏降级单列**：实装测试配置的 Seed / Steps·Scale / 队列间隔三列参数在 375px 窄屏每列仅约百像素无法操作；现窄屏降级为单列堆叠，`sm` 起恢复三列。
- **画师库复制历史抽屉移动端全屏化**：固定 320px 右侧抽屉在窄屏占屏过宽且无安全区处理；现移动端全宽展示（限宽 320px）、顶部补齐刘海安全区。
- **弹窗高度统一改用 `dvh`**：批量导入、画师配置、复制预设、任务日志等弹窗由 `vh` 改为 `dvh`，修复移动端地址栏收起/展开时弹窗底部溢出被裁的问题。
### 功能:局域网访问密码可在设置中实时修改
- **设置页新增改密入口**：「数据与维护」分类新增局域网访问密码修改，电脑与已授权的手机均可修改；保存后立即生效，无需重启服务。
- **解锁校验与会话签发迁移到媒体网关**：手机访问改为由网关实时读取 `local-data/lan-access.json` 校验密码并签发 30 天会话 Cookie，签名格式与 Worker 端完全一致；保留连错 5 次锁定一分钟的防爆破策略，Worker 端启动快照仅作本机直连兜底。

### 修复:手机访问地址不再展示虚拟网卡
- **局域网地址列表过滤**：启动日志只展示真实可访问的局域网地址，跳过 WSL/Hyper-V、TUN（198.18.0.0/15 保留测试段）等虚拟网卡，手机访问地址一目了然。
### 修复:修复编辑页左上角返回按钮偶发点击无响应问题
- **返回导航调用修复**：修复 `handleReturnFromEditor` 返回跳转逻辑，补正函数调用并移除视图跳转外层不必要的 `startTransition` 包装，确保点击左上角 `<ArrowLeft>` 返回按钮时立即、同步触发视图状态切换，杜绝点击后无任何响应的问题。
### 修复:解决从编辑页返回列表时丢失浏览位置直接回到顶部的问题
- **容器隐藏滚动防护**：在 `useKeepAliveScrollRestore` 中增加视图激活与非零高度防护（`activeRef.current && root.clientHeight > 0`），彻底阻止列表容器在切换隐藏（`display: none`）瞬间由浏览器派发的 `scrollTop = 0` 虚假滚动事件冲掉内存中的真实位置缓存。
- **返回定位锚点联通**：在 `handleReturnFromEditor` 返回跳转时重新携带 `selectedId` 作为 `returnTargetId`，使列表按需提前补齐渲染批次（`visibleCount`），确保长列表在拥有完整可滚动高度的基础上精确恢复至离开前的像素级视口位置。
### 修复:解决点击 st-chatu8 同步画师串触发 TypeError 白屏崩溃问题并修复存量数据
- **根因修复与空值防御**：修复 st-chatu8 同步的风格串因缺失完整生成参数（`params: {}`）导致进入编辑器（`ChainEditor`）时 `params.width` 为 `undefined`，进而触发 `ChainEditorParams` 自定义尺寸区域 `params.width.toLocaleString()` 抛出运行时 TypeError 白屏崩溃的缺陷。
- **全链路参数兜底**：在桥接生成（`st-chatu8-bridge.mjs`）、Worker 接口（`settingsRoutes.ts`）、工作区状态（`labWorkspace.ts`）与参数编辑面板（`ChainEditorParams.tsx`）四层全量注入 `normalizeParams` 与安全链式访问，确保任意缺失或异常参数均能自愈回退到标准默认生成参数。
- **存量数据无感修复**：自动识别并修复本地数据库中存量的 30 个 st-chatu8 同步风格串，补齐标准生成参数，点击即可秒开进入工坊。
### 体验:Pixiv 顶栏改为搜索栏拉宽铺满+独立导航按钮，对齐 Danbooru 风格
- **顶栏单行化重构**：移除 Pixiv 顶栏的胶囊式 `SegmentedControl` 选项卡，改为与 Danbooru 一致的布局——搜索栏 `flex-1` 自适应拉宽铺满，推荐 / 关注动态 / 我的收藏 / 排行榜 / 足迹 制成独立等高按钮依次排在搜索框之后，激活项实心高亮，消除中间空白与挤压感。
- **交互一致**：保持原有切换语义（含足迹视图切换、画师上下文"返回推荐"），窄屏下按钮组支持横向滚动。
### 修复:Tag 补全弹窗改为渲染在页面最顶层并彻底消除被遮挡问题
- **弹窗层级全局最高**：将实验室/编辑器所有 Tag 补全下拉框从 `absolute` 容器内定位改为 **React Portal 渲染到 `document.body`**，使用 `position: fixed` + `z-index: 9999`（页面最高层级），彻底解决弹窗被后续模块卡片、`<details>` 容器 `overflow-hidden` 或其他元素遮挡导致"输入了却看不到候选"的问题。
- **随输入框实时对齐**：弹窗位置基于输入框 `getBoundingClientRect()` 动态计算（空间不足自动向上弹出），并在窗口 resize、滚动（捕获阶段）时跟随输入框实时刷新，任何滚动容器内都不会被裁切。
- **中文角色联想提速**：聚焦输入框时即预热角色搜索倒排记录文件（数 MB），首次中文角色名输入即可秒出补全，无需等待。
### 体验:实验室输入框支持中文搜角色、角色库自定义卡片增加直接编辑入口并提速封面枚举

- **实验室中文搜角色**：重构 `searchTagDictionary` 检索流，中文输入时自动并发调取角色库全文倒排索引（`searchCharacterDictionary`），支持拼音、中文别名与跨词联想，与角色库完全对齐，并优先将角色 Tag 候选置顶。
- **自定义角色卡片交互增强**：在角色库的自定义角色卡片右上角新增快捷 `[✏️ 编辑]` 悬浮按钮，并在底部直观展示完整 Prompt/描述文本与文字编辑入口，点击直接进入还原编辑器进行调整。
- **角色封面并发加载加速**：优化 `DanbooruCover` 并发枚举管道，将并发数适度提升至 5、启动间隔压缩至 60ms，且角色单卡首屏采样数优化至 60 张，显著减少初次搜索生僻角色时的转圈等待时间。

### 修复:解决 Danbooru 全局高分榜与收藏榜数据库查询 500 超时卡死问题

- **无缝路由至官方精选接口**：定位并解决 Danbooru 官方 API 在「无关键词全站大库」下直接执行 `order:score` / `order:favcount` 必然触发官方数据库超时（500 Timeout）导致转圈卡死的缺陷。现在无特定检索词时自动路由至 Danbooru 官方专用的 `/explore/posts/popular` 精选榜单接口（高分榜对应月榜、收藏榜对应周榜），有关键词时（如 `frieren order:score`）继续走带索引的高速检索，彻底消除超时卡死并实现秒级极速响应。

### 体验:收敛 Danbooru 图库顶栏为单行下拉筛选并修复多维过滤生效

- **顶栏单行化**：移除 Danbooru 图库冗余的第二行平铺胶囊筛选栏，将「排序」、「评级」、「画幅比例」与「仅单人」全部收敛至顶栏首行标准的 `<select>` 下拉框与切换按钮，与角色库、画师库等其他图库款式完全对齐，消除纵向空间浪费。
- **多维筛选即时生效**：修复 Danbooru 匿名检索 2-Tag 上限导致的多维筛选被上游拒绝或失效问题；在请求参数安全并入的同时辅以客户端本地即时过滤，确保在任何搜索词下切换评级（G/S/Q/E）、画幅比例（竖屏/横屏/方图）与单人过滤都能 100% 即时生效并正确联动。
- **移动端体验同步**：移动端补充抽屉式筛选弹窗，全设备手感一致。

### 修复:编辑页返回列表恢复浏览位置并在离开时自动补全风格串封面

- **返回保持浏览位置**：从风格串/角色串编辑页点击左上角返回箭头时，不再把刚编辑的卡片滚回视口中央，而是恢复离开列表前的浏览位置（与侧边栏切换的滚动保持行为一致）。
- **离开自动补封面**：风格串未设置封面时，以任意方式离开编辑界面（点返回箭头、侧边栏切换页面、点风格串图标回列表等）都会自动取当前可见图片——优先当前切换到的历史图、否则最新生成图——上传设为封面；已有封面或非风格串时不动，失败静默不阻塞返回；通过保存按钮已设过封面时不重复上传。

### 修复:keep-alive 图库页面切换后恢复滚动位置

- **滚动位置保持**：修复侧边栏切换页面时图库列表总是跳回顶部的痛点。新增通用 `useKeepAliveScrollRestore` hook，在滚动时持续记录位置，视图重新激活（从 `display:none` 恢复）时自动恢复；内容异步加载导致高度不足时按帧追赶恢复，1.2s 内达标即停。已接入风格串、角色库、画师库、AITag、Danbooru、Pixiv、灵感库、历史记录全部 8 个 keep-alive 图库视图。
- **详情返回优先级**：从编辑详情返回列表（`returnTargetId` 场景）时跳过缓存恢复，交由原有 `useRestoreListAnchor` 精确定位目标卡片，两者互不冲突。

### 体验:画师库多选栏对齐居中悬浮胶囊并为角色库增配多角色槽位分配导入

- **槽位拖拽排序**：角色库多角色槽位面板支持直接按住拖拽（抓手光标）调整槽位顺序，同时保留原有 ↑ / ↓ 按钮微调，长列表下调整更顺手。

- **画师库对齐角色库**：将 `ArtistLibrary` 底部通栏大底栏彻底重构为与 `CharacterLibrary` 一致的居中悬浮毛玻璃胶囊 Dock（`已选 N 位画师` ｜ `清空` ｜ `复制全部` ｜ `导入实验室`），点击已选数量可展开浮层微调权重，并补齐一键将画师组合送往实验室能力。
- **角色库多角色槽位管理**：角色库底部悬浮 Dock 新增槽位可视化面板，支持一键查看、移除与使用 ↑/↓ 上下调整角色的槽位序号 (1..N)。
- **智能多角色槽位导入**：点击「导入实验室」时，自动以结构化 `characters: CharacterParams[]` 装载各角色的独立提示词与负面词，并根据选中数量自动均匀计算默认站位坐标（单人居中 0.5，多人 0.2~0.8 水平分布），直接点亮实验室多角色槽位。

### 优化:规范角色库来源筛选下拉文案并为卡片增加来源标记

- **文案去歧义**：将角色库顶栏来源筛选下拉框与移动端筛选弹窗中的「我的自定义 (N)」规范为纯粹维度分类「自定义角色」，消除动态总数拼接造成的选项多选误解。
- **卡片来源打标**：为角色卡片标题右侧增加「Tag 词库」与「自定义」来源徽标，在全部角色混合浏览时清晰传达数据来源。

## 2026-08-27
### 体验:画师库与角色库顶栏搜索框自适应铺满并右移控制组

- **布局优化**：将角色库（`CharacterLibrary`）与画师库（`ArtistLibrary`）顶栏搜索框调整为 `flex-1` 自适应拉长铺满，搜索框右侧的筛选/排序/抽卡与操作按钮成组平滑右移贴合，消除中间大面积空白。

### 重构:清理角色库与画师库所有显示设置关联死代码

- **死代码清理**：彻底清理画师库中不再可达的 `layoutMode`（展开列表视图与槽位排队逻辑，缩减逾 130 行）、`listImgWidth`、`gridCols` 自定义状态，以及角色库中的 `gridColumns` 自定义状态与冗余图标引用，统一使用标准自适应网格。

### 体验:移除角色库与画师库顶栏冗余的显示设置按钮

- **界面精简**：移除角色库（`CharacterLibrary`）与画师库（`ArtistLibrary`）顶栏爱心左侧的【显示设置】按钮及其气泡弹窗，进一步精简顶栏控件密度，保持视觉专注。

### 修复:修复画师库控制栏与列表布局 JSX 闭合问题

- **语法修复**：修复 `ArtistLibrary` 顶栏抽卡设置弹窗、显示设置与展开列表视图中的 JSX 标签闭合错误，确保 `build:local` 编译顺利通过。

### 体验:角色库范围筛选改为标准下拉框并去重收藏入口

- **控件统一与去重**：将角色库（`CharacterLibrary`）顶栏的范围选项卡替换为与旁边排序下拉框完全一致的 `<select>` 下拉筛选框（全部角色 / 角色 Tag / 我的自定义），同时移除下拉框中冗余的“收藏”项，由右侧独立的 `[♡ 只看收藏]` 图标按钮统一承载。

### 体验:角色库与画师库顶栏款式镜像对齐与收藏能力统一

- **顶栏空间与肌肉记忆统一**：全面统一角色库（`CharacterLibrary`）与画师库（`ArtistLibrary`）的桌面端顶栏三段式布局：左侧标准搜索框锚点（`md:w-64 lg:w-72`）、中间「排序下拉 + 🎲 随机抽卡 ▾」组合操作、右侧「🎛️ 显示设置 + 💖 只看收藏 + 🔄 刷新 + 🖼️+ 反推」。
- **角色库独立收藏按钮**：为角色库顶栏右侧新增全站统一的 `[♡ 只看收藏]` 高亮图标按钮，与画师库及其他图库保持完全一致的收藏过滤手感。
- **画师库冗余管理入口精简**：移除画师库顶栏中历史遗留的批量基准跑图队列药丸与复杂二级菜单，使画师库回归纯粹安静的“选 Tag 送实验室”核心心流。

### 修复:恢复 AITag 画廊顶栏桌面端「筛选」按钮

- **顶栏控件完整性**：恢复 `AitagGallery` 顶栏中意外丢失的桌面端「筛选」触发按钮（`ToolbarButton`），确保可以正常唤起包含 Prompt 检索、缓存状态、最新/月榜排序及模型版本选择的筛选浮层。

### 修复:画师库配置弹窗（ArtistLibraryConfig）内部 DOM 容器闭合缺陷

- **构建与语法修复**：修复 `ArtistLibraryConfig` 弹窗在添加遮罩点击监听时意外遗漏内部卡片 `<div>` 开标签导致的 esbuild JSX 构建语法解析异常，保障 Vite 生产构建与本地服务启动畅通。

### 优化:角色卡片外壳圆角与多选聚焦环对齐设计系统（审查阶段三）

- **卡片外壳对齐**：将 `CharacterLibrary` 中的角色卡片外壳圆角从 `rounded-lg` 统一升级为 `DesignSystem` 规范的 `rounded-2xl`，背景与边框统一至 `dark:bg-gray-900` 与 `dark:border-gray-800`，选中光晕统一为 `border-indigo-500 ring-2 ring-indigo-500/20`，消除跨画廊卡片质感差异。

### 优化:生图实验室参数调优表单视觉与聚焦光晕系统化收敛（审查阶段二）

- **参数表单规范化**：全面升级生图实验室（`ChainEditorParams`）的生成模型下拉框、图片尺寸预设与自定义宽高输入框、采样器、步数与随机种子输入框为设计系统标准的 `rounded-xl` 圆角，并补充统一的 `focus:border-indigo-500 focus:ring-2 focus:ring-indigo-500/20` 键盘与鼠标聚焦光晕，彻底消除旧版 4px 细碎生硬输入框风格。

### 优化:全站弹窗（Modal）ESC 键盘监听与遮罩点击关闭交互闭环（审查阶段一）

- **弹窗关闭交互契约**：为预设引用弹窗（`ChainEditorPresetModal`）、保存类型分支弹窗（`ChainEditorForkModal`）、文件夹批量导入弹窗（`FolderBatchImportModal`）、图片反推面板（`ImageTaggerPanel`）、画师库配置弹窗（`ArtistLibraryConfig`）与词库更新弹窗（`TagDictionaryUpdater`）补齐全量 `Escape` 按键监听与背景遮罩点击自动收起，实现与原生桌面一致的弹窗交互闭环。

### 优化:全站画廊与资料库顶栏左上角搜索框统一锚定（Top-Left Search Anchor）

- **全站左上角第一定位统一**：将角色库与 Pixiv 画廊中原本排在最左侧的分段选项卡（`SegmentedControl`）移至搜索框右侧，使全站所有 7 个画廊页面（风格串、角色库、画师库、Pixiv、Danbooru、AITag、灵感库）的最左上角（x=0）统一绝对锚定为标准搜索框，彻底建立跨页面一致的搜索肌肉记忆。

### 重构:全局画廊与资料库顶栏空间网格与搜索交互深度归一化

- **搜索与空间流收敛**：移除 Pixiv、AITag、Danbooru 顶栏中突兀的多余主色「搜索」按钮（统一支持回车直接检索），统一搜索框自适应拉伸契约（`flex-1 min-w-[12rem]`），彻底消除风格串库等页面的巨大空白断层；
- **全站工具与主动作对齐**：全量对齐所有画廊「左侧定位/范围 → 中部业务筛选/抽卡 → 右侧固定工具与主动作」的标准三段式物理空间契约。

### 修复:风格与角色串列表标签筛选弹层居中对齐锚定

- **气泡居中锚定**：将 `ChainList` 顶栏「标签筛选」展开面板从单侧贴右（`right-0`）纠偏为相对于触发按钮水平居中锚定（`left-1/2 -translate-x-1/2`），消除大尺寸弹层向左侧严重偏斜延伸的不协调感。

### 优化:自检轮次三 —— 暗色模式对比度与分段控制器边框契约收敛

- **边框对比度强化**：为 `DesignSystem` 的分段控制器 `SegmentedControl`、过滤胶囊 `FilterPill` 与实验室模式切换栏 `GenerationModeNav` 统一补充高精度浅深色双态边框（`border-gray-200/60 dark:border-gray-800/80`），彻底消除暗色主题与高对比度场景下控件与背景混色融合的视觉缺陷。

### 优化:自检轮次二 —— 统一各模块顶栏下拉气泡与浮动菜单的点击外部自动关闭（Click-Outside Dismiss）

- **弹层点击外部关闭契约**：为角色库、画师库、串列表、灵感库与 AITag 画廊的抽卡设置、显示设置、更多工具与高级标签筛选 popover 全面补充全局透明点击遮罩（`fixed inset-0`），实现类似原生桌面应用的即点即合交互体验。

### 优化:自检轮次一 —— 移动端顶栏纵向对齐与灵感侧栏图标选择器规范化

- **移动端顶栏纵向对齐**：优化 `AitagGallery` 移动端搜索顶栏为 `w-full min-w-0 items-center`，移除状态圆点多余的硬编码偏移，保证在各种视口宽度下搜索条、状态灯与刷新按钮垂直居中；
- **灵感分类图标尺寸穿透**：将 `InspirationShared` 中的 `CollectionButton` SVG 选择器同步升级为 `[&_svg]` 后代选择器，确保侧栏分类图标在暗色与选中态下稳定渲染 16px。

### 修复:全面统一按钮内部图标尺寸（严格限定 16px / h-4 w-4）

- **CSS 后代选择器穿透**：将 `DesignSystem.tsx` 中 `IconButton`、`ToolbarButton`、`ToolbarLink` 的直接子选择器 `[&>svg]` 升级为全局后代选择器 `[&_svg]:h-4 [&_svg]:w-4`，彻底解决图标被 `span` 或角标包裹时失效回退到 Lucide 默认 24px 导致图标大小不一的问题；
- **清理散装硬编码尺寸**：消除各顶栏组件中残留的 `h-5 w-5` / `h-[18px]` 尺寸硬编码，全站按钮内图标严格统一为 16px 标准尺寸。

### 修复:恢复实验室生图模式切换栏与左侧调参面板的桌面端对齐契约

- **空间对齐纠偏**：修正 `ChainEditorHeader` 的桌面端双列布局（`lg:grid-cols-2`），将「文生图/图生图/局部重绘/扩图」模式切换器精准锁定并对齐在左侧 50% 调参编辑面板正上方，右侧操作工具栏恢复独立右对齐。

### 修复:wrangler 启动偶发卡顿 —— 零输出看门狗 + 慢启动不误杀 + 注入 ALL_PROXY

- 现象:启动时窗口长时间停在「核心页面服务正在启动」且 wrangler 无输出(代理出口黑洞时最长 130 秒,或慢启动拖到 60~80 秒才就绪);
- 看门狗:wranger 启动 25 秒仍然零输出(黑洞特征)时自动杀掉并重启一次,不再无限静默;已修复两个自伤 bug —— 任意一行输出(含 Proxy 提示)即视为进程活着,避免误杀慢启动;被淘汰实例的 exit 事件静默,避免误退出启动器;
- 代理全覆盖:向 wrangler 同时注入 HTTPS_PROXY/HTTP_PROXY/ALL_PROXY 指向本地空端口,统一短路出站检查;
- 实测:慢启动(86 秒)正常等到就绪且未误杀;页面与状态接口 200。

### 架构与交互:多选操作栏悬浮胶囊化与角色参考库交互收敛

- **底部悬浮多选操作栏（Floating Action Dock）**：角色库多选批量操作栏升级为居中悬浮玻璃拟态胶囊栏（`shadow-2xl`、`rounded-2xl`、平滑升降动画与 `ToolbarButton` 统一动作），不再产生全屏贴底生硬挤压；
- **生图历史多选工具栏精细化**：生成历史桌面端多选操作栏升级为统一圆角、层级分割线与高对比操作按钮；
- **角色参考库对话框交互收敛**：角色参考管理弹层中的资料库/已归档范围筛选切换至标准 `SegmentedControl`，统一动作按钮触控与对齐规范。

### 架构与设计系统:收敛全站二级选项卡与过滤胶囊（SegmentedControl & FilterPill）

- **抽象核心设计系统组件**：在 `DesignSystem.tsx` 中正式沉淀分段选择器 `SegmentedControl` 与过滤胶囊 `FilterPill`，统一触控尺寸、圆角契约（`rounded-xl`/`rounded-lg`）、暗色对比度与 Badge 徽标规范；
- **全站画廊二级导航收敛**：
  - `CharacterLibrary`：角色范围筛选（全部 / 角色 Tag / 我的自定义 / 收藏）接入 `SegmentedControl`；
  - `PixivGallery`：主 Feed 选项卡接入 `SegmentedControl`，排行榜子模式与搜索排序/收藏门槛全量切换至 `FilterPill`；
  - `DanbooruGallery`：排序、评级、画幅比例与单人筛选全量收敛至 `FilterPill`。

### 架构与体验:角色库抽卡升级为 Split-Button 与全站探索状态横幅标准化

- **抽卡控件标准统一**：角色库抽卡升级为与画师库一致的 Split Button（`[Dice5 随机抽卡/再抽一批] + [ChevronDown 抽卡范围与数量设置]`），消除顶栏动态插塞返回按钮导致的右侧布局跳动；
- **纯视图设置与业务规则解耦**：角色库原杂烩设置拆分为跟随 Split Button 的「抽卡设置」与跟随 `<SlidersHorizontal>` 的纯「显示设置（每行列数调节）」；
- **全站画廊 Active State Banner**：新增并引入 `GalleryActiveStateBanner`，在角色库与画师库处于抽卡探索态时，在网格顶部渲染吸顶指示横幅（支持抽取数量反馈、再抽一批与返回目录）。

### 体验优化:生成历史顶栏管理按钮与清理图标语义纠偏

- **消除过度警示**：将生成历史顶栏桌面端红色边框的高亮管理按钮改为标准中性色 `ToolbarButton` 下拉菜单，保持视觉平静；
- **图标语义准确化**：移动端历史管理与按数量清理子项废除 `<SlidersHorizontal>`（视图调节图标），改用 `<ListChecks>` 与 `<Layers>` 精准表达数据批量管理与层级保留。

### 架构与体验:生图实验室顶层命令栏对齐设计系统契约（WorkspaceToolbar）

- **统一顶栏网格与尺寸**：生图实验室顶栏重构接入 `WorkspaceToolbar`，废除自定义双列 grid，左右分区清晰规范；
- **基础按钮规范归一**：将全部散装硬编码按钮（导入、预设、反推、Tag辅助、重置、Fork、保存）收敛至 DesignSystem 标准 `IconButton` 与 `ToolbarButton`，严格统一至 `h-10 rounded-xl` 规格；
- **模式导航返回规范**：对齐 `ChainEditorModeHeader` 返回与编辑按钮至标准 `IconButton` 尺寸。

### 架构与体验:全页面顶栏空间网格与操作区布局统一契约（Top Bar Contract）

- **统一 3+1 空间结构**：在风格串、角色库、画师库、AITag、Danbooru、Pixiv、灵感库、生成历史 8 个工作区全量对齐 Zone A (搜索/视图) / Zone B (过滤/状态) / Zone C (系统动作与主操作) / Zone D (探索型次级多维筛选)；
- **操作区固定肌肉记忆**：全页面 Zone C 统一固定顺序为 `[页面设置/更多]` → `[刷新]` → `[WD 1.4 反推]` → `[Primary CTA / 新建]`，无论切换到哪个页面，刷新与反推位置恒定，主操作恒居最右；
- **补齐缺失功能**：为角色库补齐桌面端快捷刷新，为 Pixiv 图库补齐顶栏 WD 1.4 反推入口；
- **优化搜索与过滤分区**：AITag 搜索按钮与输入框紧凑成组，生成历史总数 Badge 前置到过滤区，提升视觉节奏感。

### 优化:全局控件与交互设计语言一致性对齐与清理

- **下拉选择框标准规范**：统一画师库与角色库的 `<select>` 下拉筛选框至 `h-10 rounded-xl`，统一边框、字体与聚焦环样式，完全适配动态圆角调节；
- **收藏高亮色收敛**：统一风格串与画师库的收藏筛选激活态为 `tone="favorite"`（玫瑰红 / Rose 系列），消除黄/紫/红多种混用；
- **浮层弹窗圆角规范**：统一角色库设置与生成历史管理菜单浮层为 `rounded-2xl` 现代卡片风格；
- **清理冗余资源**：清理 DanbooruGallery 中残留的未引用图标。

### 修复:启动器回归 —— 恢复网关初始化计时变量(gatewayStartedAt is not defined)

- 上一提交(网关初始化提示与超时保护)替换时误删 `const gatewayStartedAt = Date.now();`,导致启动在「图片网关初始化中…」后立即报错退出;
- 已恢复声明,实测启动 2.2 秒全就绪,页面与状态接口 200。

### 修复:启动偶发卡住无反馈 —— 图片网关初始化可见化与 90 秒超时保护

- 现象:偶发启动时窗口停留在「核心页面服务已就绪」后长时间无新输出(wrangler/workerd 启动慢至 40 秒、网关初始化数分钟),看起来像卡死;
- 排查:对网关五步初始化(缓存/Agent/Pixiv/桥接/恢复)逐点计时,实测每步 <100ms、总 0.3s,卡顿为偶发环境因素(系统繁忙/磁盘/代理出口),无法稳定复现;
- 加固:启动器在网关初始化阶段打印「图片网关初始化中…」,并加 90 秒总超时 —— 再次遇到偶发卡顿会明确报错退出,不再无限静默;核心服务等待上限(180 秒)与分阶段提示保持不变。

### 修复:Pixiv「我的收藏」列表 Invalid request.—— user_id 必须为账号真实数字 ID

- 根因:app-api `/v1/user/bookmarks/illust` 拒绝 `user_id=me` 与缺省参数,必须传当前账号的数字 ID;此前进代码写死 `'me'` 且未持久化用户 ID,进入「我的收藏」即返回 400「Invalid request.」(收藏/取消按钮不受影响,实测正常);
- 修复:OAuth 刷新响应原带 `user.id`,现在提取并加密持久化;收藏 Feed 自动携带真实数字 ID(旧令牌首次使用自动刷新一次补齐);
- 实测:网关返回 30 条收藏,翻页、收藏/取消回归正常。

### 修复:Pixiv 收藏失败诊断与错误文案(Invalid request. 场景)

- 网关直连实测收藏/取消均正常,未复现错误;为定位真实失败场景,收藏与取消失败时增加诊断日志(记录 illustId、上游状态码、错误码与原文,只进后台日志不携带令牌);
- 前端把 Pixiv 上游 `Invalid request.` 等原文映射为可理解提示(「作品可能已失效或状态已变化,请刷新后重试」);
- 提升 TypeScript lib 至 ES2024(当前运行环境均支持,顺带满足新代码 API 类型)。

### 移除:Danbooru 抽卡漫游功能(上游 order:random 不稳定,产品决定移除)

- 移除 Danbooru 图库「抽卡漫游」按钮与「随机」排序项,以及服务层 `order:random` 查询组装(此前该查询在 Danbooru 端频繁数据库超时,重试与降级只能缓解不能根治);
- worker 保留 `order:random` 手动输入兼容(page=1 上游限制)与通用超时重试(上游 500 time-out 与网络超时自动重试,对普通查询同样生效);
- 画师库的画师「抽卡」(随机画师组合)功能不受影响,继续保留。

### 修复：官方常量同步临时失效（琥珀色圆点）—— 同步改走网关代理分流,不再受 TUN 出口抖动黑洞

- 现象：侧栏 Opus 限额行出现琥珀色圆点（同步 health=false,`imagesPerPercent` 等部分提取项 miss);
- 排查结论：不是代码改动或提取器失效 —— 联网自检在干净网络下全绿（8/8 项命中,含 `imagesPerPercent: 17.3`）;根因是官方常量同步此前用 Node 直连抓取 43 个官方 chunk,在你的 TUN 代理出口抖动时个别 chunk 超时,产生"部分提取"(partial)并被健康标记;
- 修复：`initNaiRuntimeSync` 改走网关代理分流（`remoteFetch`,与订阅接口同一路由、经本机真实代理端口）;配合既有失败 5 分钟自动重试,同步恢复稳定;实机验证 health 恢复 `ok:true`;
- 验证：`npm run test:gateway` 135/135、`npm run test:live-sync` 全部提取命中。

### 新增：设置「数据与维护」新增本地数据备份与历史备份浏览功能

- **一键异步完整备份**：在设置「数据与维护」中新增数据备份控制台，一键将包含 D1 数据库、R2 原图、历史记录、配置与密钥的 `local-data` 目录完整备份至指定目录（默认 `D:\NaiPromptManager-Backups`）；
- **实时进度反馈与单飞保护**：备份过程提供包含已复制文件数、总字节数、百分比与当前处理文件的动态进度条，且具备单飞互斥锁，防止并发重复备份；
- **历史备份扫描与唤起资源管理器**：自动扫描备份目标目录中的历史备份（无缝兼容识别此前手动备份的文件夹与 ZIP 归档），显示备份时间、容量与文件数，并支持在操作系统文件资源管理器中直接定位打开。

### 修复:启动窗口日志静音 —— 过滤高频请求日志,弃用会拖慢启动的 --log-level

- 启动窗口不再被 `[wrangler:info] GET /api/...` 请求日志刷屏(历史缩略图预热、浏览器轮询每行都打印),保留 wrangler 横幅、`Ready on` 与服务错误输出;
- 实测证明 `wrangler pages dev --log-level warn` 会把启动从 2.2 秒拖慢到 56 秒(25 倍回归),该方案已弃用;改为对子进程 stdout 做逐行过滤,启动速度不受影响;
- 需要完整日志排查时,启动器支持环境变量 `NAI_WRANGLER_LOG=all` 恢复全部输出。

### 维护：标注数据边界与 local-data 内部构成（防误删红线）

- `AGENTS.md` 新增「数据边界（保护区，禁止触碰）」强制节：`local-data/` 全量禁止删除/改名/移动，`wrangler.toml` 的 `database_id` 明确为本地 D1 存储键（不是云端残留），并规定判断“上游遗留”时必须区分数据层与代码层；
- `README.md` 存储边界章节补充 `local-data` 内部构成清单（每项可再生成性）与 database_id 警告；
- `.gitignore` 为 local-data（不可再生）与 local-cache（可再生成）补充性质注释。

### 修复：恢复 wrangler.toml 的 D1 database_id，修正“启动后数据消失”事故

- 事故原因：前一次清理把 `wrangler.toml` 的 `[[d1_databases]] database_id` 当作云端残留删除，但该 ID 实际是本地 `--persist-to` 存储键的一部分（f(database_name + database_id) 决定加载哪个 SQLite 文件）——ID 移除后本地模式改加载全新的空库，界面表现为风格串、历史、灵感全部消失；
- 真实数据从未受损：`local-data/v3/d1/.../d3e11673….sqlite`（270MB）原封未动，恢复 database_id 后立即重新加载，实测 chains 243 / artists 2 / inspirations 36 / history 8,924 与清理前完全一致；
- 已删除误生成的空库文件，并在 wrangler.toml 标注该 ID 不可移除的原因；启动闪回归正常。

### 清理：移除上游多用户与云端部署遗留（quota 列、sessions 表、角色策略、管理路由死代码）

- INIT_SQL 移除无任何读写的 `sessions` 表,以及 `users` 表的 `storage_usage / last_login / max_storage` 配额列;旧库已有列不受影响(CREATE IF NOT EXISTS + 幂等 ALTER),新库不再建死表;
- 删除全部不可达的 `/api/admin/stats`、`/api/admin/logs`、`/api/admin/clear-logs`、`/api/admin/guest-setting` 路由及 access_logs 建表辅助函数(这些路径已在个人模式下被 410 拦截,永不触发);
- 移除 `ROLE_POLICY` 角色策略常量与个人模式下恒真的权限检查(画师库管理、上传存储配额),上传路径不再写无意义的 `storage_usage` 记账;
- `/api/auth/me` 与前端 `User` 类型同步收窄,仅保留 `id / username / role`；`schema.sql` 参考快照同步更新;
- 补充一条关键约定:本地 `wrangler pages dev` 直接使用 `dist/_worker.js`(不编译 worker 源码),改动 worker 代码后必须重新执行 `npm run build:worker` 才能生效(已固化到本次验证流程)。

### 修复：本地启动偶发卡死 1~2 分钟 —— wrangler 启动期外连检查被失效的系统代理黑洞

- 根因：系统代理指向 Clash/mihomo TUN 网关（198.18.0.0/15 保留网段）的机器上，代理出口异常时 wrangler 启动时的外连版本检查会被黑洞挂起约两分钟，启动窗口长时间停在“核心页面服务正在启动”且无任何输出，超时后一切正常；
- 启动器现在给 wrangler 进程注入指向本地空端口的快速失败代理，强制启动期外连检查毫秒级跳过：页面服务实测 1.9 秒完整就绪，不再受代理软件健康状态影响（本地 pages dev 不需要出站网络）；
- 图片网关出站代理同步优化：系统代理指向 TUN 网关时自动改用本机真实代理端口（Clash Verge / mihomo 等常见端口），AITag / Pixiv 等远程图片请求不再被黑洞拖慢。

### 性能：本地启动提速 —— 历史缩略图预热让行、静态资源网关直出、清理 Wrangler 临时目录

- 历史缩略图后台预热（启动时逐页扫描数千条历史记录）不再在服务就绪瞬间开跑，改为延迟 35 秒后再启动，并在用户浏览时自动让行：缩略图并发槽与 D1 查询优先服务真实请求，页面首屏 API 总耗时实测从约 33 秒降至 0.24 秒，图片不再排队 2 秒+；
- 前端静态资源（页面、JS/CSS、Tag 词库）改由本地图片网关直接从 dist/ 提供，不再逐请求绕经 workerd，页面加载更快且不占用 Worker 请求槽；
- 启动器每次全新启动前自动清理超过 24 小时未使用的 `.wrangler/tmp` 临时目录（实测一次清理近 930 个目录、约 200 MB），避免长期使用磁盘膨胀。

### 修复：Pixiv 收藏 400 状态平滑兼容、移除顶栏冗余搜索 Tab、修复 Danbooru 抽卡漫游 500 报错与高级搜索符号支持

- **Pixiv 收藏与顶栏交互修复**：
  - 修复 Pixiv 收藏请求头缺少 App 身份标识以及对已收藏作品重复点赞返回 400 报错的问题，自动识别并平滑视为收藏成功；
  - 移除 Pixiv 顶栏冗余的「搜索」Tab 按钮，保留右侧全局搜索框与回车直达体验，界面更简洁明了；
- **Danbooru 抽卡漫游与语法增强**：
  - 修复 Danbooru 官方 API 在 `order:random` 模式下禁止携带 `page > 1` 导致上游抛出 500 错误的问题，强制 `order:random` 模式锁定 `page=1` 并禁用 HTTP 缓存，实现每次点击必出全新随机作品；
  - 放宽 Danbooru 查询符号白名单，全面支持 `<`、`>`、`=`、`~`、`*` 等高级检索语法（如 `ratio:<0.8`、`score:>50`）。

### 修复：画廊独立审计缺陷修复：足迹翻页语义对齐、存储异常内存态同步、随机抽卡抖动与画师直达守卫

- **足迹回溯体验修复**：
  - 修复足迹中多页 Pixiv 作品构造时可用页数与实际单图不一致导致详情侧栏「下一页」静默失效的问题，对齐真实页数；
  - 清空足迹时同步重置选中的历史作品状态；
- **存储与状态机优化**：
  - 修复 LocalStorage 超限异常触发截断时未同步更新内存态的问题，确保内存与持久化存储完全一致；
  - Danbooru 处于随机排序时点击「抽卡漫游」自动增加随机页偏移，杜绝同批结果“反复抽卡无变化”的困扰；
  - 修复 Pixiv「作者全集」在无画师信息记录时缺少优雅提示的问题；
  - 补充 `PixivTrendingTag` 类型定义消除类型逃逸，优化 Tab 切换与断开时的预取重置逻辑。

### 功能：Danbooru 与 Pixiv 图库全生态升级：多维筛选排序、Pixiv 个人资产联动（关注/收藏/点赞）、全套排行榜体系与相关作品推荐

- **Danbooru 灵感图库全方位增强**：
  - **快捷多维筛选栏**：支持排序方式（综合热度 / 高分榜 / 收藏榜 / 最新 / 随机）、评级（全部 / 全年龄 G / 微涩 S / 擦边 Q / R-18 E）、画幅比例（横屏 / 竖屏 / 方图）与单人模式（`solo`）；
  - **🎲 抽卡漫游 (Random Gacha)**：一键从海量图库中随机抽取高质量灵感；
  - **本地浏览足迹**：自动记录点开过的大图，支持秒级无网络回溯与一键清空。
- **Pixiv 个人资产打通与深度创作中枢**：
  - **个人资产生态**：新增「关注动态」（关注画师新作流）与「我的收藏」（个人 Pixiv 收藏夹）Feed 模式；
  - **双向同步 ❤️ 收藏 / 点赞**：在作品详情面板中可直接点击心形按钮为作品添加/取消 Pixiv 账号收藏；
  - **全套排行榜与历史穿越**：支持 8 大细分子榜单（**🔥 AI 专榜**、全站日榜、周榜、月榜、原创榜、新人榜、男性向、女性向），并配备 📅 **历史日期穿越选择器**，可任意回溯历史上某一天的排行榜神作；
  - **无限探索链路**：详情面板新增「相关作品推荐（看了又看）」，点击即刻切换到相似画风/题材插画；
  - **搜索多维筛选**：支持热门度/最新/最早排序与 `10000+ / 5000+ / 1000+` 收藏门槛快捷注入。

### 优化：解除 Danbooru 与角色库封面的 Safebooru 全年龄限制，接入 Danbooru 官方全库与多级候选兜底

- **全面接入 Danbooru 主站**：
  - 切换 `worker/routes/danbooruRoutes.ts` 与网关代理源至 `https://danbooru.donmai.us`；
  - 彻底去除 `rating: 'g'` 硬性过滤，放行所有评级图片，大幅提升角色、画师与画廊的图片收录与检索能力；
- **角色库封面多级候选智能兜底**：
  - 优化 `services/danbooruService.ts` 中的代表作选取算法：首选高质量单人经典立绘；若冷门角色未匹配到严苛单人图，自动降级放宽构图限制并以最高分可用图片兜底，彻底消灭角色卡片的空白占位。

### 功能：文件夹批量导入新增「清理无意义图片」面板，支持分类筛选并物理删除无元数据/非NAI/重复图片

- **本地废图与重复图片物理清理**：
  - 在批量导入扫描结果栏提供「清理无意义图片」专属入口与操作面板；
  - 自动归类统计 **无元数据 / 非 NAI 图片**（如普通截图、无关素材）与 **与现有风格串重复的图片**；
  - 支持按分类 Tab 查看、缩略图预览、逐项勾选与批量全选；
  - 基于 File System Access API 实现本地文件夹物理删除（支持二次确认与安全删除进度），彻底释放本地磁盘空间。

### 优化：文件夹批量导入查重规则改为纯提示词识别，排除尺寸/步数/模型/种子等非提示词参数干扰

- **纯提示词指纹识别**：
  - 调整 `components/chain/FolderBatchImportModal.tsx` 中的预设指纹判别（`computeChainFingerprint`），仅以正向提示词（Prompt）与负面提示词（Negative Prompt）作为风格串的唯一性特征；
  - 无论是不同分辨率尺寸（如竖屏、横屏、方形）、采样步数、模型版本还是随机种子，只要提示词相同即精准识别为「已在库中」，更符合创作者按配方管理预设的心流。

### 功能：全局设置新增「强制清空随机种子（始终随机）」模式，支持临时置空种子并在关闭后完整恢复

- **非破坏性种子清空体验**：
  - 在「全局设置 - 界面与内容显示」中新增「强制清空随机种子（始终随机）」开关；
  - 开启后，所有风格串与实验室（文生图/图生图/重绘/扩图）的随机种子输入框将暂时置空并标记「已强制置空」，生图请求自动使用全新随机种子，**不破坏预设底层原本保存的真实种子数值**；
  - 关闭设置后，预设原本保存的种子数字立即原样恢复显示与生效。

### 修复：纠正风格串常规保存时自动覆盖已有封面的问题，严格限定仅在无封面时自动设置初始封面

- **修复风格串封面保护策略**：
  - 调整 `services/chainCover.ts` 中的封面决策逻辑：当风格串已有封面时，右上角常规「保存」按钮仅保存提示词、模块、参数等内容变动，绝不自动将最新生成或当前翻看的图片替换为新封面；
  - 自动设置封面仅在风格串既有状态无封面（`!chain.previewImage`）且当前有生成图片时生效；如需主动更换已有封面，使用预览区的专用「设为封面」按钮。

### 修复：纠正导入 V5 图片元数据时普通非透明图片被误开启「透明背景」开关的问题

- **修复透明背景判断逻辑**：
  - 修复 `services/metadataService.ts` 中仅依据 `straight_alpha` 布尔字段存在即误判开启透明背景的问题；
  - 严格限定仅在元数据明确声明 `tag_hint_transparent_background: true` 或 `transparent: true` 时才启用透明输出，普通 V5 图片保持透明开关关闭。

### 功能：支持从本地文件夹批量读取原图为风格串，并引入「待实测」状态与生成后自动销标闭环

- **文件夹智能扫描与元数据鉴别**：
  - 风格串页面新增「批量导入」入口，支持一键选择本地文件夹（或拖拽文件夹/多张图片）。
  - 自动递归扫描并识别包含有效 NovelAI 元数据（PNG tEXt/iTXt/zTXt 以及 Alpha Stealth 隐写）的原图，过滤无生成参数的文件。
  - **生成指纹智能去重**：自动根据提示词与生成参数计算唯一指纹并与工坊现有预设比对，已在库中的重复图片自动标记「已在库中」并取消默认勾选，支持一键仅导入新素材。
- **原图直出封面与零成本沉淀**：
  - 提取完整正负提示词、采样器、步数、CFG、尺寸、多角色等参数批量创建风格预设，默认以图片文件名命名。
  - 直接将读取的本地原图作为风格串封面存入本地工坊存储，无需重新生成或花费任何 Anlas。
- **「待实测」状态标记与闭环去标**：
  - 批量导入的风格预设自动打上「待实测」标签，卡片左上角显示半透明精致的闭眼（`EyeOff`）徽标。
  - 风格串列表工具栏与移动端抽屉新增「仅看待实测」快捷筛选。
  - 在工坊/实验室中使用该风格串成功生成图片后，系统自动从预设中移除「待实测」标签与闭眼图标，完成工坊验证闭环。

## 2026-08-26
### 体验与文案：全面重构与净化前端文案体系，消除底层术语暴露并统一全局术语标准

- **消除底层泄露与开发黑话**：
  - 彻底清理暴露给用户的底层变量与工程术语（如 `prompt_text` -> `暂无提示词`，`Information Extracted` / `IE` -> `信息提取量` / `提取量`，`UC:` -> `角色专属负面:`，`local-cache/thumbnails` / `R2 原图` -> 转换为通俗的本地小图与原图数据描述）；
  - 去除 Git 专业术语（将 Chain 编辑器顶栏的 `Fork` 按钮与提示优化为符合创作直觉的 `复制为新串`）；
  - 清理多用户/SaaS 遗留警示（移除误导性的“游客模式 / 浏览他人串”黄色横幅）。
- **统一全局核心资产与操作概念**：
  - 全局导航与功能入口统一：规范导航栏为「画师库」与「角色库」（与「灵感库」保持一致），图库与详情页中统一收敛为「加入灵感库」与「送往实验室」；
  - 提示词概念统一：全应用统一为「提示词」与「负面提示词」（多角色卡片内规范为「角色提示词」与「角色负面提示词」）；
  - 主题明暗模式文案统一为「浅色」/「深色」/「跟随系统」；
  - 参数面板与画师卡片英文化本土化：参数面板标签与状态全面本地化（如模型、分辨率、生成步数、引导强度、采样器、随机种子、画质增强、负面预设、多样性、角色定位模式等），画师测试卡片消除 `Generate All`、`Failed`、`Queue` 等英文残留。
- **精简画蛇添足说明与主观背景**：
  - 精简全局设置中冗长的操作说明书与排布规则解释，去除非必要的限制提示（如 `-1.00～2.00` 约束说明与 6 人均分背景算式），回归清爽干练的专业工具质感。

### 交互：将实验室「角色参考」与「Vibe 氛围参考」全屏覆盖层重构为精致居中模态弹窗（Modal Dialog）

- **保护创作者心流（消除上下文丢失）**：
  - 彻底将过去粗暴霸占全屏的 `fixed inset-0` 管理大页面重构为标准的居中半透明大弹窗（`max-w-5xl` + `backdrop-blur-sm` + `md:rounded-2xl`）；
  - 打开参考素材管理时，背后的实验室画板、Prompt 与生成控制依然清晰若隐若现，创作者始终保持在调参心流中，不再产生“被强行拽出实验室”的割裂与迷失感；
  - 新增点击外部半透明遮罩快速退出、右上角 `✕` 明确关闭按钮、以及全局 `ESC` 键快捷关闭支持；在双栏选图界面底部提供「应用并返回实验室」明确确定入口。

### 架构与设计：建立统一视口层叠 Z-Index 六级阶梯架构并收敛设计令牌（Design Tokens）

- **统一视口层叠 Z-Index 阶梯架构**：
  - 将散落各处的随意 z-index（如 `z-[10000]`、`z-[1700]`、`z-[1200]`、`z-[1060]`、`z-[1050]`、`z-[300]`、`z-[60]` 等）彻底重构并收敛为规范严密的 6 级全局阶梯体系：
    - `z-[900]` ~ `z-[950]`：移动端悬浮条、吸底导航与浮动操作按钮；
    - `z-[1100]`：全屏工作区管理器与工作流侧面板（角色参考、Vibe、Prompt Agent、AITag 详情侧栏）；
    - `z-[1250]`：业务功能弹窗（全局设置、预设管理、分支、图片标注器、历史清理/成功、画师导入/配置、灵感库上传/灵感板、Tag词库更新等）；
    - `z-[1500]`：全屏大图查看器 / Lightbox（实验室生成大图、历史大图、灵感档案详情、角色大图预览）；
    - `z-[1800]`：高优先级二次确认弹窗（ConfirmDialog，无论从哪个管理器或弹窗触发均稳稳置顶）；
    - `z-[2000]`：全局最高级系统 Toast 通知（跨越任何浮层保持清晰可见）。
- **设计令牌（Design Tokens）与圆角体系全面收敛**：
  - 彻底清理各业务模块中残留的 `dark:border-gray-700`、`dark:border-gray-600` 与 `border-gray-300` 杂散令牌，统一收敛为亮色 `border-gray-200`、暗色 `dark:border-gray-800`；
  - 统一弹窗与容器层级圆角为 `rounded-2xl`，输入框与独立控件统一为 `rounded-xl`，暗色弹窗表面背景统一收敛为深邃质感的 `dark:bg-gray-900`。

### 视觉：采用现代立柱式侧栏设计（去除 Logo 下方多余横线，回归 56px 精炼顶栏）

- **消除视觉压迫感与栅格切割**：借鉴 Linear 与 VS Code 的现代工坊界面语言，彻底移除左侧侧栏 Logo 下方的多余 `border-b` 横线，将侧边栏重塑为通顶到地的连贯纵向立柱；同时将主工作区顶栏高度恢复为轻盈干练的 56px，彻底消除横线错位困扰与老式后台系统的格子压迫感。

### 优化：调整主题导入与导出图标为符合本地工坊直觉的专用图标（FolderInput / FolderOutput）

- **消除心智歧义**：将主题管理中的「导入」与「导出」按钮图标优化为专用的 `FolderInput`（文件装入 📥）与 `FolderOutput`（文件输出 📤），完全消除 Web 端上传/下载与本地应用导入/导出之间的视觉认知冲突。

### 功能：升级「设计主题」为完整主题预设管理系统（保存/一键切换/JSON导入导出/重命名/删除与锁定默认）

- **一体化主题管理**：直接改造设置面板顶层的「设计主题」区域为完整的主题预设中心，所有自定义与导入主题均以精致的微缩界面骨架卡片直观呈现并支持一键切换，彻底消除多余的分散区块；
- **内置默认锁定保护**：出厂默认主题 `NAI Atelier 默认` 带有 🔒 锁定标识，禁止重命名与删除，确保随时拥有一键恢复出厂的能力；
- **JSON 导入与导出**：支持单个主题或全部自定义主题导出为 `.json` 配置文件，并支持拖拽或选择文件导入（内置严格数据清洗与 ID 碰撞去重），方便跨设备同步与社区分享。

### 优化：将默认主题强调色调整为「晴空蓝」（#0ea5e9）

- **更通透清爽的视觉体验**：将默认强调色从偏紫的靛蓝（`#6366f1`）调整为预设选项中的「晴空蓝」（`#0ea5e9`），并在设置面板强调色预设中置于首位，重置外观或新设备访问时默认呈现更加纯净清爽的高光视觉。

### 修复：本地服务启动时内部 Worker 端口冲突导致 workerd 异常崩溃

- **智能探测与自动避让**：本地服务脚本（`scripts/local-server.mjs`）在启动内部 Cloudflare Worker 前动态探测端口可用性，当 Windows 系统或代理工具占用 `3001` 端口时自动向后顺延分配可用端口（如 `3003/3004/3005`），并自动注入图片网关反向代理，彻底解决 `std::terminate() called with no exception` 崩溃问题。

### 视觉：重塑默认主题 NAI Atelier 黑夜模式配色与材质层级

- **消除冷蓝眩光与视疲劳**：将暗色模式下的全局底色从高偏色冷深蓝（`#101624`）重构为沉静高级的石墨深灰/炭黑（`#0d0f14`），彻底去除夜间由于短波长蓝光聚焦引起的睫状肌疲劳与光晕震颤感；
- **重塑材质与浮起层级（Elevation）**：建立清晰的表面明度阶梯（画布 `#0d0f14` → 侧栏 `#12151c` → 面板/卡片 `#171b24` ~ `#1b202c`），配合精致微光边框（`rgba(255,255,255,0.08)`），让作品卡片自然浮起，消除暗部粘连与泥泞感；
- **强调色暗色自适应音调映射（Tone Mapping）**：自定义强调色（如晴空蓝）在黑夜模式下自动进行降噪降饱和与深邃微光适配，左侧栏「项目 Agent」按钮与主操作按钮由刺眼的实心荧光蓝升级为优雅的深色微光渐变，静止时克制沉静、交互时自然点亮，不再喧宾夺主；
- **安全模式暗色磨砂遮罩升级**：优化暗色模式下的高斯模糊与亮度对比，去除暗沉脏浊的暗斑色块，呈现纯净高级的熏黑磨砂玻璃剪影质感。
### 修复：AITag 画廊过滤无有效 Prompt 的作品

- AITag 作品列表（搜索、本地缓存、滚动追加）现在会过滤掉首图没有有效 prompt 的作品：这类条目打开详情后只会显示「无 prompt_text」、无法导入或生成，是用户明确不要的垃圾数据，不再进入画廊；
- 判定标准与导入逻辑完全一致：`Comment.v4_prompt` → `Comment.prompt` → `v4_prompt` → `prompt` → `Description` → `prompt_text` 任一字段存在非空白内容即视为有效 prompt，全部为空（或无任何元数据）的作品从列表剔除；
- 详情面板同步过滤无有效 prompt 的图片，首图快速预览壳（`buildPreviewDetail`）在首图无有效 prompt 时直接跳过，彻底消除「无 prompt_text」空壳展示；
- 若某一整页作品全部被过滤，滚动追加流会自动连续拉取后续分页填补空白（最多额外连拉 6 页），列表不会卡住或出现大段空白；
- 新增 `hasAitagImagePrompt` 判定函数（与 `extractAitagPrompt` 同一提取顺序、直接基于原始字段判定）及 `extractAitagPrompt` / `hasAitagImagePrompt` 定向单测。

### 修复：V5 模型切换时步数未正确跟随官方默认（28 → 23）

- 修复实验室切换生成模型时步数停留在 28 的问题：旧会话或旧数据可能保存了「V5 模型 + 28 步」的过期组合（28 步是 V5 发布前的默认步数），此时再次选择 V5（含 V5 Full ⇄ V5 Curated 系列内部切换）会因当前步数不等于前一模型默认值而保留 28，无法自动转为 23；
- 改为按目标模型判断的稳健规则：当前步数是官方默认之一（23/28）或未设置时，切换到 V5 系列自动设为 23 步、切换到其他模型自动恢复 28 步；其余自定义步数（如 16）一律原样保留；
- 外部导入图片元数据时继续严格遵从图片自带的步数参数，不强制覆盖；`LAB_DEFAULT_PARAMS` 的默认步数改为由默认模型（V4.5）推导，保证默认步数与默认模型始终一致。

### 优化：模型切换自适应调整推荐采样步数（V5 系列默认 23 步）

- 在实验室切换生成模型时自动匹配官方推荐的采样步数：
  - **V5 系列模型**（`V5 Full` / `V5 Curated`）：默认步数自适应调整为 23 步；
  - **其他模型**（`V4.5` / `V4` 等）：保持 28 步默认值；
  - 若用户此前已手动修改为自定义步数（如 16 或 35），切换模型时忠实保留该自定义值不变；从外部导入图片元数据时继续严格遵从图片自带的步数参数。

### 精简：彻底统一正面提示词输入并移除多字段拆分

- 遵循做减法与聚焦核心创作流原则，彻底移除 `splitPromptFields`（风格串与主体／变量提示词拆分）设置与双框输入流：
  - **统一单框输入**：正面提示词区域彻底回归清爽、直观的单一完整提示词文本框，对齐 NovelAI 官方与主流生态的标准数据流；
  - **消除导入搬运负担**：任何历史记录、外部图片或灵感库的导入直接回填单一提示词，无需手动在画风框与主体框之间剪切搬运；
  - **简化状态流**：精简偏好设置项与跨组件 props 传递，移除冗余的状态合并与反解析逻辑。

### 精简：移除实验室提示词输入中的冗余「提示词模块」组件

- 移除 `ChainEditorPromptInputs` 中过时且认知负担重的前/后置「提示词模块」（`ChainEditorModules`），让提示词输入区域回归纯净直接的单框/双框输入体验，避免界面视觉噪音与机械式过度设计。

### 视觉：重构实验室折叠栏与内部卡片视觉一体化

- 将 `LabModuleSection` 重构为一体化手风琴卡片：
  - 折叠时为完整圆角卡片，展开时 Header 与 Body 闭合为同一张卡片（顶部标题栏带细分隔底线与平滑背景过渡）；
  - 移除内容区原本悬空的顶部外边距，改由容器统一提供内边距；
  - 剥离参数设置（`ChainEditorParams`）、角色专属提示词（`ChainEditorCharacters`）、角色参考（`CharacterReferenceManager`）、Vibe Transfer（`VibeManager`）以及图片编辑控制项内部冗余的双重外框与嵌套边距，彻底消除“多层套框与悬空折叠条”带来的视觉割裂。


### 优化：消除实验室折叠模块与内层卡片的双重标题冗余

- 消除折叠栏展开后与内层卡片顶部的同名重复标题：
  - **参数设置**：移除内部卡片顶部的静态「参数设置」标签，来源徽章自然融入顶部，节约纵向空间；
  - **角色专属提示词**：移除内层重复的「角色专属提示词」文字，保留来源徽章与右侧「AI 自动构图 / + 添加角色」操作按钮；
  - **提示词输入**：单字段模式下直接展示纯净输入框与来源/复制工具栏；拆分模式下保留「基础画风」与「主体／变量提示词」的差异化字段标签；
  - **全局负面提示词**：移除内层重复标题，保留来源徽章与复制按钮；
  - **编辑模式**：同步移除图片编辑控制面板中的重复「提示词输入」与「编辑参数」标题。

### 优化：重构生图参数面板布局与自定义分辨率展开卡片

- 重构 `ChainEditorParams` 栅格分层：将「生成模型」与「图片尺寸」设为对称平分的 2 列网格，基线永远保持水平对齐。
- 当选择「自定义」分辨率时，在模型与尺寸行下方平滑展开全宽配置卡片（左侧并排宽高输入框、右侧垂直排列 Opus 免费像素联动开关与状态条），彻底消除右列拉伸导致的空白失衡。
- 将下方「采样器」、「步数」与「Seed」保持干净的 3 列平分网格，提升桌面与移动端整体视觉协调与信息密度。

### 交互：设置或上传封面时自动保存风格串／角色串

- 在风格串与角色串详情中点击「设为封面」或「手动上传」新封面时，在上传封面的同时自动保存当前的所有提示词、模块与参数改动，无需额外再点击一次右上角保存按钮。

## 2026-08-26

### 优化：AITag 模型筛选（如 V5）自适应批量填充

- 修复 AITag 页面在筛选较新或稀疏模型（如 V5）时滚动到底部加载等待慢的问题：当开启模型筛选且单页有效条目不足以填满视口时，前端自动连续批拉后续分页（最多 4 页或凑满 15 张目标增量），消除多次串行触底等待，让模型筛选下的瀑布流滚动同样流畅。

### 重构：将 ChainEditor 巨石组件拆分为模块化子组件

- 将 `components/ChainEditor.tsx`（2849 行）按功能域拆分为 `components/chain/` 下的 6 个子组件：`ChainEditorHeader.tsx`（顶部标题栏 + 操作按钮 + 信息编辑弹窗）、`ChainEditorPromptInputs.tsx`（基础画风/主体提示词输入区）、`ChainEditorModules.tsx`（提示词模块列表）、`ChainEditorCharacters.tsx`（多角色提示词管理）、`ChainEditorPresetModal.tsx`（引用预设弹窗：列表 + 详情确认）、`ChainEditorForkModal.tsx`（保存到库 / Fork 弹窗）。
- 共享展示件（`PresetSourceBadge`/`PresetSourceBadges`/`PromptCopyButton`）与 `PromptAgentOverlayController` 移入 `components/chain/PresetSourceBadges.tsx` 供各子组件复用；主组件 `ChainEditor.tsx` 精简至约 2200 行，仅保留全部状态、处理函数与未拆分区域的编排。
- 全部状态与处理器仍留在主组件，子组件为纯展示组件；JSX 逐字迁移、DOM 结构逐节点保持一致，四种实验室模式、移动端胶囊、灯箱、拖拽导入、预设导入与 Fork 行为 100% 不变，属纯结构重构。
- 递增补丁版本至 0.115.8（refactor: split ChainEditor monolith into modular chain subcomponents）。

### 重构：将 Worker 后端超级文件按业务域拆分为独立路由模块

- 将 `worker/index.ts`（4481 行）按业务域拆分为 8 个模块：`routes/settingsRoutes.ts`（局域网访问、Anlas 点数、管理端、NAI 代理、Chains/Artists）、`routes/historyRoutes.ts`（本地生图历史 D1+R2、灵感看板/灵感库、Agent 概览）、`routes/vibeRoutes.ts`（Vibe Transfer 与角色参考图库）、`routes/aitagRoutes.ts`（AITag 抓取与反代）、`routes/danbooruRoutes.ts`（Danbooru 搜索）、`routes/stBridgeRoutes.ts`（st-chatu8 历史互通）、`routes/pixivRoutes.ts`（占位模块，Pixiv 实现在本地网关侧）、`routes/types.ts`（公共 Env/RouteContext 与共享工具）。
- `worker/index.ts` 精简至约 200 行，仅负责环境初始化、CORS、全局错误捕获与按原顺序的路由分发；全部 HTTP 路由、请求参数、响应格式与错误处理 100% 保持不变，属纯结构重构。
- 递增补丁版本至 0.115.7（refactor: split worker/index.ts into domain route modules）。

### 修复：重置按钮仅重置当前激活的实验室页面

- 生图实验室右上角「重置」不再一键清空全部四个模式，而是严格作用于当前激活的页面：文生图页面仅恢复文生图的提示词、模块与参数为默认值；图生图／局部重绘／扩图页面仅清空当前编辑模式的底图、蒙版与提示词草稿。
- 重置确认弹窗按当前模式显示对应文案（如「重置局部重绘？将清空局部重绘的底图、蒙版与提示词。此操作无法撤销。」），并保持红色危险样式。
- 编辑模式重置只删除当前模式的持久化底图与蒙版资产、重置当前模式草稿与画布选区状态，其他两个编辑模式的草稿、底图与蒙版资产，以及文生图内容全部原样保留。
- 新增实验室默认参数常量 `LAB_DEFAULT_PARAMS`（832×1216、28 步、随机种子、UC 预设 None），文生图重置与编辑草稿复位共用同一免费边界默认值。

### 文档：规范 AI 协作生命周期与 Worker 会话管理

- 在 `AGENTS.md` 中新增「AI 协作生命周期与会话管理规范（Director-Worker 规范）」，明确跨对话记忆基于项目文档权威性、同一个功能/模块连续调整禁止随意销毁 Worker 而是通过消息追加复用上下文，以及新建任务必须自包含 Brief 的协作准绳。

### 文档：沉淀 AI 协作效率与分级验证梯度准绳

- 在 `VIBER_INTENT.md`（第 8.3 节）与 `AGENTS.md` 中补充 AI 协作效率与验证梯度（Pragmatic Verification）哲学：普通 UI/样式/局部逻辑追求 1~3 分钟内的敏捷反馈闭环（静态自审 + `tsc` + 定向单测），严禁为了简单样式改动而动辄启动全套构建与 Headless 浏览器模拟导致冗长等待；复杂算法、核心状态机与破坏性重构才采用全流程端到端验收。
- 明确强调防范 Tailwind 响应式断点等逻辑倒置与单测假阳性，确立「严禁用重型外部流程代替轻量、精准的逻辑思考」原则。

### 修复：统一移动端编辑模式生成入口与预览布局

- 图生图、局部重绘和扩图的手机端底部不再显示整宽大矩形生成按钮，改为与文生图一致的右下角悬浮胶囊「生成 · 免费／点数」，费用标签与预览卡内保持一致并随选区实时更新。
- 编辑模式移动端悬浮胶囊直接触发当前编辑草稿生成，生成中显示进度、键盘弹出时自动隐藏；最近结果缩略图圆钮继续保留。
- 预览卡在手机竖屏与窄容器（Agent 停靠、窄桌面）下改为按内容自适应高度，不再固定 900px 高度溢出并遮挡左侧底图说明文字。

### 修复：彻底修正移动端生成按钮逻辑倒置与悬浮胶囊状态同步

- 修复 `ChainEditorPreview` 中 `hideGenerateButtonOnMobile` 的类名逻辑倒置：原先 `lg:hidden` 会让「生成扩图结果」矩形大按钮在手机端显示、桌面端隐藏；现改为 `hidden lg:flex`，手机端隐藏、桌面端（≥1024px）显示。
- 编辑模式移动端悬浮胶囊改用 `onGenerateBarChange` 回调 + 父组件状态驱动（替代仅写一次的可变 ref），底图加载、框选区域、强度等变化后费用标签与「请先选择底图」禁用态即时同步，不再出现胶囊与预览卡内标签不一致（如 14 点 vs 20 点）的过期状态。
- 移动端编辑预览卡壳层改为按内容自适应（`flex: 0 0 auto` + 最小 240px），修复预览卡在手机竖屏塌缩为 1px、图片不可见的问题；预览卡、历史导航箭头、删除／清除按钮与下方底图控制区不再层叠或互相遮挡。

### 文档：新增 Viber 创作意志与产品哲学宪章

- 新增 `VIBER_INTENT.md`，从完整 Git 历史、CHANGELOG、README 与 AI_WORKLOG 归纳 Viber 的长期产品意图：本地单人工坊北极星、Local-First 所有权边界、即时单张反馈的创作单位、闭环优于孤立功能、克制统一的界面审美、算力与成本哲学、现役模型代际取舍、实验室四模式纯粹性、资料库自生长机制，以及对 AI 协作的期待与面向未来的决策准绳。
- `AGENTS.md` 将该文档列为最高优先级必读，要求任何 AI 在修改项目前先阅读并按产品意图校准方案。
- `AGENTS.md` 项目概览补充领域知识、NovelAI 接口说明与历史变更文档指引。

## 2026-08-25

### 功能：增强实验室底图导入与分辨率控制

- 图生图、局部重绘与扩图的编辑区域支持直接拖入 PNG、JPEG、WebP 底图，编辑区会优先拦截图片拖放，避免被全局 PNG／JSON 工作流配置导入处理；导入后继续复用尺寸校验与规范化流程。
- 文生图新增自定义宽高、64 像素步进与 Opus 免费像素联动开关，调整任一维度时可自动最大化另一维度，并实时显示总像素及免费范围状态。
- 图片编辑请求强制清空文生图多角色提示词、角色负面词与坐标，扩图画布也不再改写无效的角色位置，确保编辑模式只使用当前整图提示词。

## 2026-08-25

### 修复：从串详情返回原列表位置

- 风格串与角色串详情顶部改为“返回箭头 + 铅笔 + 串名称”，长标题在桌面与手机宽度下保持单行省略，不再挤压操作按钮。
- 从详情返回时不再重复刷新常驻列表，优先保留原滚动位置；保存后如果排序或分批渲染使原卡片离开视野，会直接在内部滚动容器中定位回该卡片。
- 角色串资料库共用同一返回锚点规则，并在从角色大图进入编辑前关闭大图层，避免返回后残留遮挡。

### 修复：区分风格串详情与多模式实验室

- 风格串与角色串详情固定为文生图工作区，隐藏图生图、局部重绘和扩图导航；旧会话即使停留在编辑模式，重新进入详情也会回到文生图。
- 详情顶部改为“铅笔 + 串名称”，铅笔打开名称与信息编辑，名称宽度限制在左侧参数区内并在过长时显示省略号；实验室仍保留四模式导航。

### 修复：补全编辑预览操作与全量历史选图

- 图生图、局部重绘和扩图会根据右侧正在显示的历史图片恢复删除、清除、前后切换、计数与下载操作，不再错误依赖文生图当前索引。
- “选择历史图片”改为直接读取历史页面的全量数据源，显示总数并支持分页；缩略图按原图比例完整显示，桌面与手机端均使用独立滚动区域。

### 功能：统一实验室图片预览与编辑工作区

- 图生图、局部重绘和扩图的右侧图片区改为复用文生图预览组件，统一图片尺寸、放大、下载、历史翻页及历史组操作；编辑结果会直接显示在同一预览区。
- 局部重绘与扩图画板移入左侧“底图与导入”，图生图不再显示不需要的画板，右侧图片不再因 Canvas 工作区而额外缩小。
- 底图来源新增“上传图片”“文生图最新”“选择历史图片”，切换底图时保留当前提示词与编辑参数；历史选择器适配桌面和手机界面。

### 修复：让实验室顶部辅助工具跟随当前页面

- Tag 辅助现在覆盖文生图、图生图、局部重绘和扩图的正负面提示词；图片反推 Tag、PNG／JSON 元数据导入与引用预设也会写入当前页面，不再误改隐藏的文生图草稿。
- 编辑页不再显示无法完整保存底图、蒙版与编辑参数的“保存到库”按钮；重置确认文案明确提示会清空四种模式及编辑资产。
- 补充三种编辑模式的 Tag 输入、预设编译、反推 Tag、元数据导入和保存能力回归测试，并验证桌面与手机布局。

### 修复：统一实验室主题色与模式导航宽度

- Variety+ 开关、CFG Scale 与 CFG Rescale 统一跟随外观设置中的主题强调色，不再混用写死的粉色。
- 文生图、图生图、局部重绘和扩图导航改为四项等分，并与左侧编辑区使用相同宽度基线；窄桌面、Agent 停靠布局和手机端自动切换为整行导航与分行工具栏。

### 修复：整理 Variety+ 参数布局

- 将 Variety+ 从正面质量预设下方移入 CFG 引导控制区，并改用与项目一致的胶囊开关，避免误解为质量预设的附属选项。
- 正负面预设与 CFG 滑块在手机端改为单列排列；文生图、图生图、局部重绘和扩图共用一致布局。

### 维护：增强 AI 需求推演规则

- AI 协作规则新增强制同类项审计：实现需求时主动检查复用页面、相关状态、兼容链路与测试，同时为产品决策、数据操作和外部影响保留明确授权边界。

### 修复：统一预设关闭项顺序

- 负面预设与正面质量预设保持一致，将 none 固定在下拉框首项；无论官方运行时返回顺序如何，文生图、图生图、局部重绘和扩图都使用相同排序。

### 修复：恢复正面质量预设下拉框

- 正面质量预设恢复为单一下拉框，不再额外显示开关；V5 提供 none／standard／light，V4 与 V4.5 提供 none／standard，并将 none 固定在首项。
- 保留 none 不追加质量提示词的真实关闭语义，以及旧风格串未保存模型时按默认 V4.5 读取预设的兼容逻辑。

### 修复：对齐官方质量与负面预设交互

- 正面质量预设恢复为独立开关；V5 开启后可选 standard／light，V4 与 V4.5 不再显示只有 standard 的无意义下拉框。
- 修复官方实时预设不含 none 时“关闭质量标签”仍被请求层回退为 standard 的问题，关闭后不再追加质量提示词。
- 旧风格串或实验室草稿未显式保存模型时，统一按界面默认 V4.5 查询模型能力，负面预设不再错误退化为仅 none。

### 修复：精简图片编辑参数

- 图生图、局部重绘和扩图的参数区不再显示无法操作的“图片尺寸（当前画布）”伪输入框，只保留真正可调整的生成参数。

### 修复：实验室滑块与提示词字体

- CFG Scale 与 CFG Rescale 恢复主题色原生滑块外观，避免可调参数被误认为禁用状态。
- 统一文生图、图生图、局部重绘和扩图提示词输入框的等宽字体与正常字重，修复负面提示词意外继承粗体的问题。

### 修复：安全模式屏蔽蒙版画笔

- 开启安全模式时，局部重绘与扩图的画笔、橡皮擦、蒙版历史操作及 Focused 选区交互全部暂停，并显示明确提示。
- 安全模式不会清除已有蒙版；关闭后可继续编辑，扩图的手动蒙版开关状态也会保留。

### 修复：按编辑模式隔离蒙版画笔

- 图生图彻底关闭透明蒙版画布的焦点与指针交互，并忽略旧草稿中遗留的蒙版数据。
- 扩图默认只使用自动生成的新增边缘蒙版；新增“手动调整蒙版”开关，开启后才显示并启用画笔、橡皮擦、撤销和反转工具。

### 修复：打开全局设置时白屏

- 修复实验室布局折叠块在状态更新阶段读取已失效 React 事件对象、导致整个设置页崩溃白屏的问题。
- 同步清理设置队列输入与历史日期筛选中的同类延迟事件读取，并补充设置页打开及折叠交互回归测试。

### 修复：实验室安全模式与大图查看

- 图生图、局部重绘和扩图的 Canvas 底图现在会沿用全站安全模式遮挡，并支持点击后临时揭示。
- 大图查看器明确豁免安全模式，打开大图后不会再次被模糊遮挡。

### 新增：四种实验室模式独立模块布局

- 设置中的实验室布局改为文生图、图生图、局部重绘、扩图四个独立折叠配置区，每页分别保存模块顺序与默认展开状态。
- 图生图、局部重绘和扩图的提示词、底图、参数、编辑工具、角色参考与 Vibe 模块接入同一排序容器；旧版本只保存过文生图布局的用户会自动迁移，其他模式保留推荐顺序。

### 新增：按模型与编辑模式对齐实验室组件

- 实验室会根据当前模型和模式自动隐藏不支持的 Vibe、角色参考等辅助模块，切换模型时停用不兼容功能但保留原有图片与参数。
- 统一 V5 角色提示词上限 32、V4/V4.5 上限 6、Vibe 上限 16，并让第 5 张起的额外费用、前端校验、网关校验和 Agent 工具保持一致。
- V5、V4.5 Full／Curated、V4 Full／Curated 使用官方运行时同步的能力与预设；补充模型切换、超限角色、编辑模式 Vibe 隔离和 Vibe 16 张回归测试。

### 修复：对齐模型预设与图片编辑角色坐标

- 生成请求改用官方按模型提供的质量／Undesired Content 预设 ID，旧开关和数字预设继续兼容；未知模型不再误套用其他模型的质量提示词。
- Focused Inpainting 会把角色中心坐标转换到局部请求画布，扩图后会按新增画布偏移同步角色坐标；网关与前端共用模型能力限制并允许 V4.5 Curated 的官方角色参考。
- 修复编辑模式保留 Vibe 选择却仍写入请求的问题，并补充模型能力、预设拼接和角色坐标回归测试。

### 新增：同步 NovelAI 官方模型能力与提示词预设

- 网关现在从官方 Web 应用同步模型能力、角色数量上限、Vibe／角色参考／透明背景支持情况，以及按模型区分的质量和 Undesired Content 预设。
- 运行时快照与健康检查纳入上述新字段；官方 bundle 结构变化或提取不完整时继续沿用最近完整快照并明确记录失效项。

## 2026-08-24

### 新增：图片编辑历史蒙版独立存储与恢复

- 新生成的编辑历史不再把完整蒙版 Base64 写入 D1 或历史列表；本地浏览器使用独立 IndexedDB 蒙版仓，远端使用按用户与历史 ID 派生的私有 R2 路径，并在删除、批量清理和旧数据迁移时联动处理。
- 历史详情按需读取蒙版；编辑历史会恢复原操作类型、Strength／Noise、Focused 选区、上下文、扩图和请求尺寸，默认清空旧蒙版，也可显式选择复用原蒙版。
- 工作区底图与蒙版改用稳定资产 ID、短延迟合并写入，并在生成、切换模式、离开页面和重置实验室前落盘或清理；异步底图与蒙版恢复不会覆盖用户的新笔画。
- 补充历史详情的编辑元数据与 Key 指纹展示，兼容旧 `actualCost` 和 Base64 蒙版记录，并为 Focused 选区移动／缩放、历史恢复和资产隔离补齐回归保护。

### 修复：统一图片编辑费用估算与真实请求尺寸

- 费用确认现在读取编辑器画布的真实像素尺寸，并与最终发给 NovelAI 的请求共用同一组宽高，避免参数面板尺寸与底图实际尺寸不一致。
- 编辑确认文案改为“本地结算估算”，不再把网关估算值误称为 NovelAI 官方实际扣费；生成成功后继续刷新当前 Key 的账号额度。

### 新增：官方图片编辑请求链与底图尺寸规范化

- 按 NovelAI 图片编辑接口生成 1/8 分辨率、阈值化蒙版；普通局部重绘与扩图共用 Inpainting 回贴流程，扩图不再伪装成 Focused Inpainting。
- 实现 Focused Inpainting 的选区面积限制、32–96 像素上下文、局部裁切放大、空蒙版自动填充和结果回贴；仅局部重绘且满足 Opus 条件时显示免费。
- 修复编辑请求的 Seed、采样器和真实请求尺寸传递，并让费用估算使用 Focused 实际请求尺寸；网关与客户端改用 `estimatedCost` 语义及新响应头，兼容旧响应头。
- 导入非 64 倍数或超限底图时提供居中裁剪、完整保留并填充、直接缩放三种规范化方式，确认后才修改画布。
- 补充 Focused、扩图计费、未来模型清单、尺寸规范化和 DPM++ 采样器回归测试，并通过官方实时常量同步校验。

### 修复：稳定图片编辑器交互状态

- 修复保存蒙版时重载底图并清空撤销栈的问题，撤销／重做最多保留 30 步。
- 修复编辑器全局快捷键劫持提示词输入框撤销的问题，并为快速笔刷轨迹补齐插值。
- Focused Inpainting 仅在局部重绘中显示，改用 32–96 像素官方上下文范围；编辑请求期间禁用重复操作。
- 移动端编辑模式不再显示错误的文生图悬浮生成按钮，编辑模式尺寸显示改为当前真实画布尺寸。

## 2026-08-23

### 修复：统一图片编辑工作区左右布局

- 图生图、局部重绘和扩图复用文生图的实验室双栏外壳，桌面端固定为左侧控制区、右侧预览与生成按钮，移动端固定为上方预览、下方控制区。
- 编辑模式统一使用文生图的预览卡片、费用标签和生成按钮，不再出现旧图片编辑面板的反向布局或重复生成按钮。
- 新增布局回归测试，锁定图生图、局部重绘和扩图的模式专属控件显示条件。

### 修复：统一图片编辑模式布局

- 图生图、局部重绘和扩图不再使用旧图片编辑面板的“左侧画布／右侧控制栏”布局，改为与文生图一致的“左侧 Prompt 与参数／右侧图片预览与生成按钮”。
- 编辑模式统一使用实验室现有的输入框、分组、边框、间距、按钮和费用标签语言，不改变文生图页面原有的左侧调参与右侧预览结构。

### 重构：统一四模式实验室与编辑 Prompt

- 实验室顶部改为同级的“文生图／图生图／局部重绘／扩图”模式导航，移除实验室返回按钮和图片预览区内的编辑入口，不再使用图片编辑弹窗；桌面端与移动端共用同一工作区布局。
- 每个模式拥有独立的 Prompt、负面 Prompt、模型与生成参数草稿，使用当前标签页 `sessionStorage` 持久化；图像与蒙版 Blob 使用独立 IndexedDB 私有资产存储，不写入历史、不上传公共队列。
- 编辑模式提供完整可编辑 Prompt、负面 Prompt，以及恢复当前完整 Prompt、仅保留风格串、恢复历史 Prompt、清空 Prompt；编辑 Prompt 不反向修改风格串，编辑结果历史保存实际请求文本和 Prompt 来源。
- 历史图片、当前预览图和本地导入图片都可作为底图；扩图后的画布与自动蒙版继续保存为编辑草稿，编辑结果始终追加新历史记录，不覆盖原图。
- 编辑模式补充模型、尺寸、步数、CFG 与采样器控件，保留现有模型能力校验、Key 隔离、公共队列、费用预估、实际结算及失败／取消不累计流程。
- 新增四模式会话恢复与草稿隔离测试；全部验证使用固定 Mock 和脱敏数据，不调用真实 NovelAI 生图接口。

### 新增：图生图、局部重绘与扩图工作区

- 实验室预览区、历史图片详情和移动端入口统一打开图片编辑面板，支持本地导入底图、图生图 Strength／Noise、局部重绘画笔／橡皮擦／撤销重做／蒙版反转，以及 Focused Inpainting 区域选择和最小参照范围。
- 扩图基于 Canvas Resize + Inpainting，支持四边独立扩展、64 倍数与总像素校验，自动生成新增区域蒙版并真实提交 NovelAI。
- 编辑请求复用现有 Key 隔离、公共队列、Worker／网关结算和失败取消不累计流程；普通编辑不套用 V5 普通生图免费逻辑，只有确认 Opus 条件的 Focused Inpainting 才显示免费，否则显示预计 Anlas 或官方实际结算提示。
- 编辑结果追加保存为新历史图片，记录操作类型、父历史、底图来源、Strength／Noise、蒙版、扩展画布、Focused 状态、预估费用与 Key 指纹，不覆盖原图并可继续导入编辑。
- 新增脱敏 payload、采样器／模型能力、画布尺寸、蒙版编码与费用结算测试；未调用真实 NovelAI 生图接口。

### 新增：V5 透明 PNG 与生成过程预览

- V5 Full／Curated 新增透明背景开关，生成请求自动补充 Alpha 提示与官方透明参数；预览区使用棋盘格呈现透明区域，导入 NovelAI PNG 时可恢复透明输出设置。
- 全局设置新增按设备记忆的“生成过程预览”，通过 NovelAI SSE 接口逐步显示采样中间帧与步数；不可用时在最终帧到达前安全回退普通生成，最终帧到达后绝不重复生图。
- 本地网关与 Cloudflare Worker 新增流式代理；公共队列继续显示排队／生成／完成状态，只有 `final` 事件才按原请求 Key 结算个人 Opus 与 Anlas，失败、取消和中间帧不累计。
- 流式模型能力接入官方 Web 应用自动同步并通过联网自检；当前官方能力表确认 V4、V4.5、V5 均支持中间帧，Alpha 透明仍只向 V5 开放。

### 修复：阻止凭据误入 Git 与前端构建

- 移除已退役且仍明文存在于 Wrangler 配置中的游客口令，并停止把 `GEMINI_API_KEY` 注入浏览器端 JavaScript。
- 补全环境变量、私钥、证书和凭据文件的 Git 忽略规则；NovelAI、模型服务、Pixiv 与局域网实际凭据仍只保存在浏览器或本机 `local-data`。
- 新增不回显原值的 Git 跟踪文件密钥扫描，覆盖常见服务 Token、私钥、Bearer、JWT、带凭据 URL 和明文命名配置；扫描接入网关测试与提交钩子。

### 新增：Agent 接入模型感知的 NovelAI 官方知识

- 新增带主题、适用模型、来源级别、复核日期和官方链接的本地结构化知识索引，覆盖 V5 能力、Tag、权重、多角色、文字、质量 Tag、负面提示词、采样、Vibe 与 Precise Reference；Agent 可按需检索和读取，不再依赖一整段容易过时的内置常识。
- Agent 现在会读取实验室当前 NovelAI 模型、提示词拆分方式与 Tag 辅助状态，并明确采用“官方发布公告 > 模型专用官方文档 > 通用官方文档 > 项目经验”的规则优先级。
- 修正 V4.5 规则误套 V5 的问题：V5 不再被约 512 T5 Token、六角色和 5×5 定位限制，角色工具按当前模型保留最多 22 个 V5 角色；V4/V4.5 仍保持六角色边界。
- 将固定横图、固定主体权重和 Tag 数量等内容标记为项目经验，并明确 V5 发布时 Vibe Transfer 与 Precise Reference 尚不可用，避免 Agent 把经验值或旧文档误称为官方规则。

### 新增：实验室 Tag 辅助快捷开关

- 实验室右上角新增与现有工具一致的 Tag 辅助按钮，位于“图片反推 Tag”和“重置实验室”之间；`Tags` 图标右下角以 `o`／`−` 直接表示开启与关闭状态。
- 一个开关统一控制全局、主体、模块、角色及负面提示词输入框的补全和逐 Tag 翻译，并在本机记住上次选择。
- 关闭后输入框恢复为普通文本编辑，不加载词典、不发起候选或翻译查询；切换时保留全部提示词并取消尚未执行的查询，方便 V5 自然语言输入。

### 修复：实验室 Tag 补全不再干扰文本输入

- 单字段“全局提示词”停止在每次渲染时清理原始文本，末尾逗号、空格和换行现在可以正常连续输入；生成时仍会独立规范化最终 Prompt。
- 候选默认不再抢占 Enter 与 Tab，只有使用方向键明确选中后 Enter 才确认补全；中文输入法组合期间会停用快捷键并取消旧查询。
- 移除按键抬起时的重复查询并加入短延迟合并，输入逗号会结束当前候选；英文与中文逗号都可划分 Tag。
- 候选改为单击／单触选择，在 Tag 中间补全会替换完整当前 Tag，并正确保留花括号、方括号与权重闭合符号。
- 新增 16 项文本保真、键盘、输入法、迟到请求、指针和补全边界回归测试。

### 优化：保存操作使用绿色语义色

- 实验室“保存到库”改为柔和的 emerald 浅色按钮，已有风格串存在待保存内容时使用实心绿色；保存完成或不可用时仍保持灰色。
- Fork 保留主题蓝色，重置保留红色，让工具、危险操作与保存操作在右上角具备清晰且稳定的颜色语义。

### 优化：整理实验室导入与提示词复制入口

- 图片／JSON 配置导入从全局提示词字段移至编辑器右上角工具栏，并改用方向正确的文件导入图标；工具栏按“导入、引用预设、图片反推、重置、保存”排列，让最终保存固定在最右侧。
- 移除低频且重复的 JSON 粘贴入口与弹窗，保留文件选择和拖放两种全局导入方式。
- 正面、主体与全局负面提示词统一使用主题色“复制”按钮，并只复制按钮所在输入框的内容，避免“复制完整提示词”的范围歧义。

### 新增：当前预览图片自动成为风格串封面

- 在实验室新建风格串或保存已有风格串时，自动把预览区当前显示的图片复制为独立永久封面；左右切换到哪张，就保存哪张。
- 当前显示原封面时不会重复上传，没有可用图片时保持无封面；仅切换预览图片也会重新启用保存按钮，角色串不受影响。
- 刚生成但尚未写入历史的临时图片同样可以成为封面；读取或上传失败时保留未保存状态，方便直接重试。

### 优化：统一工作区顶栏布局

- 风格串、实验室、画师 Tag、角色 Tag、AITag、Danbooru、Pixiv、灵感库、历史及全局设置统一使用密度感知的固定高度顶栏，标准密度为 56px；窄工作区和 Agent 展开时不再换行撑高。
- 画师 Tag 顶栏重做为单行命令栏：搜索、排序与抽卡保留为主操作，视图模式、列宽、实装槽位、批量导入、历史与任务日志按频率收进对应弹层。
- 角色 Tag 的列数与抽卡参数合并到页面设置，统计信息移入内容区；AITag 的 Prompt 搜索、风格串的 Tag 筛选改入筛选弹层，避免形成额外工具行。
- 风格串编辑器的名称、描述和标签编辑改为悬浮面板，不再改变顶栏高度；角色参考、Vibe 与 Agent 模型设置等全屏管理页同步采用统一顶栏规格。

### 新增：可自定义实验室模块布局

- 全局设置新增“实验室模块布局”，可通过拖拽或上下箭头调整提示词、角色、参数、负面提示词、角色参考与 Vibe Transfer 的显示顺序，并可一键恢复推荐顺序。
- 每个模块可独立设置进入编辑器时默认展开或收起；推荐配置保持高频模块展开，默认收起角色参考与 Vibe Transfer。
- 桌面端与移动端共用同一份本地布局偏好；损坏、重复或旧版配置会自动修复并补齐未来新增模块，避免内容意外消失。

### 修复：Opus 超额加成显示

- Opus 限额不再把官方返回的百分比截断到 100%；V5 发布活动等加成使额度超过常规上限时，侧栏会直接显示真实百分比和对应剩余张数。
- 超过 100% 时圆环保持满圈而不会反向绘制，并补充 196% 额度及张数换算的回归测试。

### 优化：生成按钮视觉层级

- 桌面生成按钮收窄为居中的紧凑操作，静止时使用低强调度主题浅色，悬停与生成时再提升视觉反馈，避免与作品争夺视觉中心。
- 使用明确的图片图标和“生成图片”文案，配合独立费用标签，并统一错误提示、排队状态与按钮所在操作区的宽度和节奏；浅色与深色模式均跟随各自主题强调色。

### 优化：生成操作移至预览下方

- 桌面右侧调整为“图片在上、操作在下”，生成按钮、排队状态和错误提示统一固定在预览下方，不再抢占作品顶部视觉位置。
- 图片继续自适应占满剩余空间，底部操作区不覆盖图片，状态切换时保持位置稳定。

### 优化：额度侧栏与提示词编辑布局

- 统一 Anlas 与 Opus 额度区的字号和字重，折叠侧栏后 Anlas 也会在钻石图标下显示紧凑余额，较大数值自动缩写并保留完整悬停说明。
- 拆分提示词时，主体／变量提示词移动到基础画风正下方；桌面右侧只保留生成、排队、错误状态与图片预览。
- 移动端主体／变量提示词归入“全局”页，不再与角色专属提示词混在“角色”页。

### 新增：可切换提示词编辑模式

- 全局设置新增“拆分风格与主体提示词”开关，默认开启并保留现有的基础画风与主体／变量双输入框。
- 关闭后将两段内容合并到原风格串位置的“全局提示词”输入框，隐藏右侧主体框并让生成按钮与图片预览自然上移。
- NovelAI 图片与 JSON 元数据导入不再逐次弹出拆分选择；开启时自动智能拆分，关闭时完整导入全局提示词。
- 仅切换编辑模式不会破坏原有分段数据；在单字段模式开始编辑后才会将内容正式归并。

### 修复：生成参数排版与重复底栏

- 生成参数改为分两行展示，模型与尺寸优先获得完整宽度，采样器、步数和 Seed 不再被压缩截断。
- 移除桌面编辑器左栏底部重复的状态与生成悬浮栏，生成操作统一保留在右侧预览区；普通风格串的保存入口移至顶部工具栏。

### 新增：NovelAI 元数据智能导入

- 图片或 JSON 导入改为覆盖前预览，可选择“智能拆分”或“保持完整”，并可直接校正基础画风与主体提示词。
- 智能拆分会把高置信度的画师、质量、年代、媒介和渲染词放入基础画风；未知内容和混合权重组保留在主体，避免破坏原提示词语义。
- 按官方 Web 应用同步 NovelAI 图片 Source / 模型哈希映射，V5、V4.5 与 V4 图片导入时自动选择对应 Full / Curated 模型，未来新增精确映射无需改代码。
- 重排生成参数区域：宽屏下模型、尺寸、采样器、步数与 Seed 同行，模型能力提示独立成行，不再撑乱控件网格。

### 新增：NovelAI Stealth PNG 元数据导入

- 支持读取 NovelAI V5 原图写入 Alpha 通道最低位的 `stealth_pngcomp` 元数据，拖入风格串编辑器后可恢复提示词、角色与生成参数。
- 标准 PNG 元数据扩展支持 `tEXt`、`iTXt` 和 `zTXt`，旧版 NovelAI 图片继续优先读取完整的 `Comment`。
- 增加图片容量、压缩长度和解压大小保护，并补充 V5 Stealth、新旧文本块及损坏数据回归测试。

### 修复：切换 Key 后 Opus 限额不再消失

- 切换 Key 后若 NovelAI 订阅请求失败，侧栏会保留 Opus 限额行并显示红色叉号与“同步失败，点击重试”，不再静默隐藏。
- 切换与重试期间保留带旋转动画的同步状态；仅在请求成功且明确不是 Opus 订阅时隐藏限额行。
- 新增切 Key 请求失败、点击重试恢复以及非 Opus 成功响应的回归测试。

### 修复：补齐 Key 隔离与失败生图记账边界

- 修复 NovelAI 生成失败时仍错误累计个人 Opus 免费图张数的问题；失败和取消请求不再写入个人用量。
- Opus 受限模型判断统一使用官方运行时同步得到的模型清单，未来新增受限模型无需依赖 V5 字符串前缀。
- 新增切换 Key 后忽略迟到预算事件、失败生成和未来受限模型的回归测试。

### 修复：恢复个人使用统计记账

- 修复 NAI5 免费生成的 Opus 张数增量字段错位，成功生成后会按当前 Key 正确累计。
- 生图完成后设置页立即刷新当前 Key 的个人 Anlas 与 Opus 统计，不再必须重新打开页面。
- 校正本机“困困的小群福利”对应的历史 NAI5 生成记录，补记 1 张 Opus 免费图。

### 修复：统一生成按钮与公共队列状态

- 公共队列的加入、排队、等待和生成中状态改为跟随当前主题的同色系渐变，不再固定使用绿色和紫色。
- 队列背景流动、扫光和旋转等待圈仅在完整动效设置下运行，减少或关闭动效时保持静态。
- 成功、取消和失败状态分别保留成功色、主题中性色和错误红色，移动端阴影与桌面端统一。
- 清理遗留生成入口的固定靛蓝样式，确保各生成入口共享同一套空闲/生成中视觉状态。

### 修复：仅在生成中显示粒子渐变

- 普通状态恢复为主题纯色按钮，只有进入生成中状态时才显示同色系渐变、光斑和左右往返动画。

### 优化：主题化生成按钮流光动效

- 生成按钮改为跟随当前强调色的同色系多段渐变，并叠加柔和光斑形成粒子感。
- 渐变与光斑在完整动效模式下左右往返，生成中加快；桌面预览和移动端按钮统一视觉，减少/关闭动效时保持静态。

## 2026-08-22

### 修复：V5 生成按钮的额度提示

- NAI 5 / V5 Curated 在 Opus 免费档时，生成按钮显示「消耗额度」，不再误标为「免费」。
- V4.5 及以下继续显示「免费」；超出免费档并产生 Anlas 费用时显示具体点数，预览区、移动端和 Agent 确认文案保持一致。

### 修复：Opus 限额刷新与错误状态动效

- 点击 Opus 限额时显示旋转刷新弧，刷新期间保留当前额度并标记忙碌状态。
- 官方常量同步失败时改用红色圆环和居中的「×」表示错误，移除不明确的琥珀色小圆点。
- 额度百分比变化继续通过圆环过渡动画表现，消耗额度时不会瞬间跳变。

### 修复：稳定官方常量同步

- 官方 Web 应用 JS 分块请求增加重试，临时网络抖动不会再直接造成 `freeTier`、`models` 等项目部分未命中。
- 部分提取结果不再覆盖上一次运行时常量，并改为 5 分钟后自动重试；完整同步成功后恢复每日同步。
- 前端运行时常量改为无缓存定期刷新，网关稍后同步成功后当前页面也会自动更新，不再永久显示旧的部分失效记录。

### 修复：切换密钥时同步刷新隔离状态

- Opus 限额刷新按 Key 合并并发请求、强制禁用缓存，切换后不会再被旧响应覆盖。
- 公共队列状态绑定发起任务的 Key，切换密钥时立即清理旧任务提示，并忽略旧任务迟到的状态回调。
- Anlas 预算变更事件携带 Key 指纹，旧密钥的保存或生图响应不会覆盖当前密钥的预算显示。
- 新增 Opus 刷新去重与公共队列切 Key 隔离回归测试。

### 修复：调整密钥状态与 Opus 限额布局

- 密钥列表将「使用中」放回操作区，与「备注」「删除」并列；当前密钥显示禁用的「使用中」，其他密钥显示可点击的「使用」。
- Opus 限额移除加油站图标、恢复说明和省略号，改为百分比圆环配合两行简洁信息：上方「Opus 限额」、下方估算图片张数。

### 新功能：按密钥隔离队列、预算与用量

- 公共队列设置、排队任务状态与取消权限按当前 NovelAI API Key 独立保存和校验，旧版单份队列设置会在首次使用时迁移到当前密钥。
- Anlas 本地预算改为按密钥独立计算，个人统计接口只返回当前密钥的数据；电脑与手机切换密钥时不会沿用上一把 Key 的预算或 Opus 状态。

### 优化：密钥、Opus 限额与模型筛选

- 密钥保管箱明确标记当前项为「使用中」；Opus 限额改为按剩余百分比变化的完整圆环，并优化低额度、透支和同步异常的展示。
- 风格串模型筛选收进与「最近更新」一致的下拉框，过滤运行时带出的 Furry、短数字和 inpainting 等遗留标识，仅保留当前可用模型及规范的未来版本。

### 新功能：启动时自动开启安全模式设置

- 全局设置 → 界面与内容显示新增“启动时自动开启安全模式”开关，默认开启；开启后每次重新打开项目都会自动进入安全模式。
- 关闭后项目启动时保持关闭，当前会话仍可通过侧栏或设置手动切换；Agent 的界面偏好工具同步支持该设置。

### 修复：密钥保管箱防误填与统计口径说明

- 「添加密钥」表单加固：密码框标记 new-password 阻止浏览器把其他服务的密码自动填入；新增 pst- 前缀校验，误粘其他服务密钥（如 LLM Key）会直接拒绝并提示；保管箱中已有的非 NovelAI 格式条目显示「格式可疑」标记，便于清理。
- 个人使用统计空状态改为区分「未配置密钥」与「已配置但无计费记录」，并说明口径：只有扣 Anlas 的生成计入个人 Anlas、只有 V5 等受限模型的免费档计入 Opus 张数，V4.5 免费小图不消耗共享资源不计入。

### 新功能：个人用量统计与预算警告

- 全局设置 → NovelAI 新增「个人使用统计（当前密钥）」：本机在当前账号累计花费的 Anlas 与计入 Opus 免费额度的生成张数（按张数折算百分比），按密钥分账号记录，切 Key 不串账；支持一键重置。工作界面侧栏保持不变——实时 Opus 限额（账号整体）+ Anlas 预算倒数。
- 统计口径：仅 V5 等受限模型的免费档生成计入 Opus 张数（透支或超出门槛的按 Anlas 计费不计张数）；生图与永久 Vibe 编码的费用计入个人 Anlas。
- 新增预算警告：本地 Anlas 预算已扣到 0 但本次生成仍需消耗时，弹出红色警告确认框（Agent 代生成同样拦截）；预算未用尽但不足以覆盖本次消耗时，在原确认弹窗追加提示。

### 新功能：NovelAI 多密钥保管箱

- 全局设置 → NovelAI 支持保存多把 API Key，每把可命名备注（如“车队 A / 备用号”）；点击「使用」即切换当前密钥，生图、Opus 限额、网关代理全部即时跟随，无需刷新页面。
- 密钥列表脱敏展示（pst-…XXXX），支持修改备注与删除（带确认）；首次打开自动把现有单密钥收编为「默认密钥」，原「记住 API Key」语义不变（控制当前密钥是否跨会话保留）。

### 性能：本地启动提速与阶段耗时显示

- 本地启动（npm run dev:local）触发的构建改用快速通道（vite + esbuild，跳过 tsc 类型检查），构建耗时约缩短三分之一；完整类型检查仍由编辑器与提交前钩子把关。
- 官方常量同步改为网关就绪 30 秒后再执行，不再挤占启动期网络。
- 黑窗口启动日志显示各阶段耗时：构建耗时、核心页面服务（含本地 D1/R2 存储恢复）耗时、图片网关耗时与总耗时，等待时能看清进度。

### 修复：风格串模型筛选改为常驻

- 模型筛选条不再依赖列表内容：无论有没有风格串、有几种模型，桌面筛选条与手机筛选面板始终显示，选项固定为可选模型清单（含网关同步到的新模型），筛选中无匹配时列表正常显示“暂无数据”。

### 修复：AITag 模型筛选归并与首图缓存重试

- AITag 模型筛选项按系列归并：去掉元数据中的版本哈希后缀（如 "NovelAI Diffusion V4.5 1229B44F" 归并为 "NovelAI Diffusion V4.5"），不再随加载出现一堆重复选项；详情面板的模型名同步清理。
- 修复首图缓存一旦遇到上游 502 就永久放弃的问题：错误状态的作品会在后续加载时自动重试；批量缓存增加连续失败熔断（连续 2 次失败即暂停本轮），避免 aitag.win 被挑战/限流时继续轰炸并刷屏错误日志。

### 新功能：模型版本标签与筛选

- 风格串列表新增「模型筛选」行（桌面标签条 + 手机筛选面板），按各串生成参数中的模型版本过滤；未设置模型的旧串自动归入 V4.5 Full，卡片标题旁以紫色徽标显示模型版本。
- AITag 筛选面板新增「模型版本」下拉（桌面与手机），按首图 NovelAI 元数据中的模型过滤已加载条目，与收藏筛选同机制。
- 历史图片详情的参数面板首行新增 Model 项，一眼区分每张图用的模型；旧历史记录（未记录模型时期生成）显示 V4.5 Full。

### 维护：「画师串」更名为「风格串」

- 决定图片样子的不只是画师 Tag，页面与全部相关文案统一从「画师串」改为「风格串」（侧栏、列表页、编辑器、Agent 工具描述、README 等）；历史文档记录保留原名。

### 新功能：常量同步失效时生成前强制警告

- 官方常量同步失效（提取全灭或超过 48 小时未更新）时，点生成不再静默按可能过期的规则计费：估算为免费的生成会弹出红色警告确认（说明失效原因与风险）后才继续；估算需扣 Anlas 的生成在原确认弹窗中追加警示。Agent 代生成路径同样拦截。
- 采用警告确认而非直接禁止：同步与生成走不同的官方域名，可能一通一断，由用户确认后仍可继续生成。

### 新功能：常量同步健康监控与自检

- 官方常量自动同步现在记录健康状态：每次同步逐项标记命中/未命中并持久化，页面抓取失败或提取全部失效会在网关日志告警；侧栏 Opus 限额行出现琥珀色圆点并附详细原因（同步失效或超过 48 小时未更新），杜绝"项目能跑但常量早已过期"的静默失效。
- 新增联网自检命令 `npm run test:live-sync`：对真实官方 Web 应用逐项验证提取器，任何一项未命中即失败，可随时手动确认同步链路健康。
- AGENTS.md 划定常量同步保护区：后续任何 AI 修改相关文件必须通过单元测试与联网自检，禁止删除或放宽相关测试。

### 新功能：官方规则常量自动同步

- NovelAI 的模型清单、Opus 限额换算系数、免费档门槛（面积/步数）与 Anlas 成本公式系数官方均未提供查询接口，本地网关现会每天自动从官方 Web 应用提取这些常量并缓存到 local-data，官方调整后项目无需改代码即可跟随。
- 生成模型的下拉列表自动追加官方新模型（如未来 V6），费用确认与本地预算扣减使用同步后的门槛与系数；提取失败时回退内置默认值，功能不受影响。
- 导入元数据中的未知模型标识生成时原样透传给 NovelAI，由服务端裁决，不再强制回落到 V4.5 Full。

### 修复：Opus 限额查询接口域名

- NovelAI 的订阅接口对第三方工具只开放 image.novelai.net（api 域名返回 400），网关代理已改用正确域名，限额组件恢复真实数据。

### 新功能：Opus 限额适配拼车共享账号

- Opus 限额为全账号共享：其他成员生图同样消耗额度，侧栏限额组件改为每分钟自动同步服务端真实状态，并支持点击立即刷新。
- 使用 V5 等受限额模型生成前，会先刷新真实额度再计算费用并弹出确认：他人刚把额度耗尽或透支时，不会再用“免费”误判导致意外扣共享 Anlas；本地预算扣减在透支快照过期时也会向 NovelAI 重取真实状态。

### 新功能：Opus 免费生成限额显示（随 V5 引入）

- 侧栏 Anlas 预算下方新增「Opus 限额」独立组件：实时显示 NovelAI Opus 免费生成额度剩余百分比、约剩张数与每日恢复速度，附进度条；透支后变红并提示所有生图将消耗 Anlas。非 Opus 订阅或未配置 API Key 时不显示，与官方一致。
- 额度映射与 NovelAI 官方 Web 应用逐行对齐（透支显示 0、百分比钳制 0–100、剩余张数 17.3 系数、每日恢复 86400/恢复秒数）并有单元测试锁定；数据经本地网关代理 NovelAI 订阅接口获取，自动剥离支付等敏感字段，生图完成后与每 5 分钟自动刷新。
- 生图费用估算同步官方规则：Opus 限额透支后，V5 的小图步数免费生成改为按 Anlas 计费（V4.5 及以下不受影响），本地预算扣减与生成前费用确认弹窗均按此计算。

### 新功能：生成模型可选（支持 NovelAI V5）

- 生图实验室参数面板新增「生成模型」选择器，可在 V5 Full / V5 Curated / V4.5 Full（默认）/ V4.5 Curated / V4 Full / V4 Curated 之间切换；模型标识与官方 2026-08-22 版 Web 应用注册表逐一核对。
- V5 沿用 v4_prompt 结构与 params_version 3，现有参数（多角色、Variety+、CFG Rescale 等）无需改动即可在 V5 上使用；Anlas 成本公式 V4/V4.5/V5 相同，费用估算保持准确。
- 按官方边界做能力提示与拦截：V5 暂不支持 Vibe Transfer 与角色参考（选择后参数面板、Vibe 面板、角色参考面板均给出提示，生成前会明确报错）；导入 NovelAI 图片元数据时自动沿用其中的模型标识。
- 旧画师串未设置模型时行为不变，仍按 V4.5 Full 生成。

### 维护：新增 AI 工作日志

- 新增根目录 `AI_WORKLOG.md`，并在 AI 协作规则中强制要求：任何 AI 工具每次修改项目后，登记模型名、日期与所做工作，且与改动进同一个提交，便于追溯多个 AI 协作留下的修改。

## 2026-08-20

### 新功能：AITag 固定为 NovelAI 来源

- AITag 页面、缓存状态、本地索引与项目 Agent 统一固定检索 NovelAI 来源，不再提供 Stable Diffusion、ComfyUI 或“全部类型”分类，避免混入不需要的生成体系。
- AITag 桌面筛选改为以筛选按钮为锚点在按钮正下方展开，保留缓存、排序和月份选项；手机筛选同步移除类型选项并明确搜索 NovelAI 元数据。
- 同步审计工具栏浮层，灵感筛选和角色抽卡设置也改为锚定各自触发按钮，避免面板跟随整条工具栏右侧定位而与按钮脱节。

### 新功能：过滤 AITag 无效短元数据

- AITag 图片导入、保存画师串与加入灵感时，会按最终有效元数据的 UTF-8 字节数检查内容；少于 30 字节的 Emoji 或其他短占位内容直接视为无可用元数据。
- 作品页共享工具栏统一建立高于图库卡片的显示层级，修复历史管理菜单被图片遮挡的问题，并同步避免角色抽卡设置、AITag 筛选等浮层出现同类遮挡。

### 新功能：作品名称遮挡可独立设置

- 全局设置的安全模式下方新增“同时隐藏作品名称”开关，默认保持开启；关闭后安全模式只遮挡图片，作品名称与标题输入框正常显示。
- 名称遮挡开启时继续保留原有交互：点击名称只显示名称，点击图片同时显示图片及对应名称，并沿用离开后自动恢复规则。
- 修复设计主题预览在白天模式仍显示黑色侧栏的问题，缩略预览现在会随明暗模式切换配色。

### 修复：安全模式覆盖与 AITag 连接

- 画师 Tag（网格与展开列表）、角色 Tag 和画师串卡片现在完整隐藏名称；点击名称只显示名称，点击封面则同时显示封面与对应名称。
- 安全模式图片遮挡会跟随明暗主题：亮色主题改为明亮低饱和模糊，暗色主题继续使用深色模糊，切换主题时即时同步。
- AITag 本机代理改用系统网络传输访问 aitag.win，避开 Node 请求被 Cloudflare 挑战页拦截导致的 403 与“连接失败”；远端 Worker 请求头同步更新。

### 新功能：安全模式同步隐藏作品名称

- 安全模式现在同时遮挡 AITag、Pixiv、Danbooru 与灵感库中的作品名称，避免仅遮住图片时仍从标题暴露私人浏览内容。
- 点击作品名称只临时显示该名称；点击作品图片则同时显示当前图片及其对应名称，不会解锁同页其他作品。
- 名称与图片沿用安全模式的自动恢复规则：鼠标移出、点击外部、切页、窗口失焦、进入后台或按 Esc 后重新遮挡。

### 新功能：公共队列服务地址可自行配置

- NovelAI 设置新增公共队列服务地址输入框，默认填入现有的 st-chatu8 兼容服务地址，也可切换到用户自行部署的兼容服务。
- 既有的启用状态、排队个性语与显示选项保持不变；旧配置缺少地址字段时自动补入当前服务，不会清空或覆盖原有设置。
- 网关统一校验服务地址，并让每个正在执行的排队任务固定使用加入时的地址，避免任务途中修改设置导致完成或退出请求发往另一台服务。

### 文档：明确独立维护与原项目来源

- README 明确 NAI Atelier 基于 kirafishy/NaiPromptManager 二次开发并独立维护，保留原项目 README 的 MIT 许可声明与来源说明。

### 新功能：NAI Atelier 设计主题与个性化系统

- 将原有界面正式整理为首套“NAI Atelier”设计语言；主题与白天、黑夜、跟随系统三种明暗模式彼此独立配合，既保留现有布局和功能位置，也统一全站色阶、表面、边框、阴影、排版与交互反馈。
- 外观设置新增实时个性化：可选择预设或任意自定义强调色，并调节界面密度、圆角语言、实色/透光表面、字号与动效；设置保存在本机，可一键恢复主题默认值。
- 主题实现改为可扩展的语义令牌和偏好模型，为后续增加完全不同的前端设计语言预留统一入口。

### 修复：Tag 词库检查在手机和本机恢复可用

- 词库状态与更新改为经由当前 NAI Atelier 本地服务转发，不再让浏览器直接访问自身的 `127.0.0.1:3002`。
- 保持词库更新服务仅监听电脑本机；局域网访问仍须通过本地服务的访问验证。

### 优化：设置内容层级与词库操作布局

- 移除各设置内容区与顶部栏、桌面侧栏重复的页面标题和说明；桌面依赖侧栏高亮定位，手机依赖固定顶部标题与返回入口，内容直接从实际设置项开始。
- Tag 补全词库改为紧凑横向卡片：说明在左、词库来源和更新操作在右；窄屏自动纵向排列，避免操作区被压缩或留下无意义空白。
- 本地服务状态的刷新操作移至对应分组标题，避免脱离语境地占据页面顶部。

### 新功能：设置收敛为创作与维护分区

- 全局设置精简为“界面与内容显示、NovelAI 与 Anlas、项目 Agent、数据与维护”四个分类；Anlas 预算随 NovelAI 连接与队列设置归并，关于信息不再占用独立页面。
- 数据与维护集中 Tag 词库、手机小图缓存、电脑缩略图缓存、重要数据备份说明与本地服务状态。电脑缓存容量和数量直接读取媒体网关实际索引，状态可手动刷新。
- “关于 NAI Atelier”增加一键打开个人 GitHub 仓库的入口；明确 GitHub 只保存代码，重要个人数据须单独完整备份 `local-data`。

### 优化：全局设置按分类拆分为独立子页

- 外观与隐私、NovelAI、项目 Agent、Anlas、Tag 词库、手机缓存和关于分别在独立内容页中打开，不再在同一张长页面中堆叠。
- 桌面端左侧分类导航直接切换当前子页；手机端因侧栏隐藏，先显示分类选择列表，进入子页后可返回列表。既有“直接打开 Agent 设置”等应用内事件仍会落到对应分类，所有设置项的即时保存、确认操作和数据结构保持不变。

### 修复：侧边栏沿用的旧产品名

- 侧边栏顶部原作者遗留的“咒语构建终端”已统一为正式英文名 NAI Atelier，不再与浏览器标题、设置和文档的品牌名称冲突。

### 维护：正式名称确定为 NAI Atelier

- `NAI` 作为 NovelAI 的常用简称统一使用全大写，`Atelier` 与其以空格分隔，正式名称定为 NAI Atelier；GitHub 仓库与 npm 包标识同步使用 `nai-atelier`。
- 界面、浏览器标题、启动器、项目元数据、网络 User-Agent、Agent 身份与 README 全部采用新名称；桌面快捷方式同步更名。上游来源说明、MIT 原作者 kirafishy 的版权署名、现有本地目录、Cloudflare 项目名与 st-chatu8 桥接标识保持不变。

### 维护：产品更名为 NaiStudio

- 产品对外名称、浏览器标题、局域网与 Pixiv 提示、启动日志、Agent 身份和项目元数据统一更名为 NaiStudio，以准确体现提示词、角色、画师、图库、灵感与创作历史组成的完整本地工作台。
- README 明确保留基于 kirafishy/NaiPromptManager 二次开发的来源说明；MIT 许可证继续保留原作者 kirafishy 的版权署名。现有本地目录、Cloudflare 项目名与 st-chatu8 桥接标识保持不变，避免影响已有安装、部署和同步数据。

## 2026-08-16

### 修复：平板区间按返回键直接退出页面而不是关闭详情层

- 历史返回层此前只在 <768px（手机）注册，而图库详情在 <1280px 都是全屏覆盖、历史页灯箱与灵感详情的模态在平板上同样存在——768~1279px 打开详情后按系统/浏览器返回键会直接离开整个页面。注册区间改为与覆盖层存在区间一致的 <1280px，桌面侧栏形态（≥1280px）不受影响。

### 文档：LICENSE 补回原作者版权署名

- MIT 要求再分发时保留原项目的版权声明；本仓库 fork 自 kirafishy/NaiPromptManager，版权行改为 "Copyright (c) 2026 kirafishy and NaiPromptManager contributors"，为独立开源做好许可合规。（顺带：prompt-agent 的会话原子写入在 Windows 上遇到与 pixiv 令牌同款的偶发 EPERM，补上同样的退避重试，消除提交门禁的间歇性拦截。）

### 优化：worker 灵感接口——schema 自愈进程级只跑一次、批量操作单往返

- `ensureInspirationSchema` 此前在每个灵感请求前都执行约 20 条 DDL（1 建表 + 16 补列 + 3 索引），本地长期运行的服务重复执行纯属浪费；现在进程内只跑一次（DDL 本身幂等，冷启动仍会自愈）。
- 批量更新/删除改为一次构建 SET 子句 + `db.batch` 单往返（此前是 N 次串行 UPDATE / N 次 SELECT+DELETE）；批量删除的归属查询改为分块 IN（D1 单查询参数上限 90 检查），资产清理逻辑不变。字段构建抽为共享的 buildInspirationSetStatements，单条更新与批量更新同源。

### 优化：灵感页搜索防抖与派生计算 memo

- 搜索框此前每击键一次就对全量 items 做一次含 prompt/notes 大文本拼接的过滤重算，现在 250ms 防抖；侧栏来源计数从"每次渲染 × 每来源遍历全表"改为一次 useMemo 聚合；每张卡片的板名查找从 O(boards) 的 find 改为预构建的 Map。大库（几百上千条）下筛选与勾选操作的重渲染成本显著下降。

### 优化：灵感卡片缩略图管道修真 + 加载策略修正

- 排查发现灵感卡片的"thumb-320"完全空转：`/api/inspirations/{id}/image` 不在客户端 canUseMediaGateway、网关 getValidatedSource、worker MEDIA_INTERNAL_SOURCE 三处内部路径白名单里，历史收藏类灵感的卡片一直在直拉 R2 完整原图。现在三处白名单同步放行（附回归测试），卡片真正走网关缩略图，详情页仍看原图。
- 卡片去掉 `eager`：此前所有过滤命中的卡片挂载即并发请求，SmartImage 的懒激活与两屏预取被短路；现在与其它图库一致。
- 列表容器补上遗漏的 `workspace-card-grid`（content-visibility），视口外卡片不再立即布局绘制——其它六个图库都有，唯独灵感库漏了。

### 重构：图库详情页设计语言统一（共享 DetailSidePanel 组件族）

- 此前五个页面的作品详情各有一套：断点分裂（Pixiv/Danbooru 在 xl 切侧栏、AITag 在 md 就切——768~1280px 平板区间出现"列表+详情上下堆叠"的畸形中间态、历史页/灵感详情又是另外两套）、按钮三套语言（共享 ToolbarButton / 手写原生 button / AITag 手写 40×40 彩色实心钮）、头部与图片容器高度单位（h-14 vs 自适应、62vh vs 固定 520px）各自为政。
- 新增共享组件族 `components/DetailPanel.tsx`：**DetailSidePanel**（统一"移动全屏覆盖 → xl 右侧栏"骨架 + h-14 头部 + 返回/关闭，兼容 agent 停靠与滚动位置缓存）、**DetailImageStage**（统一圆角 2xl 容器 + 可选多页翻页控件）、**TagChip/TagChipGroup**（统一可点击标签 chip）。
- 四个图库全部迁移：Pixiv/Danbooru 面板与头部换用共享件（Danbooru 的手写返回/关闭/原帖链接退回 Toolbar 系）；AITag 修复平板中间态（统一为 xl 侧栏 + 移动端隐藏列表，与兄弟页面一致），手写彩色按钮换为 IconButton 的语义色调（画师串/灵感/实验室），外链药丸升级为 ToolbarLink，图片高度 520px→62vh；历史页灯箱保留模态范式，操作按钮（导入/加入/下载/删除）统一为 ToolbarButton/ToolbarLink。Pixiv 翻页与 AITag 多图纵向堆叠保留为两种合法的多页模式。灵感详情是编辑器型模态、范式不同，本次不动。

### 修复：打开图库详情时背景列表整片重新加载

- 原因：SmartImage 按实测像素宽动态选缩略图档位（160~960），而详情面板打开会把网格从单栏挤成 `1fr+460px` 双栏（移动端更是整栏隐藏、宽度归零）——每张卡片的档位随之跳变（如 640→480），缓存键全部失效，整列表重新请求并闪占位；关闭详情又跳回来再重载一遍。Pixiv、Danbooru、AITag 三个图库共用这套布局与组件，机制上都会发生。修复：给档位加"棘轮"（每张图只升不降）——临时挤压/隐藏不再触发降档，缓存键保持稳定；真实的窗口/布局放大仍会升档。新增 ratchetVariantWidth 纯函数与 5 个单元测试覆盖挤压、归零、恢复、升档序列。

### 优化：Pixiv 卡片封面改为 medium 源、原图只在校验详情时加载

- 排查发现 Pixiv 封面慢的两个叠加原因：其一，卡片缩略图源用的是 `large`（约 1200px master，单张 0.3-1.5MB），经网关缩成 320px 封面，上游流量是所需的 5-10 倍；其二（更重），卡片 SmartImage 带了 `upgradeSrc=original` 且升级是"进入视口即触发"——**每张滚到的卡片都在后台拉完整原图（2-8MB）**，与封面缩略图抢网关并发槽。现在卡片源改为 `medium`（540px 真实比例，320px 显示下仍是缩小显示不糊，仅 2x 高分屏全宽时略低于 640 理想值、肉眼无感），并移除卡片原图升级——原图只在点开详情后加载（详情页本就先出 large 预览再升原图，行为不变）。网关 feed 预热的取源顺序同步改为 medium 优先，保证预热缓存键与卡片请求一致。单页 30 张封面的上游流量从 70MB+（30 大图+原图）降到约 4-6MB，缩略图不再与后台原图抢带宽。Danbooru/AITag/历史页卡片本就没有原图升级，不受影响。

### 优化：Pixiv 图库下一页 feed 投机预取（JSON 与缩略图一起备好）

- Pixiv 是游标分页：此前触底"加载更多"才开始请求下一页 feed（含上游往返），再等缩略图。现在当前页返回后立即用 nextCursor 后台预取下一页——网关对 feed 响应本就自动预热缩略图，预取等于把 JSON 和图片一并提前备好；同模式同游标触底时直接消费，预取失败自动回退。推荐流/榜单/搜索/画师页四种模式均生效，Pixiv 上游往返（最长的一段等待）从滚动路径上移走。

### 优化：Danbooru / AITag 图库下一页投机预取

- 此前连续滚动的链路是"哨兵触底 → 请求下一页 JSON（0.5-2s）→ 预热/抓取图片 → 渲染"，JSON 往返卡在滚动路径上。现在当前页稳定后立即后台预取下一页：Danbooru 同时把预取页的缩略图提交预热，AITag 按当前筛选（在线/本地缓存）走对应接口；触底追加时同签名同页直接消费预取结果，预取失败自动回退正常请求。用户滚到底时内容与图片基本就位，追加等待从"JSON+图片"缩短为"渲染"。

### 优化：画师/角色封面候选枚举从严格串行改为 3 并发

- Danbooru 封面候选此前按 300ms 严格串行枚举（每 Tag 一次 posts API 请求）：首屏预取 40 个画师/角色封面要 12 秒才枚举完，封面图才开始预热。改为最多 3 并发 + 100ms 启动间隔（有效速率约 6/s，低于 Danbooru 匿名接口 ~10/s 限制），遇 429 整体退避 2 秒。14 天本地缓存不变，冷缓存首屏枚举时间约缩短一半以上。

### 优化：网关缩略图并发提档（12/8/4/2 → 16/12/8/4，预热 4 → 6）

- 远端图单张抓取 0.8-3s 是纯网络等待，sharp 缩放每张仅几十毫秒——原档位按 CPU/内存保守标定，卡错了瓶颈资源：中档机器 4 并发填满一页 40 张要 10s+。新档位仍按机器级别约束 CPU，但允许更高的并行抓取；后台预热并发 4 → 6。同机器下图库首屏与追加页的填图速度约提升一倍。

### 重构：JSON 元数据提取纯函数从 ChainEditor 抽到 metadataService（首片拆分）

- `extractRawMetadataFromJsonText`（Comment 对象/字符串、生成参数 JSON、Description、纯文本五级回退）从 2196 行的 ChainEditor 组件迁到 services/metadataService.ts，与 parseNovelAIMetadata 同处一层，并补 7 个回退分支的单元测试。这是巨石组件拆分计划的第一片：先抽纯逻辑、可测试的部分。

### 加固：R2 key 净化与 LAN 限流 IP 信任顺序

- `/api/upload` 的 folder 与扩展名、外链转存的资产 id 此前都直接拼进 R2 key，客户端可注入 `/`、`..` 等字符写出任意前缀的 key（如伪装成 `local-history/` 路径样式）。现在统一净化为单段安全字符（字母数字与 `_-`）。
- LAN 密码 5 次锁定用的 IP 主键改为 `CF-Connecting-IP` 优先（Cloudflare 边缘写入、不可伪造），`X-Nai-Client-IP` 退居其次——本地形态下它由网关从 socket 覆写仍然可信，但公网部署形态下客户端可伪造并轮换取值绕过锁定。本项目声明只跑局域网，此项为部署形态变化时的兜底。

### 重构：worker 与本机网关的图片白名单/LAN cookie 收敛为单一来源

- 远程图主机白名单与 LAN 访问 cookie 名此前在 worker（TS/WebCrypto）与 media-gateway（Node/node:crypto）各维护一份，"改一漏一"会直接断图或断鉴权。现在两边都从 `worker/sharedWhitelist.mjs` 导入（沿用 imageDimensions.mjs 的跨运行时共享先例）；网关刻意多出的 i.pximg.net 与 443 端口校验保持不变，注释明确标注这是有意不对称。

### 重构：删除 worker 中个人模式下不可达的多用户死代码（-840 行）

- bcrypt 登录体系（/api/auth/login、guest-login、logout、第二处 auth/me、getSessionUser 会话中间件）、/api/users 全套 CRUD（建号/改密/配额/角色，位于 410 禁用门之后永不可达）、initDB 里的 admin_996 / nai_guest_123 默认弱口令播种——全部删除，`bcryptjs` 依赖随之移除，worker 打包体积 251.6KB → 195.1KB。这些代码此前给维护者"存在账号体系"的错觉，且默认弱口令在关闭个人模式开关后会复活。会话/用户表保留在 schema 中以兼容既有数据库，不做迁移。

### 优化：keep-alive 页面改为最近使用保留 4 个，内存不再单调增长

- 9 个 keep-alive 视图全部是重型图库，此前只挂载不卸载：访问过的每个页面都常驻内存（隐藏挂载），长时间使用内存只升不降。现在按最近使用排序、最多保留 4 个，超出淘汰最久未访问的；正在展示的视图始终在队尾不会被淘汰，被淘汰的视图再次访问时按原逻辑重新挂载并恢复数据。排查中确认 `localHistory.prepare` 内部已有 single-flight 去重，历史迁移的并发风险实际不存在，未做多余改动。

### 修复：Pixiv 令牌原子写入在 Windows 偶发 EPERM

- `writeAtomic` 的 rename 在 Windows 上会被杀毒/索引器短暂锁定刚写入的临时文件而偶发 EPERM/EBUSY（曾导致单测间歇性失败、拦截提交）。现在带 25ms 递增退避重试最多 5 次，仍失败才抛出。

### 重构：worker 图片来源校验抽为可测模块并补 8 组用例

- `validateMediaSource` 与 MEDIA_VARIANTS/REMOTE_HOSTS/INTERNAL_SOURCE 常量从 5000 行的 worker/index.ts 抽到独立 `worker/mediaValidation.ts`（esbuild 打包验证通过），新增 10 个单元测试覆盖：内部路径白名单（assets/local-history/vibes/character-references/st-chatu8）、远程图 https+主机白名单+凭据拒绝、大小写归一、空值/超长/换行/垃圾输入。这是 worker 侧第一批单元测试。

### 修复：Danbooru 图库搜索竞态守卫（抽出共享 useStaleGuard hook）

- `DanbooruGallery.load` 是四个图库中唯一没有竞态守卫的加载路径：快速连续搜索/跳页时较慢的旧响应会覆盖新结果。把此前为 AitagGallery 写的守卫模式抽成共享 `useStaleGuard` hook（begin 取序号 / isCurrent 判有效），两个图库统一接入，并配 renderHook 回归测试（新加载使旧序号失效、卸载后全部失效）。引入 @testing-library/react + jsdom 为后续组件测试铺路。

### 工程化：引入 vitest 并补 services 层单元测试

- 新增 vitest（node 环境纯逻辑测试）与 `npm test` 命令，pre-commit 钩子同步纳入。首批 27 个用例覆盖 services 层纯逻辑：灵感标签工具（含 sourceLabel 的 pixiv 回归）、Prompt 编译顺序（基础→前置模块→主体→后置模块、激活开关、逗号清理）、AITag 元数据解析（JSON 数组容错、图片地址组装优先级）。前端与 TS 侧此前零测试设施。

### 重构：删除 worker 中的 no-op 审计日志体系（-597 行）

- `writeSystemLog`/`incrementDailyStat`/`logAccess` 函数体开头即 return（多用户时代的审计日志在个人模式被有意禁用），但 43 个调用点仍在每次请求时 eagerly 构造 metadata 对象——纯浪费且严重误导维护者（看起来有审计，实际什么都不记）。本次删除全部调用点与函数定义及其专属助手（truncateLogString/sanitizeLogValue/stringifyLogMetadata/getClientIp/isMissingLogSchemaError/LOG_STRING_LIMIT），行为完全不变。

### 修复：pinned 封面缓存无总量上限，超限后反噬普通缩略图缓存

- 固定封面（画师/角色 Tag 封面 pin 标记）此前不设上限：pinned 累计超过 1GB 后，LRU 淘汰的 900MB 目标永不达成，每轮都会清空全部普通缩略图（图库滚动体验退化），磁盘占用仍持续增长。现在 pinned 设 256MB 上限：超过时先按"最久未访问"淘汰固定封面到 200MB 以内再走常规淘汰，并输出告警日志。正常画师/角色数量级（每张数十 KB）远达不到该上限，行为不变。

### 工程化：schema.sql 与运行时真源对齐

- 旧 schema.sql 已严重脱节（缺 base_prompt/subject_prompt/modules 等 6 列、缺 settings/vibe/aitag 等多张表）且开头是具破坏性的 `DROP TABLE`、无任何执行管道引用——照它建库必翻车。重写为 worker 内嵌 INIT_SQL 的镜像快照：12 张表全部 `CREATE TABLE IF NOT EXISTS`，附 initDB 自愈补列清单与延迟建表说明，头部明确"运行时真源在 worker、此文件仅作人读参考"。

### 工程化：pre-commit 质量门禁钩子

- 新增 `scripts/install-git-hooks.mjs`（经 npm `prepare` 在 install 时自动装配）：每次 git commit 前自动跑 eslint（0 错误才放行）、`tsc -b` 类型检查与单元测试，任一失败即阻止提交——此前所有检查全靠手跑，坏代码可以畅通进入 main。

### 工程化：引入 ESLint 质量门禁

- 新增 ESLint 9 扁平配置（typescript-eslint 推荐 + react-hooks 规则）与 `npm run lint` 命令：**0 错误**，327 个警告（约 287 处 any、27 处 hooks 依赖告警）作为后续逐步收敛的清单。规则取向：只开正确性检查、不管风格——与本库 `cond && effect()` 惯用写法和不用的对齐参数兼容。顺手清理：`.wrangler` 构建缓存纳入忽略、4 处 prefer-const、1 处 @ts-ignore 改 @ts-expect-error、历史页缓存请求的自引用 let 改 const。

### 工程化：TypeScript 开启 strict 模式

- 全项目类型检查从宽松切换到 `strict: true`。迁移中共修复 19 处类型错误，其中包含一个真实潜伏 bug：灵感来源标签映射表缺少 `pixiv` 键（Pixiv 来源的灵感标签会显示为 undefined）。其余为索引类型声明错误、死默认值、可空参数收紧与隐式 any 标注。

### 修复：worker 兜底错误透传与库内 JSON 列裸解析

- 兜底 catch 此前把 `e.message` 原样返回客户端，D1/R2 内部报错细节（SQL 片段、内部路径）会直接暴露；现在完整异常进服务端 console.error，客户端统一收到简短文案。带业务语义的校验错误本就在各路由内就地捕获返回 4xx，不受影响。
- chains/artists 的 GET 与 DELETE 路径对 tags/modules/params/variableValues/benchmarks 等库内 JSON 列直接 `JSON.parse`：一条脏数据就让整个列表接口 500，或让画师条目因 benchmarks 列损坏而**无法删除**。统一改用已有的 `parseStoredJson` 安全助手，损坏列回退为空值。

### 修复：画师基准图生成队列卸载/暂停后仍启动新任务

- 队列处理 effect 的 `processNext` 此前无生命周期控制：离开画师库页面后队列会继续启动新的 NovelAI 生成（持续扣费），节流等待期间点"暂停"也无法阻止旧闭包继续开工。现在节流等待结束、真正调用生成接口前检查存活与暂停标记（暂停用 ref 感知，避开旧闭包读到过期 state），命中即复位 isProcessing 并停止；已付费的进行中任务仍允许完成并落库，不浪费额度。

### 修复：AITag 图库搜索/筛选竞态与卸载后缓存污染

- `loadWorks` 此前只在追加模式做了 querySignature 校验，非追加路径（换搜索词/筛选/排序/跳页）完全没有 stale 检查：快速连续操作时较慢的旧响应会覆盖新结果，且无条件写入模块级 `aitagPageCache`——组件卸载后迟到的响应也会污染全局页面缓存，下次进入页面还原出错误数据。现在每次加载持有序号，等待返回后若已有更新的加载或组件已卸载则整体丢弃（不写状态、不写缓存、不弹错误）。

### 修复：连续提示时新 toast 被旧计时器提前清掉

- `notify` 每次都新起一个 3 秒 `setTimeout` 且不保存句柄：连续两次提示时，前一个计时器到点会把后一个新 toast 提前清掉。现在先 clear 旧句柄再挂新计时器，提示总是完整显示 3 秒。

### 修复：App 主流程错误处理裸奔（加载失败无感知、失败后永久卡加载态）

- `loadArtists`/`loadInspirations` 此前无 catch，切换到画师库/灵感页时若请求失败会成为 unhandled rejection，用户毫无感知；现在失败会 console.error 并弹错误提示。
- `refreshData` 的 catch 此前只识别"Database not configured"，其余错误被静默吞掉；现在其他错误也会提示。
- 创建、Fork、更新、删除画师串五个操作此前无 try/finally：数据库一旦抛错，`setLoading(true)` 之后永远无法复位，界面永久卡在加载态且导航不会发生；现在失败提示 + finally 复位加载态。

### 修复：画师库启动时裸 JSON.parse 可能整页白屏

- `nai_fav_artists`（收藏）与 `nai_copy_history`（复制历史）两处 localStorage 读取直接 `JSON.parse` 无保护，缓存一旦损坏（该 key 会被 Agent 面板并发写入，概率真实存在）画师库 useEffect 即抛错白屏。现与同文件已有的安全版本对齐：try/catch 回退为空，并用 `Array.isArray` 挡住"合法 JSON 但不是数组"的情况（`new Set(5)` 同样会抛 TypeError）。

### 修复：/api/media 与 /api/assets 代理缺少异常兜底

- 两段路由在主 try 之外执行：`/api/assets` 的 `decodeURIComponent` 遇到畸形编码（如孤立的 `%`）或 R2 读取抛错时会变成未捕获异常，客户端拿到 workerd 泛型 500 而非 JSON。现在两段都有 try/catch，返回结构化错误并 console.error 记录原始异常。

### 修复：/api/admin/stats 查询已被移除的 daily_stats 表导致必然 500

- 该端点查询的 `daily_stats` 表会被 `removeLegacyLoggingStorage` 一次性 DROP 且从不重建（`ensureAccessLogsSchema` 只重建 `access_logs`），个人模式下访问必返 500；且全项目无任何前端调用方，属多用户时代遗留。与 `/api/admin/logs` 等同类处理：纳入个人模式 410 禁用清单。

### 版本号机制：按提交历史校正到 0.79.0，并建立自动递增规则

- 按提交历史推断校正版本：0.5.0 自 2026-07-14 独立维护首次提交设定后从未变动，期间已积累 74 个 `feat:` 提交；按"feat = minor 递增"规则回溯，版本校正为 **0.79.0**。
- 新增 `scripts/bump-version.mjs`：一条命令完成版本递增（`major` / `minor` / `patch` / `set:X.Y.Z`），自动同步 package.json、package-lock.json 与 README 版本徽章，消除三处手工同步。
- 设置页"关于"的版本号改为 `vite.config.ts` 注入的 `__APP_VERSION__` 构建常量（该 define 此前已定义但从未被使用），新增 `env.d.ts` 类型声明；代码中不再允许硬编码版本字符串。
- 新增根目录 `AGENTS.md`（AI 编码工具通用规则文件）：约束每次修改完成即提交、每个提交按类型自动递增版本（feat→minor、其余→patch、破坏性变更与数据迁移→major）、CHANGELOG 记录与语言风格约定。

## 2026-08-12

### 画师/角色 Tag 目录封面自动预取并固定保存

- 打开画师/角色 Tag 页时,自动预取当前页可见（前 40 个）条目的封面候选：调用 `getCoverSet`（14 天本地缓存 + 300ms 串行限流,不重复打 Danbooru API）,把候选图提交 `POST /api/media/prewarm?pin` 抓取并**固定保存**（不参与 1GB LRU 淘汰）。滚动/浏览时封面已就位,秒开不依赖网络；已固定的封面自动跳过,不重复。画师页实测预取 40 个封面全部落盘（pinned 52→92）,角色页预取请求正常（多数命中此前浏览已固定的封面）。
- 修复预热队列 pin 任务的关键 bug：内部哨兵后缀 `\u0001pin` 为 4 字符但截断用了 5,导致 pinned 任务的来源 URL 尾部被多删 1 字符、抓取全部失败——此前"固定保存"实际从未生效。新增回归测试（pinned 任务 source 不被截断）,55+ 测试通过。

### 画师/角色 Tag 封面缩略图固定本地保留（不受 1GB 缓存上限影响）

- 封面图（DanbooruCover 从 Danbooru 拉取的候选缩略图）此前走网关缩略图缓存，但会被 1GB LRU 上限淘汰，长期不看的画师/角色封面会丢、重看时重新抓取。现在封面图请求附加 `pin=1`（SmartImage 新增 pin prop，DanbooruCover 启用）：网关将该缩略图标记为 fixed，**永不参与 LRU 淘汰**，1GB 上限只作用于普通图库缓存；固定保留的封面总量为画师/角色数量级（每张数十 KB），占用可控。实测画师页 26、角色页 51 个封面请求全部带 pin 且落盘标记；图钉“设为封面”的图此前已永久保存在本地 R2（/api/assets），链路不变。

### 哨兵提前量加大到约两屏（1200px）

- 600px 提前量只相当于 2-3 格滚轮，快速滚动时感知上仍是“划到底才加载”。Pixiv / Danbooru / AITag / 历史页的哨兵 rootMargin 统一 600px → 1200px（约两屏）：滚动还剩两屏就开始追加下一页并预热，到达时内容已就位；首屏内容不足两屏时打开页面即自动预填（实测 Danbooru 打开后 40→80 件），滚动到距底 1100px 即触发追加。连续追加与停止逻辑不变（内容增长移出哨兵范围后自然停止，软上限保护）。

### Danbooru / AITag / 历史页改用哨兵持续加载（与 Pixiv 同机制）

- 滚动事件触发（含 1.5s 冷却）在快速滚动时仍会断：冷却期内即使已接近底部也不加载。改为 Pixiv 同款 IntersectionObserver 哨兵：哨兵进入底部 600px 范围即追加下一页；追加完成后若哨兵仍在视口（用户停在底部或滚得快）会立即再触发，实现不间断连续加载，内容增长使哨兵移出视口后自然停止；追加模式无死循环风险（此前替换模式的死循环问题不适用）。实测：一次滚到底连续追加 2 页填满视口；持续滚动每 2 秒稳定追加一页（120→320 不间断）。

### 追加式加载提前约一屏触发

- Danbooru / AITag / 历史页的滚动触发阈值从距底部 200px（几乎触底才加载）提升到 1000px（约一屏）：滚动还剩一屏多时就开始追加下一页并预热，到达底部时新内容已就位，消除“划到底部才触发”的停顿。实测 Danbooru 在距底部 800px（离底还有 5045px）时即完成 40→80 件追加。冷却与防连发机制保持不变。

### Danbooru 追加页补齐预热：消除“断一下”

- Danbooru 图库此前只在首次加载时预热本页图，滚动触发的追加页没有预热——滚到新页时每张图都要首次抓取（~0.6s/张），表现就是“加载到某一页完就断一下再继续”。`appendNextPage` 成功后同样提交 `/api/media/prewarm`，追加页图片在滚动到达前已写入磁盘缓存（浏览器实测追加到 80 件后 86/86 张图快速加载完成）。
- 画师/角色 Tag 的封面候选：`DanbooruCover` 拿到候选集后预热前 3 张候选封面——首张显示命中缓存，点“下一张”切换也顺滑；与正在进行的直接加载单飞，不产生重复抓取。

### 缩略图预热提速（单变体 + 并发 4）

- 预热只生成桌面/常见布局实际使用的 thumb-320（此前同时预热 640，而 640 仅高分屏按需使用），任务量减半、并发槽占用减半；并发 3 → 4。同一批 30 张的消化时间约 20s → 10s，实测 10 秒后 8/8 缓存命中（此前需 20 秒），且预热与浏览请求共享并发池,占用更少意味着浏览时的未命中图等待更短。高分屏所需的 640/960 仍按需首抓后入缓存。顺带修复单变体下“已缓存跳过”判断失效的边界 bug（`PREWARM_VARIANTS[1]` 为 undefined），单元测试同步更新。

### 图库缩略图后台预热：滚动即缓存命中

- 首抓 0.8-3s 的等待发生在“用户滚动到那张图的那一刻”。新增网关缩略图预热器：`/api/pixiv/feed` 返回后自动把该批作品的缩略图源加入后台队列（跳过已缓存/已入队、上限 300、3 并发即时调度、失败静默），按 thumb-320/thumb-640 两个档位抓取并写进磁盘缓存；Danbooru 由前端在搜索返回后调用新增 `POST /api/media/prewarm` 接口提交本页 `sampleUrl`（来源经网关白名单校验）。用户滚动到对应图时直接命中缓存（实测 3-38ms），不再排队等首次抓取。真实账号端到端验证：同一批图预热前 1/8 命中、预热完成后 8/8 命中；新增预热器单元测试（去重入队/并发消化/已缓存跳过/队列上限，顺带修复 maxPending 边界）。

### 图库加载体验优化（预取提前 / 缩略图自适应 / 占位微光）

- 实测定位：网关磁盘缓存命中仅 3-5ms（缓存体系健康），不顺畅源于新图首次抓取每张 0.8-3s + 并发排队，首屏 20+ 张需 10-20s。三项优化：
  - **预取提前**：SmartImage 缩略图预取距离 600px → 1200px（提前约两屏加载），滚动到达时图已就位，实测滚动后 800ms 内 6 张新图完成加载；升级原图仍保持视口内触发，不抢带宽。
  - **缩略图尺寸按列宽自适应**：Pixiv / Danbooru 卡片与 Danbooru 封面不再写死 `thumb-640`，改用 `selectThumbnailVariant` 按实际像素宽选择（实测桌面 4 列自动落到 thumb-320），抓取与传输量减半；详情大图与升级原图保持高清晰度。
  - **加载占位微光**：图片加载中显示 shimmer 扫光动画（明暗主题适配），观感从“卡住”变为“在加载”。
- Danbooru 封面请求经排查已有完整节流（300ms 串行排队 + 14 天本地缓存 + 同 key 单飞），无需改动；网关并发按机器资源自适应，未调整。

### 图片反推链路打通：网络图直推 + 一键送往实验室

- **反推入口只吃本地文件的卡点**：图库里看到的网络图（AITag / Pixiv / Danbooru 详情）此前无法直接反推，必须先手动下载再选文件。现在三个图库详情均新增“反推此图”入口（AITag 每张图操作行、Pixiv 与 Danbooru 详情工具行），点击后自动抓取当前图（经本机媒体网关，避开跨域）并直接识别；外部图源不可达时提示保存到本地兜底。
- **反推结果只到剪贴板的卡点**：非实验室页面的反推结果此前只能复制后手动去实验室粘贴。面板底部新增“送往实验室”按钮（任意页面可用）：选中 Tag 作为主体提示词写入待导入参数并自动跳转实验室。浏览器端到端验证：Pixiv 详情一键反推识别 26 个 Tag → 送往实验室 → 主体提示词已含完整 tag 串，全程无手动切换。

### 侧边栏资源库顺序调整

- 桌面侧栏「资源库」顺序与移动端统一：画师 Tag → 角色 Tag → AITag → Danbooru → Pixiv → 灵感库（画师 Tag 紧跟画师串；三个外部图库连续；灵感库收尾）。

### 分页器统一为“跳转工具”，与滚动浏览语义分离

- 规则：**滚动 = 浏览，分页器 = 跳转**。AITag / Danbooru / 历史页移除“上一页/下一页”按钮（连续浏览走滚动，前后翻页按钮反而与追加模式混淆），保留并强调**页码输入跳转**（AITag“跳到第 N 页”、Danbooru“跳到第 N 页 + 跳转”、历史页桌面输入框/移动页码菜单）；跳转后替换为该页并从该页继续滚动追加（“从这里开始浏览”语义，实测跳第 3 页后滚动追加到 80 件）。Pixiv 为游标分页 API、无页码概念，维持滚动 + 加载更多；本地数据页数据量小，不加。

### 浏览模式统一为追加式无限滚动（AITag / Danbooru）

- 此前项目内两种浏览模式并存：Pixiv / 历史 / 画师串等为追加式（滚动到底把下一页接到列表下方，连续无切页感），而 AITag / Danbooru 仍是分页式（滚动触发整页替换 + 回顶，观感像“切页”）。按讨论方案统一：AITag 与 Danbooru 改为**追加式无限滚动**——滚动接近底部自动把下一页内容接到当前列表下方，不回顶、不替换；内容不足一屏时不触发、追加后 1.5s 冷却、自动追加软上限 800 条（超过后显示“继续加载更多”按钮手动续）。
- 分页器保留为显式跳页工具（上一页/下一页/页码跳转仍为整页替换），搜索/筛选/排序变化重置回第 1 页；列表文案改为“已加载 X 条 / 共 Y 条”。竞态防护：追加期间搜索/跳页则丢弃本次结果（Danbooru 用 query/page ref 校验，AITag 用查询签名校验）。浏览器端到端验证：AITag 60→120 条、Danbooru 40→80 件追加且滚动位置保持（无回顶），小幅滚动零触发，停止滚动后请求归零。

### 资源库全部页面改为真实比例瀑布流

- 资源库的 AITag / Danbooru / Pixiv / 画师 Tag / 角色 Tag 之前是固定比例网格（1:1、2:3、3:4）+ `object-cover`，图片被裁剪且不互相补齐；灵感库用真实宽高比所以观感正常。五个页面统一接入最短列分配瀑布流（`ShortestColumnMasonry`，与历史页/画师串一致）：图片按**真实宽高比**完整显示、逐张放入当前最短列互相补齐，跟随全局“图片列表布局”设置（masonry 时瀑布流，portrait/square 时保持网格）。
- Pixiv/Danbooru 直接用作品的 width/height；AITag 与画师/角色封面在图片加载后测量自然比例（`DanbooruCover` 新增 `onImageLoad` 上报，画师/角色库新增比例 state），加载中先用默认比例占位、加载后自动重排。
- Pixiv 卡片缩略图源从方形裁切版（`360x360`）改为 large（真实比例）经网关缩放，方形缩略图在真实比例 frame 里的裁切问题一并消除。浏览器端到端验证：Pixiv 4 列 43/43/46/43 张、列高差 9% 以内，采样图片自然比例与 frame 比例逐一匹配（零裁剪）；AITag 出现横图（1200/1000、1463/1000），画师/角色封面 684/1000、708/1000 等各异。

### 历史图库自动加载改为追加模式

- 上一版修复（滚动事件触发 + 翻页回顶）仍有两个问题：内容不满一屏（图片未加载时卡片很矮）时任何滚动都会触发翻页；整页替换 + 回顶让用户刚看过的最新图消失。改为**追加模式**：滚动接近底部时把下一页内容追加到列表下方（与 Pixiv 一致），不回顶、不替换，最新图始终保留在顶部；内容不足一屏时不自动加载（防任何滚动都触发）；追加后 1.5s 冷却（图片加载引起的滚动锚定事件不会连发）。分页器保留作兜底/跳页（跳页仍为整页替换）。浏览器端到端验证：小幅滚动 300px 零请求不翻页，滚到底追加一页、最新图保留，停止滚动后请求归零；Danbooru/AITag 补上同样的内容高度保护。

### 历史图库自动翻页死循环修复

- **历史页连环翻页死循环**：历史图库用 IntersectionObserver sentinel 触发自动翻页，但历史页是整页替换模式且图片未加载时卡片高度不足，翻页回顶后 sentinel 仍在预加载范围内 → 立即触发下一页 → 循环刷到最后一页：海量 `/api/assets/covers` 请求刷屏、页面永远停在“加载中”、日期分组内容闪烁。改为在滚动容器上监听用户 `scroll` 事件、接近底部才翻页并回顶（回顶后滚动事件链断开，循环不可能自维持）；Danbooru/AITag 同为整页替换模式，一并从 sentinel 改为 scroll 事件触发。浏览器端到端验证：历史页稳定停在第 1 页、图片全部加载成功，滚动一次只翻一页；Pixiv 追加模式保留 sentinel 无限滚动（85→173 件持续加载），画师/角色目录与画师串列表为追加模式不受影响。

### Pixiv 推荐无限加载 + 全部图库滚动自动加载

- **Pixiv 推荐模式无法翻页修复**：Pixiv recommended 接口的 `next_url` 携带逐页累积的 `viewed[]` 去重列表，URL 随翻页无限增长（实测 2293+ 字符），超过游标 2048 字符上限后被白名单整条丢弃，导致推荐流永远只有第一页。清洗时改为裁剪最早的 `viewed`（保留最近 60 条）并重排索引从 0 连续编号（Pixiv 对不连续索引返回 400），翻页不再有上限；真实账号验证 6+ 页连续翻页。
- **图库滚动自动加载**：Pixiv 图库改为滚动接近底部自动加载下一页（IntersectionObserver sentinel，按钮保留作兜底）；Danbooru/AITag/历史页滚动到底自动翻页（保留原分页器）；画师串列表滚动自动追加分批渲染。画师/角色资料库此前已有 sentinel 自动加载，保持一致。浏览器端到端验证：Pixiv 推荐流连续滚动 430→855+ 件持续增长。

### Pixiv 登录适配 custom scheme 回调 + 安全模式模糊残留修复

- **Pixiv 登录流程适配**：Pixiv 已将登录成功回调从 HTTPS callback 白页改为 `pixiv://account/login?code=…` custom scheme（桌面浏览器未注册协议时登录会卡在空白页，地址栏监听也抓不到）。新增 `pixiv://` URL 协议注册（`HKCU\Software\Classes\pixiv`，登录时幂等注册、失败自动降级）与 `scripts/pixiv-scheme-handler.mjs`：Windows 把 scheme 跳转交给本机 node 脚本，解析官方 code 后经 `POST /api/pixiv/login/complete` 完成登录；`parsePixivCallbackUrl` 同时接受 HTTPS callback 与 `pixiv://account/login`，`complete` 允许省略会话 id（协议处理器无 id 场景），手动粘贴两种地址均可用。已用真实已登录 Edge 账号端到端验证：断开→重连→图库全流程正常，token 仍仅加密落盘 `local-data/pixiv-tokens.json`。
- **安全模式模糊残留修复**：点击图片时只给命中的一张 `<img>` 打 `data-safe-revealed`，而 SmartImage 在同一容器内渲染主图 + 渐进升级高清叠加图（CSS 按单图判断遮挡），导致叠加图残留模糊。点击解除时改为同容器内所有 `img` 整组解除，详情大图与卡片图一致生效。

### Pixiv 图库前端（本机版）

- 新增 Pixiv 图库前端（`services/pixivService.ts` + `components/PixivGallery.tsx`）：连接管理（refresh token 仅以加密形式保存在本机 `local-data`，页面内只在临时 state 持有、操作完成后立即清空）、推荐/标签搜索/日周月榜单/画师作品浏览、`nextCursor` 加载更多。
- 作品详情支持多页切换、打开 Pixiv 原站、当前页导入实验室、保存到灵感库；ugoira 动图作品提示仅展示首帧；点击标签可直接按该标签搜索。
- Pixiv 图片统一经本机媒体网关代理（`i.pximg.net` 必须携带官方 Referer，浏览器直连会 403）：卡片缩略图走现有 SmartImage/移动端缓存链路，详情原图与灵感库保存（转 data URL）走 `/api/media` 本地代理。
- Pixiv 内容按后端接口原样透传 `x_restrict`，不做年龄分级过滤，也不提供年龄开关（与后端策略一致）。
- 桌面侧栏与移动端资源库新增「Pixiv」入口；Pixiv 仅在本机媒体网关可用，Cloudflare Worker 部署不含 Pixiv 逻辑，`/api/pixiv` 在云端失败关闭（fail closed）。

### Pixiv 内置图库：后端与安全基础

- 新增 `scripts/pixiv-local.mjs`：Pixiv App API 严格允许列表（仅 `app-api.pixiv.net` 的 `GET /v1/illust/recommended`、`/v1/search/illust`、`/v1/illust/ranking`、`/v1/user/illusts`、`/v1/illust/detail`），`next_url` 分页游标仍须通过同一 host/path/method 白名单；Pixiv 响应规范化为统一卡片结构，年龄分级 `x_restrict` 原样透传，不做任何过滤或开关。
- refresh token 与 access token 只在本机 `local-data` 中以独立密钥 AES-256-GCM 加密保存（`pixiv.key` / `pixiv-tokens.json`），原子写入并尽量 `0600`；刷新单飞，401 最多重试一次，新 refresh_token 非空才覆盖旧值；任何响应、错误与日志都不携带 token。
- OAuth 刷新请求对齐 Pixiv iOS App 客户端格式（`X-Client-Time` / `X-Client-Hash` / App headers，并兼容嵌套 token 响应）；连接时先验证 refresh token，失败恢复原连接。令牌写入使用代际保护，断开或重新连接后，旧的并发刷新结果不能把 token 写回来；损坏的密钥文件直接失败，不会被静默覆盖。
- 本机 media gateway 新增 `/api/pixiv/status`、`POST/DELETE /api/pixiv/connect`、`/api/pixiv/feed`（mode=recommended/search/day/week/month/user/detail + cursor），沿用现有局域网访问控制；部署到 Cloudflare 的 Worker 不含任何 Pixiv 逻辑，`/api/pixiv` 在云端必然不可用（fail closed）。
- 局域网设备可在通过 NPM 访问校验后浏览 Pixiv 图库，但 refresh token 的连接与断开操作仅允许在运行 NPM 的本机电脑执行。
- `/api/media` 仅在本机 gateway 白名单放行 `i.pximg.net`，抓取时自动携带 `Referer: https://www.pixiv.net/`（含重定向与 original 原图代理）；Worker 的 `MEDIA_REMOTE_HOSTS` 保持不含 `i.pximg.net`。
- 新增 `scripts/pixiv-local.test.mjs` 并挂入 `npm run test:gateway`：覆盖 host/path/method 白名单、cursor 清洗与回传、token 不回显与加密落盘、pximg Referer、断开状态、单飞刷新与 401 重试一次，全部使用 mock fetch，无需真实 token。

### 角色/画师 Tag 图钉设封面不再 403

- 图钉保存改为复用本机媒体网关链路：先经 `/api/media?variant=original` 读取 Danbooru 原图（不再让 Worker 直连 cdn.donmai.us，上游对 Worker 出口返回 403），校验来源域名、响应类型与 12MB 上限后转成 data URL 提交，由 Worker 现有上传流程写入 R2，数据库只保存 `/api/assets/...` 地址。
- 角色 Tag 改为图片读取成功后才创建/更新角色记录，读取失败不再留下空记录；画师 Tag 保留原有 `id/name/previewUrl/benchmarks`，画师 data URL 上传分支补上与角色一致的配额校验。
- 新增 `services/danbooruCoverImport.ts` 统一导入函数，严格校验 `https://cdn.donmai.us` 精确主机名；保存期间图钉自动禁用防重复提交。

### 历史图片按文件真实尺寸显示与入库

- 历史页卡片读取图片加载后的真实宽高比纠正错误的 `params.width/height`：异常记录（如测试写入的 `832×1216` 竖参数、实际 `1216×832` 横图）加载完成后自动恢复横图容器，不再左右裁切；最短列高度估算与卡片 `aspect-ratio` 统一走同一比例函数。
- 上传历史图片时不再无条件信任调用方宽高：客户端复用缩略图 bitmap 的真实尺寸覆盖，Worker 对 PNG/JPEG/WebP 做真实尺寸解析（PNG IHDR、JPEG SOF、WebP VP8/VP8L/VP8X，含边界检查与异常尺寸拒绝），multipart 与 Data URL 两条路径都以图片真实尺寸为准保存，其他生成参数保持不变。

### 画师串/角色串瀑布流不再出现右侧完整空列

- 画师串（含角色串）列表在「瀑布流」布局下改为最短列分配：卡片按当前排序逐张放入预计高度最小的列，外层显式生成等宽列容器，不再依赖 CSS `column-count` 的自动平衡，收藏、搜索、Tag 筛选等少量结果时最右侧不再出现整块空白。
- 列数同时响应移动端 1/2/3 列设置、桌面端 2-5 列设置与 `auto` 断点（手机 2、≥768px 4、≥1280px 5、≥1600px 6），并随窗口尺寸变化即时更新；卡片数量少于列数时按实际数量生成列。
- 高度估算复用图片加载后保存的实际宽高比（含 48px 标题区），图片比例更新或取消收藏后自动重新分列；卡片保持自然图片比例，不拉伸、不裁切。
- 「竖向卡片」「方形」布局与其他使用画廊布局的页面（灵感、历史、Danbooru、角色库、画师 Tag、AITag）行为不变。

### 实验室移除重复 Agent 入口，反推按钮移入其槽位；资源库导航命名统一

- 实验室/编辑器顶栏删除右上角与侧栏「项目 Agent」重复的 AI 生图 Agent 按钮（桌面侧栏与手机悬浮入口保持不变）；图片反推按钮移入原 Agent 槽位，最终顺序为「引用预设、图片反推、保存/Fork、重置」。
- 桌面侧栏与手机资源库弹层命名统一：角色库 → 角色 Tag、灵感 → 灵感库；路由标识与数据类型不变，不影响任何用户数据与链名称。

## 2026-08-11

### 图片反推 Tag 升级为全局右上角动作

- “图片反推 Tag”从实验室“基础画风”导入栏移到全局右上角：画师串、实验室/编辑器、角色库、画师 Tag、AITag、Danbooru、灵感、历史八个页面顶部右侧都有同一入口（桌面为设计系统图标按钮，手机为移动图标按钮），打开的是同一个本地 WD Tagger 面板。
- 面板在实验室等编辑器页面沿用“追加到主体”流程；在其他页面默认把选中的识别 Tag 复制到剪贴板。
- 移除实验室“基础画风”区域里的局部“图片反推 Tag”按钮，避免同一功能出现两个入口；顶栏按钮统一为与 AI Agent、引用预设一致的头栏样式。

### Danbooru 详情面板不再占用空位

- 未选择作品时列表占满整个内容区，右侧不再显示空白的“作品详情”占位面板；选择作品后桌面端在右侧展开详情，手机端进入全屏详情层，关闭后恢复满宽列表。
- 手机端详情层接入统一的历史记录处理，返回按钮与浏览器后退均可关闭详情回到列表。

### Tag 库 Danbooru 候选图与固定封面

- 角色 Tag 和画师 Tag 卡片都可左右切换查看同一精确 Tag 的全部可用候选图，按 Danbooru 热度降序逐页加载；角色候选继续过滤多人图、漫画、玩偶和特殊服装等不适合作为角色卡的图片。
- 可将当前候选图设为该 Tag 的固定封面。图片会由电脑端下载并保存到项目自己的存储中，不依赖临时浏览器缓存或外链持续可用。
- 角色与画师 Tag 卡片统一右上角操作栏：共享收藏与固定封面位置；画师专属的原帖、放大和基准图操作在同一列继续排列，不再互相遮挡。

### 顶栏按工作区工具栏语言重排，隐藏内部技术标记

- 画师串、角色库、画师 Tag 三个页面顶栏统一工作区工具栏语言：主搜索与主动作清晰前置，筛选与视图/列数归入次级分组，状态信息弱化为普通灰色文本。
- 内部技术标记不再面向用户：`__character_catalog__`、`NAI`、`aitag` 等来源/类型标记从画师串标签筛选（含移动端抽屉）与引用预设弹窗的 Tag 筛选中隐藏，标签仅取自已可见串的真实用户标签。
- 画师串：标签筛选从工具栏移出为工具栏下方的次级筛选条（存在真实标签时显示）；排序、收藏、刷新与新建动作保持同排。
- 角色库：次级行改为“搜索 + 排序 + 列数”分组，状态“显示 · 目录 · 自定义”右对齐并弱化。
- 画师 Tag：首行搜索占满整行成为主操作，网格/列表切换、列宽滑杆与刷新归为次级；次行左侧为状态（弱化）+ 排序 + 原图/实装，右侧聚类抽卡组与导入/历史/收藏等工具。
- 全部功能与移动端布局保持不变，仅调整结构、分组与层级。

## 2026-08-10

### 内置 Danbooru 素材检索与本地图片反推 Tag

- 资源库新增 Danbooru 页面，通过 Safebooru 只检索通用级图片；支持英文 Tag、中文精确匹配、双 Tag 组合、分页、分类 Tag、复制、导入实验室和加入灵感库。
- 画师 Tag 与官方角色目录不再要求预先生成海量配图：画师使用精确 Tag 下的最高分作品；角色从高分候选中排除多人图、漫画、文字梗、玩偶、特殊服装等干扰，再根据候选中高频出现的发型、饰品与服装 Tag 选择最接近典型外观的单人图，热度和清晰构图作为次级排序。泛角色没有可靠单人形象时宁可留空；本地封面始终优先。
- 实验室新增“图片反推 Tag”，在电脑本地按需下载 `SmilingWolf/wd-vit-tagger-v3`（约 379 MB），用 CPU 推理并分别控制通用 Tag 与角色 Tag 阈值；图片不会上传到第三方识图服务。
- Danbooru 请求由电脑代理并严格限制到 Safebooru 的公开作品接口，缩略图沿用媒体网关和安全模式；新增远程地址白名单回归测试。
- 图片与 Tag 数据仍归各自来源及作者所有；模型采用 Apache-2.0，推理运行库采用 MIT，项目自身继续采用 MIT。

## 2026-08-09

### 灵感图库升级为创作知识库

- 明确历史收藏与灵感库的职责：历史收藏作为快速收件箱，灵感库负责长期精选、整理、检索和复用；历史与 AITag 的入口文案统一改为“加入灵感库”。
- 新增灵感板、标签、备注、来源血缘、1～5 星评分、置顶、归档、最近使用时间和使用次数；旧数据启动时自动迁移，不需要手工处理数据库。
- 灵感页面重做为资源工作区，支持智能分类、组合搜索、标签与评分筛选、四种排序、批量移动/加标签/置顶/归档/删除，以及桌面侧栏和手机筛选布局。
- 详情支持自动建议标签、展示来源与相似灵感，并提供完整替换、追加 Prompt、仅正面、仅负面和仅参数五种实验室导入方式。
- 灵感可直接创建画师串、角色参考、Vibe 或设为画师串封面；每次复用都会更新使用统计，便于按最近使用和使用次数找回高价值资料。
- 历史加入灵感时共享同一份本地原图，删除任一引用不会误删仍在使用的图片；AITag、上传和 Agent 创建的灵感均记录各自来源。
- 当前筛选结果改为一次渲染并立即请求 320px WebP 缩略图，不再按 60 张分批等待，打开页面即可连续看到整页图片进入加载。
- 项目 Agent 新增灵感板、备注、标签、评分、置顶和归档整理能力，从历史创建灵感不再把原图转换成 Base64 重复上传。
- 灵感库视觉重新对齐全局设计系统：恢复搜索优先的单行工具栏，桌面筛选使用与 AITag 一致的浮层，手机复用统一底部抽屉；二级导航、来源徽章、弹层圆角、按钮层级和移动端图标尺寸同步收敛。

### 缩略图生成并发按电脑配置自适应

- 取消电脑端固定同时生成 2 张缩略图的保守限制，根据逻辑处理器数量和物理内存自动选择 2、4 或 6 路并发。
- 当前开发电脑为 i5-12600KF、32GB 内存，将使用 6 路并发；低内存或低核心设备继续使用较低并发，避免批量解码大图造成资源峰值。
- 新增并发分档回归测试，覆盖高核心低内存时自动降级的情况。

### 图片列表统一使用真实缩略图

- 桌面端使用共用缩略图组件的列表不再直接下载原始 PNG，和手机端一样按卡片尺寸请求 320px 或 640px WebP；图片详情仍读取原图。
- st-chatu8 外部历史图片加入受限缩略图网关，列表按需生成并复用电脑端 WebP 缓存，不再在手机或桌面列表中传输原图。
- 新增媒体源白名单回归测试，确保缩略图网关只开放项目图片和格式严格的 st-chatu8 历史图片地址。

### 历史图库改为整页立即加载

- 打开历史图库或切换分页时，当前页 20 张缩略图会立即开始加载，不再等待卡片进入滚动预加载范围。
- 历史图库图片使用浏览器高优先级加载；其他可能包含大量条目的资源图库继续使用按需懒加载。

### 修复缩略图回滑后偶发空白

- 共用缩略图组件现在以页面的实际滚动容器作为懒加载观察范围，预加载距离不再被内部滚动区域裁掉。
- 图片首次进入预加载区后保持激活，不再因短暂离屏而卸载图片或取消请求；向下滚动后回滑会直接复用已加载结果。
- 保留卡片的离屏绘制优化、电脑 WebP 缓存和手机 IndexedDB 缓存策略。

## 2026-07-30

### 05:32 — 修复手机端生成按钮被安全模式拦截

- 修复安全模式从手机浮动操作按钮向外查找图片、把相邻的最近生成图误判为点击目标的问题；“生成”按钮和底部导航不再被安全模式静默吞掉点击。
- 将交互控件设为安全模式图片命中的边界，同时保留点击图片卡片进行临时解模糊的原有行为。
- 手机浏览器未配置 NovelAI API Key 时改为显示项目内错误提示，不再只把原因渲染到手机端不可见的桌面预览区域。

## 2026-07-29

### 22:40 — 恢复画师串封面并修复同步误清空

- 修复 st-chatu8 预设没有配图时，连接器误将“未提供配图”解释为“删除 NaiPromptManager 封面”的问题；无配图的 st-chatu8 预设现在只同步提示词内容，不再覆盖本项目已有封面。
- 从 2026-07-14 数据备份精确恢复 119 个原画师串的封面地址；恢复前额外保存当前数据库、桥接状态和 SillyTavern 设置快照。
- 新增无配图同步回归测试；真实同步后项目仍保留 146 个封面，逐一请求均返回有效图片，st-chatu8 显示 134 个画师串中 132 个带配图。

### 16:06 — 提高大数据恢复时的启动容错

- 将本地核心服务的启动恢复等待上限从 45 秒调整为 3 分钟；约 4.16 GB 本地数据在异常退出后需要恢复时，不再被启动器过早判定失败并强制终止。
- 保留分阶段进度提示；常规启动仍会在服务就绪后立即开放图片网关，不会因为上限提高而固定多等。

### 15:28 — 将 st-chatu8 同步改为事件触发与低频兜底

- 取消连接器每 60 秒一次的固定同步，改为 SillyTavern 启动时同步、窗口重新获得焦点时同步，以及 st-chatu8 画师串、配图、Vibe 或组合实际保存变更后延迟 3 秒同步。
- 增加轻量数据签名，普通 SillyTavern 设置保存和连接器自身保存不会反复触发同步；连续修改会合并成一次请求。
- 保留每 10 分钟一次的低频兜底，避免极少数未触发设置事件的修改长期不同步；手动“立即同步”入口保持不变。
- 修复历史同步每次都将全部 6687 条记录分批提交给内部 Worker 核对的问题；现在只查询桥接状态中从未确认的新原图，重复同步对既有历史产生 0 次数据库核对请求，避免长期运行累积内存与无意义 I/O。

### 04:46 — 让 st-chatu8 中的同步画师串配图直接可见

- 确认画师串配图并未丢失：SillyTavern 中 13 条 `previewImageId`、13 个存储文件和图片请求均正常；问题来自 st-chatu8 普通预设下拉框只显示文字，且可视化选择器按名称分页后，带图条目分散在第 2、3、10 页。
- 在 st-chatu8 原生可视化选择器工具栏增加“有配图 13”入口，直接展示所有共享画师串缩略图；点击缩略图会调用原生搜索定位对应预设，再按原有卡片逻辑选择，不修改 st-chatu8 的预设数据结构。
- 连接器同步状态增加“带配图”数量，避免只看到画师串总数却误以为封面没有同步；手机端配图库使用双列底部浮层，桌面端使用紧凑四列浮层。

### 04:13 — 打通 SillyTavern st-chatu8 资料与生成历史

- 新增配套的 SillyTavern 第三方扩展，在 st-chatu8 与 NaiPromptManager 之间双向同步画师串、画师串封面、永久 Vibe 和 Vibe 组合；发生冲突时始终以 st-chatu8 为准，NaiPromptManager 独有的新资料仍可先补入 st-chatu8。
- 将 st-chatu8 的 6687 张生成原图单向接入本项目历史，不复制约 11 GB 原文件，也不导入其缩略图；历史图片继续直接读取 SillyTavern 的原图路径。
- 修复画师串互通时封面未保留，以及同步状态记录旧封面地址导致相同封面可能被重复上传的问题；当前 134 个画师串中 13 个带封面，实测全部可在本项目页面正常加载，重复同步后数量和地址保持稳定。
- 连接器设置卡片改为适配 SillyTavern 窄设置栏的三段布局，标题、同步按钮、自动同步与状态不再互相挤压或竖排；缓存版本提升至 1.2.0。
- 新增桥接安全与回归测试，覆盖路径穿越、原图/缩略图分离、历史去重、st-chatu8 权威冲突、封面保留与去重、Vibe 内容哈希统一；网关测试共 36 项。

## 2026-07-28

### 02:30 — 修复 Agent 已打开时进入画师串出现预览窄条

- 修复多个 Keep-Alive 编辑器各自持有 Agent 控制器时的全局状态冲突：尚未打开的编辑器 Agent 不再清除另一个可见 Agent 所拥有的 `agent-panel-docked` 状态，解决“先开 Agent、再进入画师串”后右侧预览被压成窄条、必须拖一下 Agent 才恢复的问题。
- 工作区增加实际宽度观察器，在画师串首帧、Agent 开关/拖宽、侧栏折叠/拖宽及窗口变化时同步紧凑编辑器状态；不再只依赖 Chromium 偶尔延迟生效的容器查询。
- 实测两种顺序：先开 Agent 再进入画师串时，545px 剩余工作区立即切为纵向布局，编辑区与预览区均为 535px；在编辑器内关闭 Agent 后恢复 1065px，再打开时自动回到紧凑布局，全程无窄条和横向溢出。

### 01:49 — 修复浅色主题无法覆盖系统深色的问题

- 将 Tailwind 4 的暗色变体从默认的系统媒体查询改为应用根节点的 `.dark` 类控制，使“跟随系统、浅色、深色”三档设置真正独立生效；修复系统处于深色时点击浅色只改变设置状态、界面却没有反应的问题。
- 补齐页面根背景的深色样式，避免页面加载或弹层切换间隙出现错误的浅色底；实测深色下侧栏为 `rgb(23, 25, 35)`，切换浅色后立即恢复 `rgb(255, 255, 255)`，根节点状态同步正确。

### 01:34 — 重排 Agent 顶栏、会话列表与输入区

- Agent 顶栏改为“导航、会话标题、模型设置、常用工具”的紧凑结构，将占位明显的模型按钮和思考等级下拉合并进统一设置浮层；长会话标题获得稳定的省略空间，清空、全屏与撤销图标不再互相挤压。
- 会话抽屉改为固定高度的紧凑列表行，原先始终参与布局的重命名与删除按钮收进单行操作菜单；补齐暗色搜索框文字与占位符对比度，四条会话可在首屏连续阅读，不再出现大块空白。
- 输入区统一为附件方钮、高对比输入框和纯图标发送钮；修复暗色主题下输入文字近乎不可见、输入框缺少最小宽度造成横向溢出，以及“执行”文字按钮体积突兀的问题。
- 在 1280×720 暗色桌面实测侧栏停靠、长会话、会话菜单、模型设置与输入区，均无横向溢出；完整构建和 `git diff --check` 通过。

### 01:24 — 优化页面形态切换、拖动与长会话 Agent 流畅度

- 页面导航改用 React 非阻塞过渡，大型媒体网格启用视口外渲染跳过，并去除卡片网格和卡片尺寸相关的 `transition-all`；画师串、角色库、画师 Tag、灵感、历史与 AITag 等 Keep-Alive 页面切换时减少无效布局和绘制。
- 桌面侧栏拖宽、手机 Agent 悬浮入口拖动、Agent 桌面宽度与手机高度调整统一改为逐帧更新；拖动期间不再反复重渲染页面或同步写入浏览器存储，只在松手时保存最终位置。
- 将 Agent 开关状态从庞大的生图实验室中隔离，已挂载的 Agent 通过独立浮层事件直接打开；关闭后保留会话 DOM 与排版缓存，不再销毁或以 `display: none` 强制下次重新布局，避免开关 Agent 时牵动实验室和长消息列表整体重算。
- 长 Agent 会话默认只渲染最近 60 条，旧消息分批展开；超过 4000 字符的旧回答默认显示摘要并按需解析完整 Markdown，消息列表和 Markdown 均增加记忆化，已有模型与会话数据不重复清空加载。
- 设置、确认面板、手机底部抽屉和全屏详情加入 140～180ms 的轻量合成动画，并完整支持“减少动态效果”系统偏好；安全模式的鼠标移出检测同步改为逐帧合并。
- 重做 Agent 消息底部信息栏：token/费用单行省略，复制与重新生成在手机端使用固定尺寸图标按钮、桌面端显示简短文字，解决窄屏操作文字被挤成竖排的问题。
- 完整构建、30 项网关测试及 `git diff --check` 通过；390×844 手机视口验证 Agent、页面和弹层无横向溢出。

## 2026-07-27

### 23:52 — 为实验室提示词增加逐项中文翻译

- 所有实验室 Tag 提示词输入框增加紧凑的双行翻译区，按原 Prompt 顺序展示英文词项与中文含义；现有 Tag 词库翻译使用主题紫色，支持逗号、换行、竖线、花括号/方括号权重、`1.2::tag::` 和 `artist:` 等写法。
- 对词库没有收录的自定义提示词显示“待翻译”，并提供绿色“翻译缺失项”入口；补译复用当前项目 Agent 选择的 DeepSeek、Gemini、Grok 或自定义模型，但使用独立无会话请求，不写入 Agent 对话，也不授予项目操作工具。
- AI 补译严格校验模型 JSON，只接收原请求中的词项，并持久保存到电脑 `local-data/tag-translations.json`；以后在其他输入框或手机端遇到相同词项会直接复用，词库翻译始终优先于 AI 结果。
- 翻译查询采用 250ms 防抖和分片请求复用，长 Prompt 的翻译区限制高度并独立滚动；1440×900 桌面及 390×844 手机视口验证紫色词库翻译、灰色缺失状态、权重解析和无横向溢出，完整构建、30 项网关测试及 `git diff --check` 通过。

### 06:59 — 修复手机 Agent 真机拖动并统一移动端图标

- 重写悬浮 Agent 的触控手势：按下时主动捕获 Pointer、阻止页面滚动抢占手势，移动和松开继续由窗口级事件兜底；轻点在 Pointer 结束时直接打开，键盘操作继续兼容，解决桌面模拟可拖但部分手机无法拖动的问题。
- 将悬浮标签从 44×56px 缩小为 36×44px，图标缩至 16px并减轻阴影；实测跨屏拖动、左右吸附与轻点打开互不误触。
- 资源库弹层由 80px 高卡片压缩为 64px，四个入口统一使用 24px 居中盒和 18px 图标；底部导航图标收敛到 19px并增加固定居中盒，修复视觉尺寸不一和光学偏移。
- 统一 `MobileIconButton`、工作栏图标按钮、历史管理、设置关闭、实验室引用/保存/重置、Agent 返回、Vibe 与角色参考返回等图标的 16–18px 规格和 Flex 居中方式。390×844 视口逐项测量，可见纯图标按钮水平与垂直偏移均为 0px；完整构建、29 项网关测试和控制台检查通过。

### 06:43 — 调整手机 Agent 悬浮入口默认位置

- 将未自定义位置时的 Agent 悬浮入口从右侧中段改到左下方，并在 390×844 视口下与底部导航保持约 56px 间距，避免紧贴导航或遮挡导航按钮。
- 对旧版初始位置执行一次性迁移，使已有设备刷新后也采用新的左下默认位置；用户此后手动拖动的位置继续保留，不会再次被重置。

### 06:37 — 让手机 Agent 悬浮入口支持拖动吸附

- 悬浮入口支持手指或鼠标按住拖动，移动过程实时跟手，松开后自动吸附到距离最近的左侧或右侧边缘，并限制在顶部安全区域与底部导航之上。
- 将吸附边缘和垂直位置保存在当前设备，刷新页面、切换功能页或重新打开后仍保持上次位置；普通轻点继续打开 Agent。
- 增加 5px 拖动阈值和短时点击抑制，避免滑动被误判为打开操作。390×844 视口实测右侧拖至左下、自动吸附、刷新复位和再次点击均正常，控制台无新增错误。

### 06:30 — 将手机 Agent 改为屏幕边缘悬浮入口

- 移除“设置 → 项目 Agent”中的打开按钮，设置页只保留模型、供应商和权限配置，不再承担工具启动入口。
- 手机端新增吸附在屏幕右侧中段的 Agent 悬浮标签，跨画师串、资源库、历史和实验室常驻，避开底部导航且不随内容滚动；资源菜单或设置打开时自动隐藏，避免压在弹层上方。
- 手机实验室顶部隐藏重复的 Agent 按钮，只保留引用、保存与重置；桌面侧栏和桌面实验室入口保持不变。390×844 视口实测悬浮入口可打开原有 Agent 面板，页面无横向溢出。

### 06:24 — 修正手机历史日期条、实验室操作色与 Agent 入口

- 取消历史日期分组条的吸顶与遮罩效果，使日期随图片列表正常滚动，不再覆盖首行缩略图。
- 将实验室右上角 Agent、引用、保存到库和重置统一为相同的 44px 中性卡片按钮；前三项使用统一靛蓝图标，重置仅以红色图标表达危险语义，避免四块按钮各自使用不同底色。
- 从手机“资源库”菜单移除分类错误的 Agent 入口，资源库只保留画师 Tag、角色库、AITag 与灵感库；在“设置 → 项目 Agent”增加明确入口，同时保留实验室快捷入口和桌面侧栏入口。
- 完整构建通过；390×844 手机视口实测日期条为普通文档流、四枚实验室按钮视觉一致、资源库分类正确、设置入口可用，浏览器控制台无新增错误。

### 05:35 — 重构全项目工作栏、卡片与设置界面

- 移除画师串、角色库、画师 Tag、AITag、灵感、历史和独立实验室内容区的重复页面标题，将顶部统一改为搜索、筛选、状态与主操作工作栏；限制桌面搜索框宽度并统一约 57px 的紧凑高度，手机端继续保持单行和 44px 触控区。
- 引入采用 ISC 许可的开源 `lucide-react` 图标体系，替换侧边栏、手机导航、设置、Agent、空状态和主要操作中的 Emoji、手绘 SVG 与风格不一的符号；收敛 Agent、Anlas 和普通操作的多余粉紫渐变，公共排队全过程的既有绿紫动态渐变保持不变。
- 统一媒体卡片视觉：画师串卡片只保留封面与名称，复制和删除改为桌面悬停操作；角色卡只长期显示复制与导入实验室两个主操作，编辑、删除和 Danbooru 继续由详情承载；灵感卡片压缩次要文字，画师 Tag 与 AITag 的状态和收藏控件统一尺寸与层级。
- AITag 未选择作品时改为真正的全宽列表，不再预留空白详情栏；选中后再进入主从布局。历史页按今天、昨天或具体日期分组并提供日期筛选，刷新和管理操作统一到工作栏，原有底部分页不变。
- 桌面全局设置重做为“左侧分类导航 + 右侧单项内容”的宽屏双栏布局，手机仍使用全屏折叠分组；外观、NovelAI、Agent、Anlas、词库、缓存与关于各自独立，去掉 Agent 和点数设置的大面积彩色底。
- Agent 会话、模型、工具状态和输入区统一为靛蓝品牌色与 Lucide 图标；长代码块和长逗号提示词自动变成可折叠、可单独复制的内容卡片，手机底部面板可在不挤压项目页面的情况下阅读长回答。
- 移除重复加载的 Tailwind CDN 和英文页面语言声明，改为完全使用本地构建样式并声明简体中文，消除额外外网请求与生产环境警告。1440×900 桌面和 390×844 手机实测无横向溢出；AITag 空详情不占位，实验室与 Agent 布局正常，29 项网关测试、完整构建及 `git diff --check` 通过。

### 04:38 — 收敛全项目字体层级

- 逐页检查桌面与手机端主要功能区的字号层级，保留页面标题、正文、状态图标、密码输入与大图控件等合理尺寸，避免使用全局缩放破坏可读性和手机输入体验。
- 将画师串与灵感卡片标题统一为 14px，缩小画师串和画师 Tag 的桌面搜索文字及“新建画师串”按钮，解决多列卡片和紧凑工具栏中文字过于抢眼、长名称更易截断的问题。
- 将新建画师串弹窗标题从 24px 收敛至 20px并减少标题留白，灵感详情桌面标题同步从 24px降至 20px；完整构建与 `git diff --check` 通过，1440×900 桌面和 390×844 手机视口均无横向溢出或新增控制台错误。

### 04:20 — 让画师串大图查看覆盖 Agent

- 修正画师串历史大图与 Agent 的层级关系：大图查看属于最高优先级内容层，打开后会覆盖整个项目和右侧 Agent，图片按完整浏览器区域居中显示，不再被 Agent 截断。
- 补充明确的“关闭大图”无障碍名称；实测左右翻页与关闭按钮均位于大图层内，退出后 Agent 对话保持原有打开状态，控制台无新增错误。

### 04:10 — 修复画师串编辑器被 Agent 遮挡成窄条

- 统一 Agent 右侧停靠与工作区让位的响应式断点，修复中等桌面宽度下 Agent 已位于右侧、工作区却仍按全宽排版，导致画师串预览栏被覆盖后只露出一条窄栏的问题。
- Agent 停靠时会按可用空间限制自身宽度，编辑器依据实际剩余宽度自动改为上下排列；低于 1024px 时改用底部抽屉，避免侧栏、编辑器与 Agent 三者互相挤压。
- 完整构建与 `git diff --check` 通过，并在 900×800、1180×800 和 1440×900 下验证底部抽屉、纵向重排及桌面停靠边界均无重叠。

### 04:01 — 重做可折叠、可调宽的桌面侧边栏

- 桌面侧边栏收窄为 216px，并支持折叠为 68px 图标栏、拖动调整至 192～320px、窄幅吸附折叠以及双击边缘恢复默认宽度；宽度与折叠状态会在本机保存。
- 按工作区、资源库和记录重新整理导航层级，统一紧凑图标、选中指示和键盘焦点样式；底部重新编排项目 Agent、Anlas、安全模式和全局设置，将版本信息移入设置页“关于”。
- 修复安全模式开关滑块越出轨道的问题；Anlas 积分改为不可点击的纯状态展示，不再意外打开全局设置，积分配置仍由明确的“全局设置”入口管理。

### 03:45 — 重新平衡紧凑顶栏的留白与控件层级

- 修复上一轮过度压缩后画师串、角色库、AITag 与灵感右侧操作拥挤甚至超出可视区域的问题；顶栏改为按“标题与主操作 / 搜索与筛选”分层，所有常用按钮在 1440×900 下完整可见且无横向溢出。
- 角色库保留分类、新建与随机抽卡入口，将抽卡范围和数量收入轻量设置浮层；AITag 保留双搜索框，将类型、缓存、排序与月份收入桌面筛选浮层，避免低频选项长期占满顶栏。
- 画师 Tag 在搜索框内显示当前结果数，长统计信息仅在超宽桌面显示，并限制基准图 Slot 区域宽度；灵感与历史恢复适度纵向留白，普通页保持约 57～58px，筛选密集页保持约 98～107px。
- 完整前端与 Worker 构建、`git diff --check` 通过；桌面逐页检查控件位置、浮层开关和页面溢出，手机布局保持原有单行紧凑规格。

### 03:32 — 统一并压缩各功能页顶栏

- 统一画师串、角色库、画师 Tag、AITag、灵感与历史页面的顶栏间距、分隔线和响应式结构；手机端全部收敛为约 61px 的单行工具栏，390×844 视口逐页确认无横向溢出。
- 将角色库桌面顶栏从三层 166px 压缩为两层约 98px：分类与抽卡操作合并到第一行，搜索、排序、统计和列数合并到第二行；画师 Tag 与 AITag 等筛选密集页面保持紧凑两层，不牺牲原有工具。
- 灵感、历史和画师串等普通页面改为约 53～58px 的紧凑单层；移除重复说明与历史页顶部的重复分页控件，分页仍完整保留在列表底部，避免顶栏长期占用浏览空间。
- TypeScript、完整前端与 Worker 构建及 `git diff --check` 通过；1440×900 桌面与 390×844 手机视口完成逐页高度、横向溢出和控制台回归。

### 02:18 — 统一画师串页面的固定顶栏

- 将画师串与角色串列表从“整页共同滚动”改为与历史、角色库、画师 Tag 等页面一致的分层结构：顶栏保持在独立固定区域，只有下方卡片列表滚动。
- 保留现有搜索、筛选、新建、桌面工具栏和响应式卡片布局；桌面与 390×844 手机视口分别滚动超过 1000px 后顶栏坐标保持不变，页面主体不滚动且无横向溢出。
- TypeScript、完整前端与 Worker 构建及 `git diff --check` 通过，浏览器控制台无新增错误。

### 01:27 — 修复并补全项目 Agent 全链路

- 修复 Agent 局部更新画师串参数时用默认值覆盖尺寸、采样器、Vibe 与角色参考快照的高危问题；新增完整单条画师串读取接口和 `get_chain` 工具，修改前会读取真实模块、参数与引用快照后再合并保存。
- 精简每轮请求：浏览器不再重复上传前 200 条完整预设和整批 Vibe；合并重复资料搜索工具，增加角色 Tag 专用中英文／作品搜索与 Tag 分片 LRU 缓存。会话持久消息提升到 200 条，上下文按中英文 Token 估算并只从完整用户轮次裁剪。
- 修复“选了不思考却实际低强度思考”；任务流事件改为内存合并并约每 500ms 批量落盘，空会话列表不再由 GET 隐式创建会话，空闲任务轮询退避到 10 秒，移除旧单会话迁移和已废弃配置写入链路。
- Agent 凭据改由 `local-data/prompt-agent.key` 独立随机密钥加密，首次启动自动迁移旧凭据，重设四位局域网密码不再导致模型服务失效；无法迁移或解密时会在设置页明确警告。自定义 Provider补齐安全附加请求头，并拒绝将 Authorization、Cookie、X-API-Key 等敏感鉴权头明文保存。
- 补齐项目工具：角色参考重命名、默认 Strength/Fidelity、归档确认与恢复；从历史创建待编码 Vibe、设置画师串封面；把电脑已缓存的 AITag 图片导入灵感、角色参考、Vibe 或画师串封面；管理当前设备画师 Tag 收藏并即时同步打开页面。Vibe 和角色参考归档继续强制走危险操作确认。
- 灵感资料查询补回生成参数，README 同步真实能力、存储与安全边界。修正手机自定义接口页请求头和模型标题栏的窄屏换行；已在桌面与 390×844 手机视口验证设置、会话和叠加 Agent，无新增控制台错误或横向溢出。
- 29 项网关测试、TypeScript、完整前端与 Worker 构建、Node 语法检查及 `git diff --check` 全部通过；未调用真实 LLM、NovelAI、AITag 远程接口，也未消耗 Anlas。

### 00:16 — Agent 停靠时按可用空间重新排版工作区

- 修复桌面右侧 Agent 打开后，页面仍按浏览器全宽维持过多列、导致卡片和文字被硬挤小的问题；主工作区改用自身剩余宽度作为响应式依据，Agent 关闭后仍保持原桌面布局。
- 画师串、角色库、画师 Tag、历史、灵感与 AITag 分别设置合理的卡片最小宽度；拖动 Agent 到 420～680px 时自动减少列数并换行，页面不产生横向溢出，角色库与画师 Tag 的用户列数偏好仍会在空间足够时正常生效。
- 生图实验室与画师串编辑器在空间不足时从左右双栏切换为编辑区在上、预览区在下；顶部操作会按需换行，参数区降为两列，预览高度限制在一个桌面视口附近，不再形成又窄又长的两栏。
- AITag 窄工作区改为列表与全宽详情切换；Vibe 和角色参考管理器在极窄工作区改为上下布局，详情、资料卡和当前组合不再互相挤压。
- 桌面容器规则仅在 1280px 以上且 Agent 确实停靠时启用；手机底部 Agent、Agent 全屏模式和未打开 Agent 的页面均不受影响。已在 1440×900 下实测 Agent 420px／520px 的三列／两列换行、实验室上下重排和无横向溢出；TypeScript、完整前端与 Worker 构建、26 项网关测试及 `git diff --check` 全部通过。

## 2026-07-23

### 16:18 — 补全 Pi 自定义模型接口

- Agent 设置新增“添加自定义接口”，不再只能使用 Pi 内置供应商；支持 OpenAI Chat/Completions、OpenAI Responses 和 Anthropic Messages 三种协议，以及 HTTP 本地模型服务。
- 每套接口可配置名称、Base URL、API Key和多个模型；每个模型可独立设置模型 ID、显示名称、上下文长度、最大输出、识图与推理能力，并可随时编辑或删除。
- 自定义接口通过 Pi 的 `createProvider` 和统一模型运行时正式注册，流式对话、工具调用、会话模型切换、图片输入和思考等级均复用现有 Agent 链路，不是另写一套简化请求。
- 新增电脑端连接测试，由电脑访问接口的 `/models`；手机不会直连模型服务。自定义 API Key继续使用 AES-256-GCM 加密保存在 `local-data/prompt-agent.json`，列表与浏览器接口不返回密钥。
- 支持无密钥的 Ollama、llama.cpp、vLLM 等本地兼容服务；拒绝 `file:`、带内嵌账号密码的 URL、未知协议和空模型配置，测试请求禁止跟随重定向，避免密钥被转发到其他地址。
- 新增 Pi 自定义 Provider运行时、识图模型映射和 URL 边界回归测试。26 项网关测试、TypeScript、完整前端与 Worker 构建及 `git diff --check` 通过，未请求真实模型接口。

### 16:02 — Agent 改为桌面侧栏与手机底部面板

- Agent 默认不再接管整个网页：桌面端从右侧打开为 420～680px 可调宽度侧栏，宽屏会为侧栏让出页面空间，中等宽度采用覆盖式侧栏，当前实验室仍保持可见和可操作。
- 手机端改为默认约 68% 屏高的底部面板，可通过顶部拖拽条在 25%～92% 之间调整；输入区固定在面板底部，不再无条件遮住整个实验室。
- 新增手动进入／退出全屏按钮；侧栏宽度、手机面板高度和全屏偏好保存在当前设备，下次打开继续沿用。
- 会话列表改为 Agent 面板内部抽屉，桌面侧栏不再长期被会话栏挤占；关闭 Agent 仍回到原页面，运行中任务和对话状态保持不变。
- 修复手机全局 44px 触控规则意外放大拖拽条、遮挡顶栏按钮的问题。已在 1440×900 与 390×844 视口验证侧栏让位、底部面板、全屏切换、会话抽屉和无横向溢出；TypeScript、25 项网关测试、完整前端与 Worker 构建及 `git diff --check` 通过。

### 15:32 — Agent 改为不打断当前页面的全局叠加层

- 修复从画师串编辑器打开项目 Agent 时被当作离开页面、错误弹出“放弃未保存的更改”确认的问题；打开 Agent 不再执行实验室页面跳转，真正离开编辑器时仍保留原有未保存保护。
- 画师串与角色串编辑器会把尚未保存的实时草稿直接交给叠加 Agent，关闭后名称、提示词、参数、页签和编辑状态均保持原样。
- 在画师串列表、历史、灵感、AITag 等非编辑页面打开 Agent 时，改由后台项目实验室承载上下文并通过应用顶层 Portal 显示，当前页面、滚动位置和导航选中状态不会变化。
- 为当前编辑器与后台实验室拆分独立的打开信号，避免隐藏页面和当前页面同时弹出两份 Agent。TypeScript、25 项网关测试、完整前端与 Worker 构建及 `git diff --check` 通过，并完成本地页面叠加与关闭回归验证。

### 15:22 — Agent 重新打开时定位到最新消息

- 修复 Agent 面板关闭后保留“暂停跟随底部”状态、再次从其他页面进入时停在会话开头的问题。
- 打开 Agent 和切换会话现在都会重置为跟随最新消息，并等待异步历史真正渲染后在浏览器绘制前直接定位到底部，避免先跳空列表后仍停留在顶部。
- 面板打开后用户主动上滑仍会暂停自动跟随，并保留“回到底部”按钮，不影响阅读旧消息。

### 15:14 — 修正图片导入与 Agent 的提示词分区

- PNG、JSON、历史、灵感、AITag 和角色库导入实验室时，整图主 Prompt 统一写入“主体／变量提示词”，不再误写进“基础画风（画师串）”；同时清空旧基础画风，避免导入内容与原画师串重复叠加。
- NovelAI V4/V4.5 元数据中的 `char_captions` 继续恢复到角色专属区域，负面提示词和生成参数保持原有完整导入能力；导入确认文案同步说明实际覆盖范围。
- Agent 增加严格字段分流：画师、媒介、渲染和风格进入基础画风，整图场景、动作和构图进入变量区域，角色外貌、服装、身份 Tag 与角色专属负面词进入角色专属提示词。
- 将“多角色管理”明确改名为“角色专属提示词（单／多角色）”；即使只有一个角色，Agent 也必须使用角色专属工具，除非用户明确要求写入变量框。
- 增加单角色字段隔离回归测试，确认 Agent 设置角色不会覆盖变量提示词。TypeScript、25 项网关测试与 `git diff --check` 通过。

### 02:22 — 生成结果立即显示，不再等待历史保存

- 修复连续生图时预览仍优先显示上一张历史图片的问题：NovelAI 返回新图后立即切换到“刚刚生成”状态，不再等待本地历史写入完成才更新预览。
- 手机端在新图返回时立即打开结果大图；历史保存完成后无感切换到电脑本地原图地址，若用户已经关闭大图则不会再次强制打开。
- 在提交几 MB 的 Base64 原图到电脑历史前主动让出一次浏览器绘制机会，避免 JSON 序列化和局域网上传阻塞首帧显示。
- 将“生成成功”和“历史保存成功”拆开处理：本地历史偶发保存失败时保留并显示已生成图片，只提示历史保存失败，不再误报整次生图失败。

### 01:12 — 新增 NovelAI V4.5 Precise / Character Reference

- 参考 NovelAI 官方 Precise Reference 文档、当前官网请求协议和 st-chatu8 2.7.8 的可读实现，在实验室“角色”页新增电脑本地角色参考资料库；支持搜索、上传、SHA-256 去重、重命名、默认参数、详情、归档与恢复。
- 支持官方 `角色`、`画风`、`角色与画风` 三种类型和最多 4 张参考图；每张独立设置 Strength 与 Fidelity，高级数值限制为 -1.00～2.00，Information Extracted 保持 0～1，Fidelity 按官方协议转换为 `1 - fidelity` 后发送。
- 电脑网关在生图前按本地资产 ID 读取原图，自动纠正旋转方向，按比例放入最接近的 1024×1536、1472×1472 或 1536×1024 官方画布，并只向 NovelAI 发送受信任的 `director_reference_*` 字段；浏览器不能伪造原图或官方字段。
- 每张参考图、每张生成图固定增加 5 Anlas；生成按钮和项目确认面板显示费用明细，NovelAI 成功返回后才扣除本地点数预算。角色参考只允许 V4.5 Full，并与 Vibe Transfer 双向互斥。
- 手机列表继续使用电脑缩略图，详情与生图原图留在电脑；画师串、灵感和历史随完整参数保存参考 ID、名称、类型和数值快照，ParamsViewer 可以复查当次参考及费用。
- 项目 Agent 新增角色参考搜索、从项目历史保存参考图和设置参考组合工具；只接受项目内部资产 ID，最多 4 张，并遵守 Vibe 互斥及统一付费生成确认，未开放任意文件或网址读取。
- 增加角色参考字段清理、类型映射、画布选择、负值与增强值、Fidelity 转换、Agent 资产边界和 Vibe 互斥测试。24 项网关测试、TypeScript 检查、完整前端与 Worker 构建及 `git diff --check` 通过；未请求真实 NovelAI、未消耗 Anlas。

## 2026-07-22

### 23:31 — 修复 Agent 第五轮断线、OAuth 和大数据边界问题

- 增加任务轮询接管：刷新页面或手机重新连接后，面板会恢复运行状态、允许停止/追加要求，并在任务结束后重新载入会话消息；响应连接关闭时只取消悬挂确认，不会中止电脑端普通任务。
- OAuth 登录流程在电脑端保留临时 flow、PKCE/state 和多步提示，后续输入继续同一流程；授权链接、设备码和说明文字现在会完整显示。
- 清空对话接口增加运行锁，不能在 Agent 后台工作时清除；工具调用为空文本的历史消息也会保留。
- 项目搜索和概况改用 Worker 轻量接口，只传输必要字段与统计数据，不再先加载灵感、画师资料和画师串的 Base64 封面。
- 任务事件中的确认请求编号脱敏，连续文本增量合并并将回放容量提升到 500 条/单段 100,000 字符，减少长任务丢失关键过程。
- 频率限制只统计通过凭据和模型校验的有效任务请求，避免错误配置消耗额度。
- 完整构建、TypeScript 检查、17 项网关测试及 `git diff --check` 通过，未调用真实 LLM、NovelAI 或消耗 Anlas。

### 23:13 — 修复 Agent 第四轮事务、断线和大数据量问题

- 实验室修改改为整轮事务：Agent 在隔离草稿中顺序工作，成功结束且编辑器版本未变化时才一次性应用；用户在后台运行期间的手动修改不会被中途工具或最终草稿覆盖，失败任务也不会留下半套修改。
- 会话启动阶段纳入修改和删除锁；历史清理要求 `days` 与 `keepCount` 必须且只能填写一个，缺少参数不再默认清空全部历史。
- 浏览器刷新、手机切换网络或连接断开不再中止电脑端普通 Agent；若断线时正等待确认，只取消该确认并让任务明确失败，避免在后台挂到超时。
- 预设和 Vibe 搜索改为查询完整电脑资料库，Vibe应用会按ID补查资产，不再受前端前200条上下文限制。
- 新增轻量项目概况接口，由数据库直接统计画师串、角色、灵感、画师资料、Vibe和组合数量，只返回最近5条无图片历史；资料搜索只扫描必要文本字段，不再序列化 Base64 图片。
- 连续文本与思考增量在任务事件文件中合并，保留工具、确认和排队事件；Steering/Follow-up 也进入回放记录。
- 支持同一 pi Provider 在 API Key 与 OAuth 之间明确选择登录方式，OAuth-only Provider继续直接进入OAuth流程。
- 完整构建、TypeScript 检查、17 项网关测试及 `git diff --check` 通过，未调用真实 LLM、NovelAI 或消耗 Anlas。

### 22:55 — 修复 Agent 第三轮并发、回放与后台任务问题

- 为 Agent 启动阶段增加会话级占位和全局并发计数，两个同时到达的请求不会再启动同一会话的重复 Agent。
- 将工具执行改为顺序模式，避免多个实验室或项目写入工具并行修改同一份草稿和资料。
- 修复主题、安全模式指令只被记录但不生效的问题；Agent 修改后会同步页面状态、浏览器本地偏好和安全模式图片显示。
- 任务恢复现在会根据事件记录重建文本增量、工具开始/完成和操作事件，不再只显示事件数量；事件文件增加串行写入队列，避免快速事件互相覆盖。
- 清空或删除 Agent 会话时同步清理任务状态和事件文件，避免旧的“中断任务”提示重新出现。
- 灵感和画师资料工具查找历史图片时优先使用单条历史接口，旧于最近100条的图片也可以被引用。
- 将 Agent 请求体上限调整为 48 MB，并在前端按 Base64 后大小限制附件组合，四张合法图片不会因 JSON 额外开销随机失败。
- AITag 索引/暂停/继续、收藏以及 Tag 词库更新纳入项目确认面板；画师基准图配置增加字段白名单、数量、尺寸、步数和文本长度校验。
- 完整构建、TypeScript 检查、17 项网关测试及 `git diff --check` 通过，未调用真实 LLM、NovelAI 或消耗 Anlas。

### 22:36 — 修复 Agent 第二轮审计问题

- 危险项目操作现在必须同时持有当前会话、确认请求和已批准状态的短期令牌；确认前直接调用接口会被拒绝，项目执行失败也会清理令牌并唤醒等待中的 Agent，不能绕过确认或卡死会话。
- `request_generation` 改为真正阻塞在确认握手上：只有用户完成费用确认并且生图成功后 Agent 才会收到成功结果；取消、缺少 NovelAI Key 或生成失败都会返回失败，不再提前继续对话。
- 附件入口根据当前模型的识图能力自动禁用，服务端再次校验模型能力；同时支持 pi 内置 OAuth Provider（包括授权链接、设备码和手动代码提示），凭据仍以加密形式保存在电脑。
- 实验室 Agent 结束时增加版本保护：用户在 Agent 后台运行期间手动修改内容时，不会被最终草稿静默覆盖；清空 Agent 对话增加项目内二次确认。
- 任务新增最近 200 条事件回放记录，断线或重启后重新打开会话可以看到中断提示与事件数量；历史原图检查优先走单条接口，找不到旧图时才使用受限分页回退。
- 增加全局 Agent 并发上限（最多 3 个）和一分钟频率限制（最多 12 次），避免误操作造成资源和模型费用异常。
- 更新 README 与本日志；TypeScript、完整构建、17 项网关测试及 `git diff --check` 通过，未调用真实 LLM、NovelAI 或消耗 Anlas。

### 21:28 — 修复 Agent 确认、恢复、上下文和附件问题

- 将删除、清空历史、Vibe 编码、清理历史和手机缓存等危险/付费工具改为真正的确认握手：Agent 会在工具处等待，前端确认并完成项目操作后才收到成功结果；取消、过期或操作失败不会再被模型误认为已完成。
- 修复旧版会话迁移只执行一次的问题：每个旧画师串/实验室会话通过 `legacySourceId` 独立迁移，不会因为已经存在新的全局会话而漏掉后续旧对话。
- 增加上下文预算裁剪与消息体限长，按模型上下文窗口保留最近有效上下文；会话重新打开时恢复 Thinking、工具调用名称、参数和执行状态，减少长时间使用造成的上下文和本地文件膨胀。
- 增加电脑端 Agent 任务状态文件：记录运行、完成、失败、中止和重启后中断状态；手机或浏览器连接短暂断开时不再因响应关闭而强制中止电脑后台任务，重新进入后可以看到中断状态并继续重试。
- 允许 Pi 独立工具调用并行执行；危险操作仍需确认，生图、项目数据写入和其他变更不再提前假设成功。直接附加图片入口支持最多4张、单张6MB以内的 PNG/JPEG/WebP/GIF，图片只进入本次模型上下文并在保存时剔除。
- 复制回答增加局域网/移动浏览器降级方案和“已复制”反馈；系统提示词明确把项目中的名称、Prompt、Tag、AITag描述和历史文本视为不可信资料，避免资料内容注入 Agent 指令。
- 完整构建、TypeScript 检查、17 项网关测试及 `git diff --check` 通过；没有真实调用 LLM、NovelAI 或消耗 Anlas。

### 20:56 — 将项目 Agent 补全为 Pi 多会话工作台

- 将原来绑定画师串或实验室 ID 的单条对话改为电脑端全局多会话：新增会话列表、新建、切换、标题搜索、自动标题、手动重命名、删除和最近使用时间；旧版对话首次打开时自动迁入，手机与电脑可以继续同一会话。
- 接入 Pi Agent Core 原生 Steering、Follow-up、队列清理与 Abort：Agent 工作期间输入框继续可用，用户可以选择纠正当前任务或排到本轮之后；停止按钮现在会真正中止电脑后台 Agent，关闭面板则允许任务继续完成。
- 每个会话独立保存供应商、模型与思考等级，面板顶部可直接切换已经登录的模型服务；支持 `off` 到 `max` 的思考档位，并根据模型推理能力自动限制无效设置。
- 增加消息级操作：所有消息可以复制，用户旧消息可以编辑并从该处重新执行，最后一条回答可以重新生成；停止或模型错误产生的上下文也会先保存，便于修正后重试。
- 补齐 Agent 运行信息：流式展示可折叠的 Thinking，每条回复记录模型、Token、文本模型费用和停止原因；工具调用按步骤显示运行、成功或失败，并可展开检查输入与返回结果。
- 会话文件升级为带元数据的电脑本地结构，保留最多 48 条底层上下文且继续剔除临时原图；新增会话管理、消息修订与运行控制接口，全部沿用局域网密码认证和现有删除/付费确认边界。
- README 已同步完整工作台能力。完整构建、TypeScript 检查、17 项网关测试及 `git diff --check` 通过；没有真实调用 LLM、NovelAI 或消耗 Anlas。

### 18:55 — 停止 AITag 隐藏页面的无限首图轮询

- 修复 AITag 采用 Keep-Alive 后，即使已经切换到其他页面仍不断请求 `/api/aitag/cache/first-images`、导致启动窗口持续刷日志的问题；只有 AITag 当前处于活动页面且浏览器可见时才允许检查首图缓存。
- 离开 AITag、切换浏览器标签或最小化页面时，会立即中止当前首图检查、同步定时器和详情缓存轮询；电脑端已经开始的实际图片缓存任务不受影响，重新进入 AITag 后按需恢复检查。
- 首图长时间没有新进展时按 1、2、4、8、12、12 秒逐级退避，连续六轮无结果后刷新一次本地状态并停止，不再以约一秒间隔无限查询；新增图片完成时会重置退避并继续处理剩余项目。
- 完整构建、TypeScript检查、16项网关测试及`git diff --check`通过。

### 18:36 — 将 AI Agent 扩展为全项目业务 Agent

- 修复 Agent 不能查看最后一张生成图的问题：新增生成历史列表与原图检查工具，电脑会从本地历史存储直接把真实图片和完整 Prompt、负面 Prompt、参数交给支持视觉输入的模型；手机只发送指令，不重复下载原图。当前模型不支持识图时明确阻止并提示切换，不再假装已经看图或要求用户重新描述项目中已有内容。
- 将工具范围从实验室扩展到项目业务数据：可以读取项目概况，搜索、创建和更新画师串、角色串、灵感与画师资料，检索 AITag 作品及生成参数，读取、重命名、归档和恢复永久 Vibe，并新建或更新 Vibe 组合。项目修改完成后会刷新相关电脑端缓存与历史页面。
- 新增真正的全局项目入口：桌面侧栏和手机资源库可直接打开项目Agent，不必先找到某个提示词输入框；全局入口进入常驻实验室承载对话，完成任务后Agent可以切换到画师串、角色库、画师Tag、AITag、灵感、历史或实验室页面。
- 补齐项目设置与维护工具：Agent可以读取并调整Anlas预算、多人拼车队列、画师基准图，检查或启动Tag词库更新，管理AITag收藏及索引状态，并调整当前设备的主题、安全模式、手机图片布局和小图缓存上限。清空手机缓存仍需项目确认框。
- 保留明确的安全边界：Agent 仍没有 Windows、任意文件、Shell、系统进程、浏览器或任意网址权限；所有内部数据请求转发当前局域网认证 Cookie。删除画师串、角色、灵感、历史、画师资料或 Vibe 组合，清空历史、付费 Vibe 编码和 NovelAI 生图都必须先经过项目确认框。
- 修复 Agent 运行时丢失未列入工具表的高级生成参数的问题；实验室写回现在保留用户未要求修改的原字段，原有“撤销本次”快照继续可用。
- 重做 Agent 对话显示：安全渲染标题、粗体、列表、代码和 HTTPS链接，工具进度收纳到对应回答下方并区分执行中、完成和失败，不再用独立气泡遮挡对话。流式输出自动跟随底部，用户主动上滑后暂停并显示“回到底部”。模型标题直接标明是否支持识图。
- 会话持久化主动剔除临时图片内容，避免历史原图被重复写入 Agent 对话文件。新增真实图片结果、非视觉模型阻断和危险工具只请求确认的自动测试；完整构建与14项网关测试通过，未真实调用 LLM、NovelAI或消耗 Anlas。

### 17:52 — 按 pi 官方逻辑重做 AI 配置

- 撤掉供应商、模型和 API Key挤在同一区域的临时配置表单，按 pi 官方 `/login`、`/model`、`/logout` 三段式逻辑重做：登录与退出按供应商独立管理，当前模型作为单独选择，不再互相绑定。
- 供应商目录直接读取 pi-ai 内置 Provider，不再只硬编码 DeepSeek、Gemini、Grok和OpenRouter；登录列表支持模糊搜索、已配置状态和模型数量，旧版已经加密保存的密钥可以直接迁移使用。
- 模型选择只展示已配置供应商的可用模型，并支持按名称、模型ID和供应商统一搜索；列表标出当前模型、上下文长度、推理与图片输入能力，切换后立即成为 Agent默认模型。
- 新增适配桌面和手机的全屏供应商、模型选择器，保留电脑端 AES加密凭据、局域网认证和Agent受限工具；API Key不会进入浏览器存储，退出单个服务不会影响其他模型服务或历史对话。

### 17:24 — 接入 pi.dev AI 生图 Agent

- 生图实验室右上角新增星光 Agent 入口，使用 pi.dev 的轻量 Agent 核心接入 DeepSeek、Google Gemini、xAI / Grok 与 OpenRouter；Agent 可以直接修改基础画风、主体、负面提示词、提示词模块、多角色、生成参数和永久 Vibe，不再要求用户把对话结果手动复制到实验室。
- 为 Agent 提供严格受限的结构化工具，只允许读取当前实验室、搜索本地 Tag/预设/Vibe并修改生图草稿；未开放电脑文件、命令行、浏览器或任意网络工具。Vibe ID与编码必须来自电脑现有资料库，一次最多选择4个。
- 新增桌面与手机共用的全屏 Agent 面板，支持流式回答、工具执行状态、快捷指令、停止任务、电脑端对话持久化和“撤销本次 Agent 修改”；用户明确要求出图时才请求生成，付费生成继续使用项目内 Anlas 二次确认面板。
- 全局设置新增“AI 生图 Agent”区域，可选择供应商与模型；LLM API Key 使用 AES-256-GCM 加密保存在电脑 `local-data`，不会写入浏览器存储或回传给手机，配置接口只返回是否已配置。
- Agent运行在电脑图片网关，并复用启动器检测到的电脑系统代理；手机仅连接电脑，不需要为 Gemini等模型单独配置网络。会话、密钥与工具执行都受现有四位局域网认证保护。补充未认证阻断、密钥密文隔离、Vibe工具边界与网关无密钥错误测试，全程未调用真实 LLM 或 NovelAI、未消耗 API 额度与 Anlas。

### 16:26 — 让绿紫动态反馈覆盖完整队列流程

- 修正对“排队中”的狭义处理：从准备加入、连接队列、等待位置、获得许可到 NovelAI 正在生成，整个公共队列任务期间持续显示同一套绿紫流动渐变与扫光，不再在获得许可后错误切回普通紫色。
- 加载圆圈从按钮最左端移回中央状态文字旁边，并在文字右侧加入等宽透明平衡位；圆圈与文案形成一个整体，同时文案仍以整个按钮为基准严格居中。
- 桌面整行生成按钮、手机紧凑胶囊以及离开编辑器后的备用提示同步采用新的完整流程状态，成功和失败的停留时间保持不变。

### 16:18 — 补全核心页面服务的启动反馈

- 排查启动窗口停在“Tag 更新服务”后的现象，确认该位置是 Wrangler 核心页面服务尚未输出的等待空窗，并非图片网关或 Tag 服务卡死；无残留端口和项目后台进程，重要数据未受影响。
- 启动器现在会明确显示“核心页面服务正在启动”与“已就绪”，等待较久时按阶段提示已经等待的秒数，不再让静止终端看起来像无响应。
- 核心服务等待时间统一为明确的 45 秒上限，超时后给出可操作的中文错误提示；完成真实启动、3000/3001接口访问和整棵进程关闭验证。

### 16:10 — 增强排队动画并延长结果状态

- 排队状态的绿紫背景改为更快、更明显的连续流动，并增加一层柔和扫光；用户可以直接看出按钮正在排队，而不是误以为静止渐变。
- 成功状态固定显示 5 秒，失败状态固定显示 8 秒，为确认结果和阅读错误信息留出足够时间；新任务开始时会安全取消旧状态的延时清理，避免旧计时器误删新任务提示。
- 修复实验室在生成结束瞬间提前换回普通生成按钮的问题，桌面和手机现在都会完整显示成功或失败状态后再恢复。

### 16:00 — 重做公共队列按钮的居中与四态反馈

- 公共队列状态收敛为正常、排队、成功、失败四种视觉语言：普通过程保持原有靛紫渐变，等待队列使用绿紫缓慢流动渐变，完成使用翠绿渐变，错误使用玫红至深红渐变，不再为每个内部阶段分配不同颜色。
- 为渐变增加柔和高光、内描边和对应色阴影，使状态具有层次但避免纯色块或高频闪烁；系统开启“减少动态效果”时自动停止排队动画。
- 状态文字改为以整个按钮为基准严格居中，左侧加载图标和右侧取消按钮独立定位；桌面整行按钮、手机紧凑胶囊和离开编辑器后的备用提示共用同一套布局与配色。

### 15:34 — 将公共队列状态并入生成按钮

- 生图实验室与画师串编辑器不再在右下角重复悬浮公共队列提示；桌面端原有的整行生成按钮会原地切换为加入队列、前方任务数、当前使用者个性语、获得许可和生成状态。
- 排队阶段的取消操作保留在同一状态栏右侧，状态与操作来源保持一致，不再遮挡预览图、历史页码或图片操作。
- 手机端沿用原来的拇指生成按钮位置，并在排队时将该按钮替换为紧凑状态胶囊，不额外叠加第二个悬浮层。
- 只有离开实验室或编辑器、后台任务仍在继续时，才显示全局备用状态提示，避免切页后失去排队进度与取消入口。
- 使用桌面真实页面确认生成按钮保持在预览区上方、宽度与下方预览框对齐；完整构建与 TypeScript 检查通过，验证未触发生图或占用真实公共队列。

### 15:15 — 修复本地服务启动超时与 Wrangler 幽灵后台

- 排查确认启动超时并非图片网关或公共队列初始化造成，而是 Windows 旧启动链路通过 `cmd → wrangler.cmd → Wrangler → workerd` 多层派生进程；启动失败或关闭窗口时只结束外层，遗留的 Wrangler/Miniflare 长期占用内部端口和 `local-data` 资源。
- 清理八个确认属于本项目、且未提供 `3000/3001` 服务的历史残留进程；没有触碰其他 Node 程序、VPN、ChatGPT/Codex或任何本地数据。
- 启动器改为携带 Wrangler 官方 Node 参数直接启动实际 CLI，去除多余的 CMD 包装层；启动失败、Ctrl+C和终止信号现在统一关闭图片网关、Tag 服务并清理 Wrangler 整棵进程树。
- 内部服务就绪探测增加单次请求超时，避免某次半连接永久卡住整个启动流程。
- 在端口完全空闲的真实环境完成“启动 → 检查 `3000/3001/3002` → 访问首页与认证接口 → 关闭”回归：三个服务均正常就绪、HTTP 返回 200，关闭后项目 Node/workerd 残留数和占用端口均为 0。

### 05:32 — 修复公共队列后 NovelAI 生图误报 VPN 异常

- 排查确认 Windows 系统代理与 VPN 正常，浏览器可以访问 NovelAI；故障是公共队列请求使用了电脑代理通道，而获得队列许可后的 NovelAI 生图仍误走 Node 直连，直连超时后被笼统显示为 VPN 异常。
- 普通生图、永久 Vibe 生图及缓存失效重试现在统一复用电脑网关检测到的系统代理，不再出现“队列可以连接、真正生图却无法连接”的网络分叉。
- 付费 Vibe 编码请求同步改走相同代理通道，保证手机局域网和电脑端使用一致的电脑 VPN 环境。
- 通过不带 API Key、不触发生图的只读请求验证：Node 直连 NovelAI 超时，使用 Windows 当前 `127.0.0.1:7897` 系统代理后连接正常；新增代理传输回归测试，验证过程未消耗 Anlas。

### 05:23 — 新增与 st-chatu8 兼容的多人拼车公共队列

- 在全局设置的 NovelAI 连接区域新增“多人拼车公共队列”，相同 NovelAI Key 使用与 st-chatu8 一致的 SHA-256 指纹进入同一公共队列，不增加私有房间模式。
- 队列协调统一放在电脑生图网关，覆盖实验室、画师串、角色预览、画师 Tag 基准图以及手机局域网生图；手机无需单独开启 VPN，也不会直接连接 NovelAI。
- 公共服务只接收 Key 指纹、随机客户端 ID、任务 ID 和最多15字个性语，不发送原始 Key、Prompt、图片、Vibe 编码或生成结果；真正的生图请求仍由电脑直连 NovelAI。
- 新增全局紧凑状态条，显示加入队列、前方任务数、当前使用者个性语、获得许可和生成状态；等待阶段可以主动取消，并在成功、失败、取消或断开时释放队列位置。
- 队列设置写入电脑 `local-data`，电脑和手机统一生效；公共服务连续失败时明确停止本次生成，不会静默绕过队列造成共享账号并发。
- 使用本地模拟服务验证 Key 哈希兼容、敏感数据隔离、轮询取锁和完成释放；验证过程没有调用作者的入队接口、没有占用公共队列，也没有调用 NovelAI 生图或消耗 Anlas。

### 04:52 — 修复项目无法连接 AITag

- 排查确认 aitag.win 接口仍正常，故障来自电脑当前 DNS 将 AITag 域名解析到证书不匹配的错误服务器；浏览器因使用 Windows 系统代理而不受影响，项目内部 Worker 直连则持续超时并返回 500/502。
- 本地启动器现在自动读取环境变量或 Windows 当前启用的系统代理，并让 AITag API、作品详情及远程图片统一通过电脑图片网关访问；不要求手机单独开启代理或 VPN。
- 保留现有本地索引、搜索、月榜、收藏、作品详情、R2 图片缓存和手机缩略图链路，远端恢复后会自动重试先前标记失败的图片。
- 新增仅限本机内部服务调用的 AITag 转发入口，使用局域网密钥验证并严格限制为已知 AITag API 与图片域名，避免成为任意网络代理。
- 实际验证最新列表返回 60 条、远端总数约 255 万条，作品详情与三张图片元数据正常，首图成功写入本地资料库并生成 320px WebP 缩略图。

### 03:54 — 新增本地 Anlas 点数预算与自动计费

- 新增电脑与手机共用的本地 Anlas 预算，首次默认 1666 点；桌面侧栏直接显示剩余点数，全局设置支持手动校准和恢复默认值，并明确区分本地预算与 NovelAI 官网实时余额。
- 根据 NovelAI V4/V4.5 当前计价公式计算普通生图费用，并按 Opus 单张、1024×1024 像素以内且不超过 28 Steps 的免费条件显示“免费”或预计点数。
- 生图按钮在提交前展示预计消耗；只有 NovelAI 成功返回图片后才扣除本地预算，失败请求不扣，并通过电脑网关即时同步所有已打开页面。
- 永久 Vibe 的每个新 Information Extracted 编码成功后扣除 2 点，确认面板同时显示扣除前后预算；重复编码、已有编码、文件导入和恢复付费编码均不重复扣点。
- 计费器兼容 SMEA、图生图强度、多张生成、第 5 个起的额外 Vibe 和 Precise Reference 附加费用；当前项目最多启用 4 个 Vibe，因此日常复用已编码 Vibe 不额外扣费。
- 新增费用规则自动测试并完成完整构建；验证过程未调用真实 NovelAI 生图或编码接口，未消耗 Anlas。

## 2026-07-21

### 18:06 — 对齐 NovelAI 官方 V4.5 永久 Vibe 生图协议

- 根据 NovelAI 官方网页和最新版 st-chatu8 的可读实现重新核实永久 Vibe 数据流，电脑网关改用官方 `reference_image_multiple_cached` 结构发送已编码 Vibe，不再把永久编码误放进原图字段。
- 为每份编码使用进程级随机密钥生成不可推测且稳定的 HMAC 缓存键：首次请求附带编码，后续请求复用 NovelAI 服务端缓存；缓存失效时识别 `INVALID_CACHE_KEYS` 并自动补回编码重试一次。
- 新增 128 MB 有界 LRU 编码内存缓存和并发读取合并，减少连续生图时对电脑本地数据服务的重复读取，同时不缓存 Vibe 原图。
- 生图前统一清理 V4.5 不兼容的旧参考字段，验证编码完整性、模型、数量与强度数组，并在本地归一化后明确关闭服务端二次归一化。
- 新增电脑网关自动测试，覆盖强度归一化、缓存包装、稳定缓存键、并发读取合并、LRU 淘汰和 NovelAI 缓存失效响应识别；测试未调用真实 NovelAI 接口、未消耗 Anlas。

## 2026-07-18

### 04:34 — 新增永久 Vibe Transfer 资料库与多 Vibe 生图

- 在实验室“全局”页新增紧凑 Vibe Transfer 状态模块和桌面/手机共用的全屏管理面板，提供资料库搜索、上传、官方文件导入导出、详情、重命名、默认强度、归档与恢复。
- 接入 NovelAI V4.5 Full `/ai/encode-vibe`：每个新的 Information Extracted 编码前固定显示 2 Anlas 项目确认面板，已存在编码直接复用，日常生图不再调用付费编码接口。
- 支持同一 Vibe 保存多个提取量变体，一次选择 1～4 个 Vibe，独立设置 Strength、拖动或按钮排序，并在总强度超过 1 时按比例归一化和展示实际发送强度。
- 新增命名组合的保存、载入、覆盖、重命名和删除；画师串与历史随完整参数保存独立 Vibe 快照，后续修改资料库组合不会改变旧配置。
- 新增本地 D1 元数据表与 R2 持久文件目录，原图、独立 WebP 缩略图、永久编码和组合均保存到 `local-data`，浏览器缓存清空不会丢失。
- 支持完整、仅编码和多变体 `.naiv4vibe` 导入，保留未知模型编码；导出包含原图、独立缩略图、模型编码、参数哈希与导入信息。
- 电脑图片网关在生图前按本地 ID 读取编码，只向 NovelAI 发送编码与强度；手机不下载 Vibe 原图或编码，并继续复用局域网密码、电脑 VPN、缩略图与安全模式。
- 为相同图片、模型和提取量增加 SHA-256 去重与并发任务合并；付费编码返回后若暂时无法入库，会写入 `local-data/vibe-recovery` 并在重启后自动恢复，避免重复扣费。
- 历史与灵感参数详情新增 Vibe 名称、提取量、原始强度、实际强度和来源组合展示；缺失或模型不匹配的编码会在生图前明确阻止请求。
- 使用隔离临时数据完成 Vibe 建库、导入、导出、组合、归档与恢复接口回归，并验证 390×844 手机布局、桌面布局和 Android 返回键层级；测试过程未调用真实 NovelAI 编码接口，未消耗 Anlas。

## 2026-07-17

### 18:50 — 按项目信息架构全面重写 README

- README 改为带项目图标、状态徽章、章节导航、对照表、功能地图和 Mermaid 流程图的完整项目主页，避免连续堆叠功能清单。
- 将核心创作工作流、Tag 与资源资料库、手机局域网、手机 UI、本地图片存储、安全隐私、备份恢复、日常启动和技术架构拆分为独立一级章节。
- 手机局域网章节单独说明四位密码、30 天授权、错误锁定、电脑 VPN 请求链路和 3000/3001/3002 端口职责；手机 UI 章节单独说明导航、触控规范、页面布局和图片显示偏好。
- 为画师串、实验室、预设引用、历史、灵感、Tag 补全、画师 Tag、角色库和 AITag 分别建立清晰功能块，并补充数据边界、GitHub 与真实数据备份的区别及使用限制。

### 18:38 — 将 AITag 提升为 README 独立重点章节

- README 新增完整 AITag 专章，说明 aitag.win/Pixiv AI 作品检索、Prompt 搜索、NAI/SD/ComfyUI 筛选、最新/月榜与月份目录。
- 补充本地渐进索引、首图/完整缓存分级、收藏筛选、远程故障时离线回退，以及桌面双栏和手机全屏详情行为。
- 明确 AITag 元数据识别与“导入实验室、保存画师串、收藏灵感”三条参数复用路径，并在功能对照、页面说明和来源列表中单独列出 AITag。

### 18:30 — 重写 README 并突出相对原项目的改造

- README 改为以“基于原项目的个人本地改造版”为主线，开篇加入原项目思路与当前版本的对照表，明确独立维护关系与 MIT 许可延续。
- 按个人化、本地数据、局域网图片服务器、手机 UI、中文 Tag、画师 Tag、角色库、实验室、安全模式、AITag、历史/灵感和统一交互 12 个主题详细说明当前改造。
- 删除或修正旧文档中多用户平台、云端图库和公开 Cloudflare 部署等已不符合当前版本定位的内容，补充日常启动、手机访问、数据备份、缓存边界和第三方资源来源。

### 04:48 — 安全模式不再模糊品牌图标

- 为桌面侧栏左上角的应用调色盘 Logo 增加安全模式忽略标记；安全模式仍模糊内容图片，但不再隐藏品牌标识。

### 04:44 — 改用 Microsoft 官方高清调色盘 Emoji

- 放弃从浏览器截图裁切的低分辨率素材，改用 Microsoft Fluent Emoji 官方仓库的 `Artist palette` 3D 透明 PNG；网页标签、桌面侧栏和桌面快捷方式统一使用该完整原图。
- 从官方 256×256 PNG 生成带透明留白的多尺寸 ICO，兼顾 Windows 快捷方式箭头与小图标清晰度；更换图标路径以绕过旧缓存。
- 新增第三方资源来源与 MIT 许可说明，便于后续追溯和更新。

### 04:35 — 缩小统一图标并增加透明留白

- 将调色盘主体缩小到图标画布约 60%，四周增加透明边距，避免桌面快捷方式箭头和浏览器、侧栏的小尺寸图标裁切主体。
- 网页标签、桌面侧栏与桌面快捷方式统一切换到新版图标路径，同时刷新 Windows 图标显示缓存。

### 04:29 — 统一应用与桌面启动器图标

- 从现有浏览器标签的调色盘图标生成统一的 `app-icon.ico`，网页标签和桌面侧栏品牌区改为读取同一图标资源，替换原来的紫色“N”。
- 桌面新增同名启动快捷方式并使用调色盘图标；原 `NaiPromptManager.bat` 继续作为实际启动脚本保留并设为隐藏，双击快捷方式的启动行为不变。

## 2026-07-16

### 20:54 — 移除快速导入开关外层圆环

- 去掉引用预设中快速导入开关的圆形外层焦点环和外层视觉样式，只保留紧凑的横向开关轨道与滑块，44px 触控区域不变。

### 19:23 — 将预设来源统一放到板块标题旁

- 模块与多角色管理的来源从内容卡片内部移到板块标题旁；混合引用多个预设时在标题后汇总显示多个来源，并继续保留各自的“已修改”状态。
- 基础画风、主体提示词、全局负面提示词和参数标题统一采用相同的来源徽标位置；手机端允许标题与来源自然换行，操作按钮保持独立对齐。

### 18:36 — 按区域追踪引用预设来源

- 移除固定显示在基础画风旁的单一预设标签；基础画风、主体提示词、全局负面提示词和参数现在只显示各自实际引用的来源。
- 引用的模块和角色按条目记录来源，追加不同预设时可以同时显示各自来源；替换只更新被替换部分，不覆盖其他区域的来源。
- 手动编辑已引用的内容后，对应来源标记为“已修改”；切换串、重置或导入完整图片/JSON 配置时会清理不再适用的来源状态。

### 17:50 — 将引用预设提升为编辑器全局操作

- 将“引用”从基础画风旁移至编辑器右上角，避免误解为只能引用画师串；使用双引号图标替代文字，并保留悬浮提示与无障碍标签。
- 生图实验室右上角的引用、保存到库和重置统一为 48px 方形按钮，按“引用 → 保存 → 重置”排列；基础画风区域只保留直接导入和粘贴操作。

### 03:55 — 重做引用预设快速导入开关

- 修复手机端开关被统一触控尺寸撑成大圆的问题；保留 44px 触控区，将实际开关轨道和滑块恢复为紧凑、清晰的视觉尺寸。

### 03:43 — 修复手机端引用预设弹窗布局

- 引用预设弹窗在手机端改为标题、关闭按钮、导入模式和类型切换分层排列，避免控件挤压、文字竖排和开关重叠。
- 弹窗内容区改为更紧凑的自适应卡片网格，并在弹窗打开时隐藏底部悬浮生成按钮，避免遮挡弹窗。

### 03:23 — 优化手机资源库、补全菜单与实验室输入体验

- 手机资源库四宫格调整为左上画师 Tag、右上角色库、左下 AITag、右下灵感库。
- 历史页面底部将页码切换移到统计文字上方，浏览完列表后更容易操作翻页。
- Tag 补全下拉菜单支持在整个列表区域上下滑动，改为双击候选后插入，降低滑动误触；键盘弹出时隐藏悬浮生成按钮和预览圆图，避免遮挡输入区域。
- 画师串手机卡片隐藏占用空间的复制浮层，保留底部单独的爱心收藏按钮并校正其位置；桌面端复制入口保持不变。

### 02:35 — 微调手机端按钮与卡片细节

- 保存到库按钮扩大触控区域并确保收纳图标在按钮内真正居中。
- 历史页面手机顶栏按钮改为靠右排列；跳页面板将“尾页”与“跳转”调整为首页同色与紫色主按钮的清晰顺序。
- Tag 词库的“词库来源”和“更新词库”操作改为靠左排列。
- 画师串收藏图标改为爱心，与名称水平对齐并压缩卡片底部多余空白。

### 01:55 — 精修手机图库、历史和实验室布局

- 画师串手机卡片改为读取图片真实宽高的双列瀑布流；手机端只保留名称，隐藏描述和更新时间，复制按钮改为纯图标，收藏按钮保持独立触控区。
- 移除手机实验室编辑区底部的空白预览占位，保留悬浮生成入口；保存到库按钮更换为收纳箭头图标，避免与复制图标混淆。
- 历史页面顶部改为“筛选、管理、刷新”工具栏，新增本地/局域网日期范围筛选，分页移动到列表底部并保留跳页抽屉。
- 词库设置拆分为“词库来源”和“更新词库”两个入口，来源按钮直达 ffdkj GitHub 原始仓库。
- 完成手机与 1440px 桌面回归、TypeScript 检查、完整构建和差异检查。

### 00:11 — 重做手机图库与生图实验室布局

- 新增手机图片列表全局偏好：瀑布流、竖向卡片、方形，以及自动/1/2/3 列；灵感、历史、画师串、角色库、画师 Tag 和 AITag 列表统一响应设置，详情与原图行为不变。
- 灵感库刷新按钮并入搜索工具栏；历史页面改为可配置图片布局，默认两列瀑布流，日期移到卡片下方，手机分页改为紧凑箭头。
- 手机生图实验室改为“全局、角色、参数”三个真正独立的面板；全局放基础画风、模块和负面提示词，角色放主体/变量与多角色管理，参数集中生成参数。
- 移除手机实验室底部固定栏和“画师串”徽标，生成改为悬浮按钮，生成完成后可直接查看结果；灵感库不再点亮实验室导航。
- 实验室常用按钮文案精简为“引用、导入、粘贴”，基础画风标题在手机端保持两行居中显示。
- 完成 TypeScript、完整构建、390px 手机视口与 1440px 桌面视口回归，确认无横向溢出。

## 2026-07-15

### 22:32 — 恢复独立灵感库

- 恢复桌面侧栏原有“灵感”入口和完整宽度的独立灵感库页面，不再将灵感图库挤入桌面实验室编辑区域。
- 手机端也移除实验室中的“灵感”页签，灵感库改为资源库二级面板中的独立页面；实验室恢复提示词、参数、预览三个专属页签。

### 22:04 — 精简手机导航并重组资源入口

- 删除手机普通页面重复的 Logo、页面名和安全模式顶部栏，内容区直接从搜索及操作工具栏开始；手机底栏改为“画师串、资源库、实验室、历史、设置”，并使用细紫色指示条标记当前入口。
- 新增资源库二级菜单，集中收纳画师 Tag、角色库和 AITag；移除原“更多”面板，历史和设置不再被隐藏，手机底栏层级更直接。
- 灵感图库并入生图实验室，手机实验室新增“灵感”第四页签，桌面实验室提供灵感/返回编辑切换；桌面侧栏不再单独展示灵感入口。
- 主题切换从手机菜单和桌面侧栏统一移入全局设置，新增浅色、深色、跟随系统三种选择；手机安全模式移入设置，桌面继续保留侧栏快捷开关。
- 手机滚动条改为透明轨道和低对比度半透明滑块；画师串卡片压缩封面高度，图片失败占位改为更轻量的单行重试提示。
- 完成 390×844 手机与 1440×900 桌面回归，确认资源库、实验室灵感、主题/安全设置、桌面导航和页面宽度正常，控制台无新增错误。

### 20:12 — 修复手机缩略图批量误报加载失败

- 修复手机滚动或切换页面时过早撤销缩略图对象地址，组件短暂复用失效地址后将正常图片误判为“加载失败”的问题；缩略图现在只在来源替换或组件真正卸载时释放。
- 修复缩略图请求刚被取消又立即重新进入页面时复用已中止请求的竞态，新请求不再被旧请求的清理流程误删或中止。
- 抽查历史页当前缩略图接口全部正常返回，并完成画师串与历史之间切换、快速切页及上下滚动离屏回归，均无失败卡片或永久加载状态。

### 19:46 — 完整重做手机端 UI

- 新增共用手机页面顶栏、工具栏、44px 图标按钮、底部抽屉和全屏详情组件，统一安全区、动态视口高度、遮罩关闭与 Android 返回键层级；手机顶部只保留安全模式，设置和主题统一收进“更多”。
- 画师串改为紧凑双列；角色库和画师 Tag 将搜索、抽卡、排序及低频工具重新分层；历史改为三列缩略图、单行分页、长按多选和详情内删除；灵感上传及详情改为手机全屏布局。
- AITag 手机端取消空白桌面详情栏，作品详情改为全屏并可返回恢复列表；桌面端从 1440px 起保持列表与详情双栏。
- 生图实验室和画师串编辑器取消手机双层顶栏，增加提示词、参数、预览三个页签和固定底部生成操作；补全候选会根据软键盘后的可用空间自动向上或向下展开。
- 全局设置在手机端改为全屏折叠页，项目确认框在手机端改为底部确认面板；所有手机按钮、输入框和选择框统一至少 44px 高，桌面布局保持原有行为。
- 完成 360×800、390×844、430×932、844×390 与 1440×900 视口回归，确认无横向溢出、返回层级正常，TypeScript、完整构建和差异检查通过。

### 18:10 — 修复手机局域网环境不支持 randomUUID

- 修复手机通过局域网 HTTP 访问时，生图成功后自动保存历史因 `crypto.randomUUID is not a function` 中断并显示红色错误的问题。
- 新增统一兼容 ID 生成器：安全上下文优先使用原生 `randomUUID`，普通局域网 HTTP 自动改用 `crypto.getRandomValues` 生成 UUID v4，并保留旧浏览器最终回退。
- 历史记录、画师串模块与角色、画师预览任务、灵感收藏、AITag 收藏和元数据导入全部改用同一生成器，避免其他手机操作再次触发相同错误。

### 17:56 — 防止重复启动本地服务

- 修复已有 NaiPromptManager 服务运行时再次双击桌面 BAT 会重复占用 `3000/3002`、显示 `EADDRINUSE` 并误报启动失败的问题。
- 启动器和本地服务脚本现在会先校验现有实例；确认项目已运行时直接打开 `localhost:3000` 并正常退出，不再创建第二套 Worker、图片网关或 Tag 更新服务。

### 17:12 — 手机生图改由电脑 VPN 转发

- 修复手机通过局域网访问时仍由手机浏览器直连 NovelAI、导致未在手机开启 VPN 就显示 `Failed to fetch` 的问题；前端现在只请求同源的 `/api/generate`。
- 本地图片网关新增受四位密码保护的 NovelAI 生图代理，请求链路改为“手机 → 电脑 → 电脑 VPN → NovelAI”；API Key 仅随单次请求转发，不写入电脑服务日志或配置。
- 生图代理使用电脑 Node 网络栈并流式返回 NovelAI ZIP，限制请求体大小与五分钟超时；电脑 VPN 不可用或连接超时时返回明确中文错误，不再统一显示模糊的网络失败。

### 16:52 — 修复图片网关与 Tag 更新服务端口冲突

- 修复内部 Worker 与 Tag 更新服务同时占用 `3001` 导致 `localhost:3000` 被错误转发、电脑首页只显示“仅允许本地 NaiPromptManager 页面访问”JSON 的问题；Tag 更新服务改用独立的 `3002` 端口。
- 启动检测改为校验 Worker 专属的局域网状态接口及响应结构，不再把其他返回普通 HTTP 状态的进程误判为 Worker 已启动。

### 16:46 — 将电脑改造成局域网图片服务器并优化手机图片缓存

- 启动流程新增本地图片网关：外部继续使用 `3000` 端口，Wrangler Worker 改为仅在本机 `3001` 端口运行；普通请求透明转发，四位局域网密码、错误次数限制和30天授权保持有效。
- 新增受保护的 `/api/media` 图片接口，手机列表按卡片尺寸请求 320px 或 640px、质量72的 WebP 小图；详情、下载与元数据读取继续使用原图，手机详情原图使用 `private, no-store`。
- 电脑按需生成缩略图并保存到独立的 `local-cache/thumbnails`，缓存上限1 GB，超过后按最近最少使用顺序清理到约900 MB；缓存与 `local-data` 重要数据分离，不参与备份或 Git。
- 画师串、角色库、画师tag、灵感、历史、aitag 和编辑器候选卡片统一使用可取消、可重试的懒加载组件；手机只预加载约一屏范围，离开视口后释放临时图片 URL，桌面继续直接使用原图路径。
- 手机使用局域网 HTTP 时浏览器不提供 Cache Storage，因此改用在该环境可用的 IndexedDB Blob 缓存实现同等行为；默认上限100 MB，可切换为关闭、25 MB、50 MB或100 MB，并在全局设置中查看用量和一键清空。
- 本地资源缓存头由 `public` 改为 `private`；图片网关限制来源、大小、超时和并发，并阻止未知域名、私有地址、文件协议及未授权设备访问。

### 03:12 — 新增手机局域网访问与移动端布局

- 本地服务改为监听家庭局域网，启动窗口自动列出手机访问地址与持久化四位密码；电脑本机通过 `localhost` 继续免密使用。
- 手机等其他设备首次访问显示独立密码页，验证成功后保持授权30天；连续输错5次暂停1分钟，所有数据接口、本地历史图片和缓存资源均受会话保护。
- 移动端改用4个常用入口加“更多”面板的底部导航，集中收纳 aitag、灵感、历史、设置、安全模式和主题，避免7个入口拥挤在一行。
- 角色库和画师tag在手机上固定为双列卡片，顶部显示当前页面，触控区域和安全区域适配手机；桌面侧栏及用户设置的桌面列数保持不变。
- 新增局域网访问说明；访问密码和签名密钥仅保存在 `local-data/lan-access.json`，不进入 Git，也不开放公网访问。

### 02:36 — 完善角色库智能搜索

- 角色搜索新增中文名、英文 Tag、作品名、括号变体和中文中间片段匹配，并支持“原神 胡桃”“碧蓝档案 泳装”这类多关键词组合查询。
- 搜索结果改为相关性优先，再按所选热度或名称顺序排列；卡片会标明匹配来自中文名、英文 Tag、作品/变体或多个字段。
- 新增 128 个按需加载的角色倒排索引分片及紧凑角色记录文件，避免搜索时扫描 98,966 条完整目录或请求大量分页；保留单字前缀搜索兼容。
- 词库格式升级至第 7 版；修复作品名使用 `()`、`（）`、`《》`、`【】` 等不同括号时识别和排序不一致的问题。

### 02:13 — 修复角色 Tag 的 Danbooru 跳转

- 跳转 Danbooru 前将角色 Tag 中用于显示和 NovelAI 输入的空格恢复为下划线，避免多单词角色被网站误判为多个独立 Tag 而显示无结果。
- 单词角色和带括号的角色变体继续使用同一套安全编码，不改变项目内角色 Tag 的复制、搜索或生成格式。

### 02:02 — 将角色串升级为角色库

- 将原“角色串”入口改造成角色库，收录 98,966 个带中文名称、英文 Tag 和作品热度的角色 Tag，支持中英文搜索、热度与名称排序、滚动加载、收藏和随机抽卡。
- 保留现有手工还原角色；“穆宁雪”这类没有官方 Tag 的角色继续保存完整外貌与服装提示词，并可单独筛选、编辑、复制或送入实验室。
- 角色 Tag 与自定义还原角色均可按需生成本地预览；官方角色可跳转 Danbooru，抽卡支持混合、仅 Tag 或仅自定义三种模式及 6、12、24 位数量。
- Tag 更新脚本新增角色目录的热度与名称分页索引，词库格式升级至第 6 版；重新生成当前本地 Tag 快照并保留完整冷门角色。

### 01:19 — 统一全项目二次确认弹窗

- 新增全局共用的项目风格确认弹窗，统一居中位置、明暗主题、遮罩、危险操作配色和明确的操作文案；默认聚焦“取消”，并支持 `Esc` 与点击遮罩关闭。
- 替换未保存离开、参数覆盖、封面替换、删除串、删除或清空生成历史、清除画师串历史组及批量删除灵感图片等全部浏览器原生确认框。
- 普通参数覆盖使用紫色确认操作，删除、清空、重置和丢弃未保存修改使用红色危险操作；项目源码不再调用浏览器原生 `confirm`、`alert` 或 `prompt`。

### 01:05 — 重做实验室重置确认框

- 将浏览器原生确认框替换为与项目明暗主题一致的居中模态框，消除浏览器样式割裂和位置偏上的问题。
- 明确列出将被清空的内容，并提供灰色“取消”和红色“确认重置”操作；默认聚焦取消，同时支持 `Esc` 和点击遮罩关闭，降低误操作风险。

### 00:55 — 恢复实验室快捷重置图标

- 在“保存到库”右侧直接加入红色重置图标，不再使用三点菜单或文字菜单项；点击后仍需确认，避免误触清空。
- 重置图标的右边框与下方内容面板对齐，“保存到库”随之向左移动。

### 00:47 — 对齐实验室保存按钮

- 将实验室顶栏的桌面端左右内边距调整为与下方内容面板一致，使“保存到库”按钮右边框和主体区域右边框对齐。

### 00:41 — 移除实验室重置菜单

- 删除实验室右上角的三点菜单、重置入口及对应重置逻辑，避免为单个不使用的功能保留额外层级。
- “保存到库”现在是实验室顶栏最右侧唯一操作，并自动占用原菜单腾出的位置。

## 2026-07-14

### 23:58 — 修复实验室更多菜单被顶栏裁切

- 将实验室右上角的原生折叠菜单改为独立浮层，避免菜单受到顶栏溢出规则影响而只显示空白边框和内部滚动条。
- 菜单现在会完整显示在顶栏下方，点击空白区域自动收起，关闭后不留下占位或残影。

### 22:55 — 重组编辑器操作与全局设置

- 左侧导航底部新增“全局设置”，统一管理 NovelAI API Key 和 Tag 补全词库；移动端顶栏也提供设置入口。
- API Key 默认仅保存在当前浏览器会话，可选择在本机记住；修改后会立即同步到已打开的实验室和画师预览生成界面，无需刷新。
- 从画师串详情和实验室顶栏移除正负提示词复制按钮及 API Key 输入框；完整提示词复制移至主体区域，负面提示词复制移至对应输入区域。
- 实验室顶栏仅保留主要的“保存到库”操作，并把破坏性的“重置实验室”收进更多菜单；画师串详情继续使用底部保存栏。

### 21:45 — 画师完整目录新增随机抽卡

- 在画师tag页新增随机抽卡工具，可按 6、12 或 24 位抽取并一键更换下一批，卡片仍可收藏、生成预览和加入画师组合。
- 提供“惊喜混合”“完全随机”“热门画师”三种卡池；默认混合活跃画师与完整目录，兼顾可用性和发现冷门画师的惊喜感。
- 抽卡直接按随机位置读取完整 146,835 位本地画师目录，不受当前滚动加载进度影响；同一批不重复，并避开最近五批结果。
- 返回目录时恢复抽卡前的滚动位置，抽取模式和数量会保存在本机供下次沿用。

### 19:45 — 显示画师热度并支持完整目录排序

- 在画师卡片和展开列表中显示 Danbooru `post_count`，以“作品数量”作为画师 Tag 的热度参考。
- 新增热度高到低、热度低到高、名称 A→Z 和名称 Z→A 四种排序，选择会保存在本机并在下次打开时沿用。
- 新增 294 个名称排序分页；两个反向排序通过倒序读取现有分页实现，保证滚动加载到后续页面时仍保持全目录正确顺序。
- 中英文搜索结果也遵循当前排序，不会只调整当前已经显示的卡片。

### 13:50 — 画师目录支持滚动自动加载

- 将 146,835 个画师 Tag 按热度拆分为 294 个本地分页文件，首次读取 500 位，接近页面底部时自动追加下一批 500 位。
- 顶部状态改为明确显示“当前显示 / 完整目录 / 本地预览”，避免把完整目录数量误认为当前已经渲染的数量。
- 保留中英文全目录搜索；搜索直接读取对应索引，不要求先滚动加载到目标画师。
- 移除与热度排序不兼容的 A–Z 快捷栏，并在底部保留手动“加载更多”按钮作为自动触发的备用操作。

### 13:11 — 画师tag改为本地自维护目录

- 移除对长期未维护的 `twoearcat/nai-artists` 图片仓库及遗留 GitHub 导入接口的依赖。
- 画师tag页面现在接入每日更新的中英 Tag 快照，目录包含 146,835 个画师 Tag；默认展示 500 位热门画师，并支持中英文搜索完整目录。
- 新目录中的画师不会预先占用数据库或图片空间；缺少预览时可使用 NovelAI V4.5 按统一测试提示词生成，成功后才把画师和图片保存到本地。
- 页面显示本地预览数量、目录总数和中文解释；Gacha、批量选择、收藏及权重组合均可使用目录画师。
- 按用户要求执行一次性迁移，清除旧仓库导入的全部画师记录及其本地图片，不保留旧数据。

### 11:49 — 增强 Tag 词库更新的网络容错

- 修复连接 `raw.githubusercontent.com` 被重置时只显示“命令执行失败，退出码 1”的问题。
- 下载遇到连接中断、超时等临时网络错误时自动重试；GitHub Raw 多次失败后自动切换到同一 ffdkj 仓库的 jsDelivr 备用线路。
- 弹窗现在会显示连接被中断、超时、地址解析失败或服务器拒绝连接等具体原因，并提示检查网络或代理。
- 下载仍在临时文件中完成，所有线路均失败时保留当前可用词库，不覆盖现有数据。

### 11:36 — 新增 Tag 词库在线更新按钮

- 在实验室右上角新增“Tag 词库”入口，可查看当前 Tag 数量和生成时间，并从页面直接检查更新。
- 新增仅监听本机地址的更新服务，只接受当前本地项目页面发起的请求；下载、生成分片和应用新词库时会显示阶段进度与错误原因。
- 更新时使用上游版本标识进行比较，没有变化便跳过约 30 MB 的重复下载；发现新版后自动生成中英文分片并重新构建。
- 更新完成后自动清理页面中的旧清单和分片缓存，无需刷新网页即可继续使用新词库。

### 11:11 — 改用中英对照 Tag 补全词库

- 将原有 1,045,193 条 Danbooru 官方快照替换为 ffdkj 每日更新的中英对照数据库，共收录 317,337 条使用量不少于 10 的 Tag，并继续合并 NovelAI V4.5 专属 Tag。
- 补全候选现在同时显示英文原名和中文解释，支持直接输入中文搜索；选中候选时仅插入 NovelAI 可识别的英文 Tag。
- 修复使用键盘 `↑`、`↓` 切换候选时高亮项不会跟随滚动的问题，超出可视区域后下拉列表会自动滚动。
- 更新词库生成脚本，英文和中文索引分别按需分片加载；日常使用仍完全读取本地快照，不访问外网。

### 05:08 — 新增实验室 Tag 自动补全

- 在实验室的基础画风、模块、角色正负面、全局负面和主体提示词输入框中加入输入法式 Tag 联想，支持鼠标选择、方向键、`Enter`、`Tab` 和 `Esc`。
- 由 Danbooru 官方 API 生成本地快照，共收录 1,045,193 个未废弃且至少关联一张作品的 Tag；热度仅用于排序，冷门角色、画师和作品 Tag 不会被删除。
- 合并 NovelAI V4.5 官方质量、审美、年代和数据集 Tag，并在候选中标明 Tag 类型与使用量。
- 补全仅替换光标所在的 Tag，完整保留 NovelAI 的 `{}`、`[]` 和 `数字:: ::` 权重结构，不自动添加或平衡权重符号。
- 词库按前两个字符拆分为 1,776 个本地分片，仅在输入时按需加载；新增 `npm run update:tags` 用于手动重新生成最新快照，日常启动不访问外网。

### 04:14 — 更换画师tag导航图标

- 将容易被误认为图片文件的旧图标替换为调色盘图标，使画师、绘画风格和艺术创作的含义更加直观。

### 04:10 — 军火库改名为画师tag

- 将导航栏和项目说明中的“军火库”统一改名为“画师tag”，名称更直接地体现页面用途。

### 04:05 — 统一安全模式图片显示速度

- 修复画师tag和灵感页面解除图片模糊明显偏慢的问题；安全模式现在会覆盖页面自身的 700 毫秒图片过渡，并统一使用 160 毫秒快速显示。
- 保留两页原有的图片懒加载淡入效果，仅在图片被安全模式临时显示时覆盖动画时长。

### 03:58 — 优化安全模式重新遮挡操作

- 已临时显示的图片会在鼠标移出图片区域时立即重新模糊，同时保留再次点击图片进入详情的原有操作。
- 在触屏设备或未直接点击图片时，点击图片外区域会重新遮挡已显示图片；`Esc`、切页和窗口失焦逻辑保持不变。

### 03:48 — 新增全局安全模式

- 在主题切换按钮上方新增“安全模式”开关，开启后全站图片默认以模糊、压暗和降饱和方式遮挡，并在移动端标题栏提供快捷开关。
- 首次点击图片只临时显示该图片并拦截原操作，再次点击才执行原操作；按 `Esc`、切换页面、窗口失焦或标签页进入后台时自动重新遮挡。
- 安全模式开关状态保存在浏览器本地设置中，刷新页面或重新启动后仍保持上次选择。

### 03:28 — 优化本地启动速度

- 启动时仅在源码更新或构建产物缺失时运行 TypeScript、Vite 和 Worker 构建；代码未变化时直接启动服务。
- 直接使用项目内安装的 Wrangler，移除重复的版本检查和 `npx` 启动开销。
- 关闭 Wrangler 交互式控制台菜单，减少启动窗口中的无关输出。

### 03:19 — 移除持久化操作日志

- 停止前端发送操作审计事件，后端不再写入 `access_logs` 或每日使用统计。
- 停用日志查询、日志清理和客户端日志接口；首次运行新版本时自动删除旧日志与统计表。
- 保留浏览器控制台和本地启动窗口中的错误输出，便于临时故障排查。

### 03:13 — 桌面启动器改为直接启动

- 双击桌面 `NaiPromptManager.bat` 后直接检查项目、Git 分支和 npm，然后启动本地服务并自动打开浏览器。
- 移除启动/备份选择菜单；服务停止后直接退出启动器。

### 03:08 — 改为完全个人模式

- 移除账号密码登录、游客口令、退出登录、“我的/用户管理”、角色徽章和存储配额界面。
- 后端不再读取 Session Cookie，所有请求自动使用本机所有者身份；沿用旧管理员 ID 以兼容现有数据归属。
- 移除“游客不可见”和内容作者标识，所有本地内容均可直接编辑管理。
- 账号、游客、密码和用户管理接口在个人模式下停用；个人构建仅允许由本地启动脚本启用，禁止作为公开云端 API 使用。

### 02:48 — 更新日志改为时间线

- 更新日志改为“日期 → 时间”的倒序结构，方便按实际修改顺序查看。

### 02:47 — 补充更新日期

- 将无日期的“未发布”记录合并到 `2026-07-14`，以后不再使用无日期条目。

### 02:36 — 历史图片迁移到本地服务

- 本地生成历史由浏览器 IndexedDB 迁移到本地服务的 D1 + R2，图片和参数现在随 `local-data` 一起备份。
- 新增旧历史自动迁移：分批保存到本地服务，全部成功后才清除浏览器副本；失败时保留原数据。
- 本地历史接口仅由本地启动脚本显式启用，云端部署默认关闭，避免私人历史被上传到 Cloudflare。
- 历史图片改为独立资源地址并启用浏览器私有缓存，分页不再传输 Base64 JSON。

### 02:21 — 建立更新日志

- 新增 `CHANGELOG.md`，并在 README 顶部加入更新日志入口。

### 02:18 — 更新本机启动脚本

- 更新本机桌面启动脚本的分支检查：由旧分支 `Qun` 改为 `main`。
- 移除启动菜单和窗口标题中遗留的 `Qun` 名称。

### 02:14 — 修复历史页面

- 修复“历史”页面无法即时显示新生成图片的问题；新增本地历史变更通知，并支持同一浏览器不同标签页之间同步刷新。
- 修复右上角“刷新”按钮被同页缓存逻辑跳过、无法读取新记录的问题。
- 历史分页改为通过 IndexedDB 时间索引只读取当前页数据，不再加载并排序全部 Base64 图片。
- 修复快速切换页码时旧请求覆盖新页面、相邻页预加载使用旧页码清理缓存的问题。
- 修复清空历史后仍残留旧页码、缓存和图片详情的问题。
- 修复跳页功能可能选中页面内其他输入框的问题。
- 为删除、清空和清理操作补充错误提示及无效数字校验。
- 图片改为异步解码，并保留当前页相邻页面预加载和三页缓存。

### 01:52 — 建立独立仓库

- 将当前版本设为独立 Git 仓库，以 `main` 作为唯一主分支，并移除原项目远程地址及提交历史。
- 在 README 中保留原项目来源说明，新增 MIT `LICENSE` 文件并继续采用相同开源协议。

## 2026-07-03

### 04:06 — 压缩 AITag 筛选控件文字间距

- 将 AITag 四个下拉筛选的左侧内边距缩小约一半，并将字号降低一级，改善等宽窄按钮中的文字利用率。
- 搜索输入框和搜索按钮保持原有尺寸，不受本次紧凑化影响。

### 03:41 — 对齐 AITag 筛选栏与作品详情栏

- 将 AITag 顶部筛选区域与右侧 `460px` 作品详情栏按同一网格列布局，使 NAI 类型筛选的左边框不再超出详情栏左边缘。
- NAI/SD/ComfyUI、缓存状态、最新/月榜和月份四个下拉框改为等宽分配，切换筛选时不再出现明显宽度跳动。
- 月份选择框在“最新”模式下继续保留空白占位，避免最新与月榜之间切换造成工具栏重排。

### 03:15 — 新增月榜月份目录并修复作品页数显示

- AITag 月榜接入 aitag.win 的可用月份配置，新增当前月份、历史月份和更早月份选择，不再将月份列表写死在前端。
- 当前月份使用实时月榜接口，历史月份使用固定月份接口；不同月份分别保存和读取本地缓存，避免相互混入。
- 修复部分作品组列表显示 `0P` 的问题，图片数量会依次从列表字段、详情图片和原始图片 URL 中推导。
- 修复当前月份的页数上限被本地缓存页或当前页错误覆盖的问题，远程请求成功时以 aitag.win 返回的总数为准。

### 02:47 — 精简 AITag 作品卡片

- 隐藏卡片标签列表并压缩底部信息区域，减少标题下方无效占位。
- 收藏爱心移动到标题行右侧并与标题垂直居中；浏览量、收藏量和作品 ID 保持单行横向排列。
- 统一蓝色首图缓存标记和绿色完整缓存标记的位置，固定贴在卡片底边，不再受标题或统计文字高度影响。

### 01:56 — 新增 AITag 本地收藏

- 每个 AITag 作品组新增爱心收藏按钮，收藏状态保存在本地数据库并能即时更新卡片。
- 缓存筛选下拉新增“收藏”，排列在“全部”之后，可只查看已收藏的作品组。
- 收藏操作与首图缓存、完整详情缓存相互独立，不会为了收藏而删除或降级已有缓存。

## 2026-07-02

### 21:55 — 改用缓存完成事件更新 AITag 卡片状态

- 首图保存完成后由后端长轮询结果直接通知当前页面更新对应作品，不再只依赖固定间隔反复查询。
- 修复首图已经写入本地但卡片长期保持无色、刷新后才变蓝的问题；切页、筛选或组件卸载时会取消旧等待请求。
- 避免在不同 Cloudflare Worker 请求之间复用流或请求对象，修复 `Cannot perform I/O on behalf of a different request` 与 `Promise will never complete` 错误。

### 21:04 — 防止最新作品被旧缓存覆盖

- 修复“最新”列表先正确显示 aitag.win 新作品，数秒后又被本地旧记录混入并覆盖顺序的问题。
- 后台首图状态同步只合并当前页已有作品的缓存字段，不再把缓存查询返回的其他作品重新插入远程列表。
- 保留远程列表的原始顺序、总数和当前页，只更新本地封面 URL、缓存级别与收藏状态。

## 2026-06-29

### 00:35 — 完善 AITag 详情渐进加载提示与深色滚动条

- 首图预览模式的“其余图片正在本地补全”提示移到第一张图提示词下方、第二张图原本出现位置的顶部，不再挤占首图卡片空间。
- 完整详情已存在时立即显示元数据和已有图片，后台继续补齐其余本地图，不再用整页“加载详情中”遮挡内容。
- 调整深色主题滚动条颜色，使其与夜间背景协调，同时保留足够对比度以便识别和拖动。

### 00:02 — 修正 AITag 完整缓存状态即时更新

- 修复作品组完整下载后卡片短暂变绿又退回蓝色、刷新后才恢复绿色的问题。
- 详情缓存完成后同时刷新当前卡片、详情状态和底部“完整保存”计数，避免旧列表响应覆盖新缓存级别。
- “已缓存全部”严格表示完整详情 JSON 与组内全部图片均已保存到本地；仅有首图或部分图片时仍显示为蓝色首图缓存。

## 2026-06-28

### 23:31 — 首图详情先展示、其余内容后台补齐

- 点开仅保存首图的作品组时，立即显示第一张本地图及其元数据，再异步获取和保存组内其余图片与详情。
- 移除图片补全失败后的重试冷却限制；每次需要补全时都允许立即重试，失败不会阻塞已有详情展示。
- 对本地已有详情但图片不完整的旧缓存进行懒检查和兼容升级，不清空现有 AITag 缓存。

### 20:55 — 修正“已缓存全部”的判定含义

- 将详情缓存拆分为 `hasCachedDetail` 与 `hasFullyCachedImages` 两个状态，只有两者同时满足才归入“已缓存全部”。
- 修复部分作品虽然保存了详情 JSON，但组内图片没有全部落地，却被错误标成完整缓存并在再次点开时重新加载的问题。
- 本地详情命中后先立即返回，缺失图片在后台补齐；图片仍可使用远程 URL 兜底，不影响参数查看和导入。

### 19:43 — 增加 AITag 缓存级别视觉区分与筛选

- 作品卡片按缓存级别显示不同底色：完整缓存使用较深绿色、仅首图使用蓝色、未缓存保持普通底色。
- 顶部类型筛选和缓存筛选改为下拉式，新增“全部、已缓存全部、已缓存首图”等本地缓存视图。
- 后续精简掉没有实际使用价值的“未缓存”筛选，并将“缓存全部”文案缩短为“全部”。

### 17:38 — 让完整保存计数实时刷新

- 修复在当前页点开并完整保存作品后，“完整保存”数量要等切到下一页才一次性增加的问题。
- 后端详情保存成功后即返回最新缓存状态，前端同步更新底部统计与当前作品卡片。
- 清理与“看过作品”重复的统计逻辑，只保留“首图保存”和“完整保存”两个有明确含义的计数。

### 12:25 — 重构为“看过留下、点开保存”的 AITag 缓存

- 移除手动输入目标页数以及启动、暂停、继续后台索引的复杂流程，改为随浏览自动建立本地缓存。
- 访问列表页时保存该页作品的标题、标签、AI 类型、作者、图片 URL、页码等基础信息，并将每组第一张图及第一张图元数据保存到本地。
- 点开作品组时保存完整详情 JSON 和组内全部图片；再次打开时优先读取本地详情与本地图。
- 联网时优先读取 aitag.win 最新列表并写入缓存；远程不可用时回退到看过的本地页面，未看过的页面显示明确离线缺失提示。
- 底部状态改为“首图保存”和“完整保存”，并增加 aitag.win 红绿连接状态指示。

### 10:10 — 验证并调整 AITag 类型过滤策略

- 清空一次早期测试缓存，重新验证 aitag.win 的 NAI、SD 和 ComfyUI 查询行为。
- 类型筛选使用排除法构造远程查询，例如 NAI 排除 SD 与 ComfyUI，避免简单搜索 `q=NAI` 将标题、作者名或其他文本中的 NAI 误判为类型。
- 保留页面浏览类型筛选，并将缓存相关类型选择收纳为下拉控件，避免平铺按钮占用工具栏空间。

### 09:10 — 建立首版 AITag 本地缓存与离线兜底

- 在本地 D1 新增作品列表、作品详情和索引状态表，缓存数据随 `local-data` 一起备份，不触碰画师串、灵感和历史表。
- 新增本地缓存搜索、状态、索引、暂停和继续接口；列表优先读取本地记录，不足时通过 aitag.win 代理补齐。
- 首版支持按前 N 页后台索引、节流请求和详情按需归档，为快速跳页与远程故障兜底提供基础。
- 后续根据实际使用体验，将这套手动索引方式替换为自动的“看过留下、点开保存”模式，旧表保留用于兼容而不删除数据。

### 07:48 — 增强 AITag 跳页与生成信息展示

- 页码显示改为可点击输入，支持直接输入目标页并跳转，同时限制在有效页数范围内。
- 作品详情除模型名称外，新增文生图、图生图、局部重绘、氛围参考和角色参考等可从元数据识别出的生成类型标签。
- 打开作品大图详情时隐藏列表页顶部统计和筛选内容，避免工具栏继续占用大图浏览空间。
- 直接跳转较远页面速度受 aitag.win 远程请求限制，这一问题随后推动了本地缓存与离线归档的实现。

### 07:10 — 增加画师串历史生成图片组浏览

- 从画师串进入实验室时，预览区可左右查看该画师串曾经生成的图片，普通预览和大图模式均支持按钮与键盘 `←`、`→` 切换。
- 历史组按画师串 ID 关联，因此后续修改提示词仍可继续查看该串此前生成的图片。
- 新增“删除当前”和“清除全部”操作，只从当前画师串的可滑动图片组中移除关联，不删除本地历史原图；清除全部带项目内二次确认。
- 大图查看时隐藏预览区左上角管理按钮，普通预览中的操作按钮改为竖向排列，避免遮挡图片。

### 06:30 — 新增详细使用日志页

- 在“我的”页面的使用统计与个人设置之间新增日志查看区域，记录页面操作、AITag 请求、缓存处理和错误等详细事件。
- 日志功能后来在完全个人模式改造中被整体移除，历史记录仅用于说明当时的维护阶段。

### 05:30 — 保持各功能页切换状态

- AITag 页面切换到其他功能页再返回时，保留列表、页码、筛选、已打开详情和滚动位置，不再重新加载并丢失浏览进度。
- 将相同的页面状态保留思路扩展到画师串、画师 Tag、角色、灵感、历史和实验室等主要功能页。
- 修复从 AITag 或历史导入实验室时只完成页面跳转、参数没有填入的问题，导入数据改为在实验室挂载后可靠消费。

### 04:45 — 重做 AITag 详情快捷操作和筛选布局

- 删除列表顶部“复制第一张画师串”按钮，将操作集中到用户点开的作品大图详情中。
- 详情操作改为“保存画师串、收藏灵感、导入实验室”三个纯图标彩色按钮，并按画师串、灵感、实验室顺序排列。
- 灵感导航和相关操作统一使用星星图标，画师串操作使用与导航一致的收纳图标。
- 将 AI 类型、缓存状态、最新/月榜等筛选收纳进顶部工具栏，减少独立标签栏占用的垂直空间，并修复末行卡片下方异常大块空白。

### 03:10 — 新增 AITag 页面

- 在画师 Tag 下方、灵感上方新增 AITag 功能页，通过本地 Worker 代理读取 aitag.win 的 Pixiv AI 作品列表与详情。
- 支持搜索、最新/月榜、NAI/SD/ComfyUI 类型筛选、列表分页、作品组详情和多图元数据显示。
- 详情中的图片参数可一键导入实验室、保存为画师串或收藏到灵感；保存前统一解析 Prompt、Negative、模型、尺寸、采样器和其他生成参数。
- 修复从画师串进入实验室时侧栏错误点亮“角色串”的导航状态，并为灵感入口换用独立星星图标。

### 01:00 — 修复灵感完整保存负面提示词

- 统一 AITag、历史和其他入口收藏到灵感的数据结构，保存图片时同时写入正面提示词、全局负面提示词和完整生成参数。
- 修复灵感图片元数据缺少 `Negative`、再次导入实验室无法还原负面提示词的问题。

### 00:45 — 修复历史导入与页面填充

- 从历史图片选择“导入到编辑器”时补齐全局负面提示词，不再只导入正面 Prompt 和基础参数。
- 历史页面主内容区域改为填满剩余高度，消除列表底部多出一整栏空白的问题。

## 2026-06-27

### 05:15 — 实验室支持粘贴和导入 JSON 元数据

- 在实验室新增 JSON 导入入口，既可选择 `.json` 文件，也可在弹窗文本框中直接粘贴完整 JSON 数据。
- JSON 与 PNG 元数据走同一套 NovelAI 参数解析逻辑，可还原 Prompt、Negative、尺寸、步数、采样器、种子、角色和相关生成参数。
- 为格式错误、缺少元数据或无法识别的 JSON 增加明确提示，不覆盖当前正在编辑的内容。

### 04:40 — 实验室支持跨窗口拖入 PNG

- 为实验室图片预览与导入区域增加全局拖放支持，可直接从资源管理器或其他窗口拖入 PNG 并读取 NovelAI 元数据。
- 保留原有点击按钮选择文件方式；拖入非图片、多个不支持文件或无法读取元数据时显示错误提示。
- 拖放期间增加明确的悬停视觉反馈，并阻止浏览器把图片当作页面导航目标打开。

### 04:13 — 建立 Qun 本地个人版与数据保护流程

- 在原目录 `D:\NaiPromptManager` 建立本地 `Qun` 分支并提交已有个人修改，不复制第二套项目目录。
- 作者 GitHub 仅保留为更新下载来源，禁用远程 push 并设置普通 `git push` 不执行，避免个人修改上传到原作者仓库。
- 确认 `local-data` 由 `.gitignore` 排除；桌面启动器改为只启动 Qun、备份本地数据或退出，移除自动 `git pull` 更新入口。
- 修复 Windows 启动脚本读取当前 Git 分支为空、错误阻止 Qun 启动的问题，并完成一次代码与 `local-data` 状态备份。
- 建立更新前关闭服务并备份到 `D:\NaiPromptManager-Backups\时间戳\local-data` 的流程，任何作者更新都先备份、再 fetch 与合并。
