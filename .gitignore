# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets and local environment overrides
.env
.env.*
!.env.example
!.env.sample
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json

# TypeScript build cache
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
plans/*
docs/plans/
.planning

# 本地部署数据：local-data/ 为不可再生核心数据（D1/R2/密钥/会话），禁止删除；
# local-cache/ 为可再生成缓存（缩略图/模型），可清理但删除只影响速度。
local-data/
local-cache/

# Wrangler 本地缓存
.wrangler/

# Wrangler 运行调试日志（local-server.mjs 重定向到项目内，避免撑爆系统盘；可整体删除）
.wrangler-logs/

docs/db-field-addition-guide.md
public/tag-data/
